# Planning Panel — Adversarial & Meta-Audit Findings

**Panel health.** {"planners_ok": 3, "adversaries_ok": 2, "meta_audit_ok": 1, "findings": 37, "blockers": 2, "majors": 14}  ·  degraded lenses: `[]`

Every citation sampled from these findings was independently re-verified against the
working tree before the plan was written; all resolved. See the plan's
§Code-grounding verification.


---

## Convergence map (where >= 2 planners agreed)

### Do NOT replace implement-plan's terminal template token with a `<placeholder>` — `SKILL_STATUS_TEMPLATE`'s `[a-z-]+` cannot match a leading `<`, so the conformance guard would go VACUOUS rather than green, silently dropping that skill from coverage.

- **Planners:** code-grounded, sequencing, domain-convention
- **Why high signal:** All three arrived at it independently by reading the same regex at tests/test_repo_structure.py:41, and all three flagged it as the most tempting wrong move because it reads as the cleanest fix. I re-verified the regex directly. It is the fix recreating its own bug class — a green suite with less coverage than before — which is precisely the failure mode the RCA diagnoses. It is an acceptance criterion in Phase 1, not a note.

### No shared prose home for the stage vocabulary: keep the six restatements and make each cite its track README, because the guard is what stops drift and a seventh register would not.

- **Planners:** code-grounded, sequencing, domain-convention
- **Why high signal:** This is the ONE design call the RCA explicitly handed to the plan (:126-129), and three planners reasoning from different lenses — architecture, sequencing, and convention — converged on the same answer with three non-overlapping arguments: the template is the copy source, `_declared_stage_tokens()` parses from exactly the three READMEs so relocating would break the parser, and requests/README.md:16 already assigns the authority. Independent convergence on a genuinely open call is the strongest signal available here.

### Prose must land inside the fence-blind `_advance_status_section` window (create-implementation-plan :167->:183, implement-plan :254->:275), and placement must be verified mechanically rather than by eye.

- **Planners:** code-grounded, sequencing, domain-convention
- **Why high signal:** All three measured the same windows and all three named it the highest-probability way the phase looks done and is not — the natural place to add prose (below the fence, near the template body) is outside the window. Two independently proposed calling the helper in a REPL to confirm. Verified: `NEXT_SECTION` at :53 is `^##\s` with no fence awareness, and both windows stop at a `##` heading inside a code fence.

### The unguarded stage-DISCOVERY readers (create-implementation-plan:56, :65-66; implement-plan:61, :92-93) must move in the same commit as the writers.

- **Planners:** code-grounded, sequencing
- **Why high signal:** Two planners found these by grep, independently, and neither the RCA nor the intake enumerates them. I verified all four verbatim. They are FUNCTIONAL, not cosmetic: land the token rename alone and the next pipeline run hunts the Index for a word that no longer exists in the repo. No test covers any of them, so nothing would go red — the same silence that hid three of the four original defects.

### Both upstream artifacts mis-cite the `_done/` skip as tests/test_request_links.py:36; it is at :41.

- **Planners:** code-grounded, sequencing
- **Why high signal:** Two planners caught the same off-by-five independently and I confirmed it: :36 is the `def _scanned_files()` line, :41 is the generator with the `"_done" not in p.parts` filter. Harmless to the fix, but a cold agent following the citation lands on a signature and may conclude the artifact is wrong about more than it is. The correction is recorded rather than left for rediscovery.

### The `next:` field gets a one-word prose fix and NO mechanical guard — its live values are free-form actions and targets, so any regex is vacuous or reds skills that are not wrong.

- **Planners:** code-grounded, sequencing
- **Why high signal:** Both declined RCA hardening (a) beyond its cheapest point, and the third planner's counter-proposal (declare a next-action vocabulary and guard it) is directly contradicted by requests/feature-requests/README.md:104, which types the field loosely, and by diagnose-bug/SKILL.md:102's free-form `<feature-slug>`. Two-planner convergence plus a verified contradiction is why the third proposal is dropped with a reason rather than folded in.

### The `reviews/` carve-out must be explicit in any 'roll every artifact' instruction, because the invariant-A guard globs non-recursively.

- **Planners:** code-grounded, domain-convention
- **Why high signal:** Both traced it to the same line (tests/test_repo_structure.py:479) and to the same live example. I verified `_done/1.1-app-shell/reviews/preflight.md:1` carries `> **Status:** preflight`, a word no grammar declares, green only because of that non-recursion. Without the carve-out the new instruction invites the next agent to retroactively restage panel provenance — a real harm introduced by the fix itself.

### Phase-final close-out is the fix dogfooding itself: roll this item to `fixed` (not `implemented`) across all four artifacts plus the Index, and archive into `_done/` with the link updated in the same edit.

- **Planners:** code-grounded, sequencing, domain-convention
- **Why high signal:** All three make it the last phase and all three note that a `_done/` move with a stale link makes the Index guard VACUOUS rather than red (the `continue` at :476-477). Two independently observe that this is the first complete run of the bugfix track, so anything surprising about the archive path is new information about the contract. It is also the strongest available end-to-end acceptance: if the newly-fixed skills, followed literally, leave the repo green, defects 1 and 3 are demonstrably closed.

### The three repro tests are the acceptance contract and must never be edited, relaxed, or reworded to make a fix pass.

- **Planners:** sequencing, domain-convention
- **Why high signal:** Both state it as a hard rule, and one adds the precise carve-out: the only sanctioned touch is Phase 3's helper refactor, whose acceptance must preserve the conformance test's UNION semantics (a skill legitimately serves several tracks). Narrowing that helper to one track would be a silent regression dressed as hardening — the same shape as the bug.


---

## Adversary findings

### F1 — EDIT D fixes the work-dir template but leaves the track-README template on the same line non-composing for calibration

- **Severity:** major · **Confidence:** high · **Category:** correctness · **Reviewer:** code-grounded
- **Location:** `.claude/skills/implement-plan/SKILL.md:55`
- **Problem:** Line 55 reads verbatim: "`<work-dir>` = `requests/<track>-requests/<slug>/`; the **track README** (whose Index you advance) is `requests/<track>-requests/README.md`." Phase 1 EDIT D and its acceptance criterion ("names three tracks and three LITERAL work-dir paths") address only the FIRST half of that sentence. The second half is the same non-composing template applied to the file the skill actually writes the terminal stage word into: for a calibration item it resolves to `requests/calibration-requests/README.md`, which does not exist (the directory is `requests/calibration-findings/`, verified — `requests/calibration-findings/README.md:88` is where the `retuned` grammar lives). So a three-valued resolution that derives `retuned` correctly would then be told to advance an Index in a file that isn't there. This is exactly the failure mode EDIT D was written to prevent, one clause to the right, and the plan's own argument for EDIT D ('the template cannot compose for calibration') applies verbatim. `create-implementation-plan/SKILL.md:51` carries the identical construction and has the same gap under Phase 2's alignment step.
- **Proposed fix:** Extend EDIT D (and Phase 2's alignment of create-implementation-plan:49-51) to replace BOTH templates on those lines with the three literal pairs: feature → `requests/feature-requests/<slug>/` + `requests/feature-requests/README.md`; bugfix → `requests/bugfix-requests/<slug>/` + `requests/bugfix-requests/README.md`; calibration → `requests/calibration-findings/<slug>/` + `requests/calibration-findings/README.md`. Add to Phase 1's acceptance: 'implement-plan:53-55 names three tracks, three literal work-dirs, AND three literal track-README paths; the string `<track>-requests` no longer appears in either back-half skill.'

### F2 — Phase 1's mandatory baseline pins HEAD to fb0406e, which the plan's own commit cadence guarantees will be false

- **Severity:** major · **Confidence:** high · **Category:** sequencing · **Reviewer:** code-grounded
- **Location:** `requests/bugfix-requests/pipeline-status-rollover/ROOT_CAUSE_ANALYSIS.md:34`
- **Problem:** Phase 1 step 1 states three hard preconditions before any edit: branch name, empty `git status --short`, and "`git log --oneline -1` must show `fb0406e`". The first two are right. The third cannot hold: this plan is itself a stage-3 deliverable that `.claude/skills/create-implementation-plan/SKILL.md:235` hands to `/commit` to land, and the plan's own files_to_touch entry for `IMPLEMENTATION_PLAN.md` says it is 'Written by this stage, opening at `planned`' and rolls to `fixed` in Phase 4 — i.e. it is committed before implementation begins. At Phase 1 start HEAD is therefore the plan's commit (with BUGFIX_REQUEST.md, ROOT_CAUSE_ANALYSIS.md and the Index all at `planned`), not fb0406e. A cold implementer following the precondition literally halts on a healthy tree. The pytest-count check in the same step is the one that actually discriminates 'wrong branch', and it stays valid (still 3 failed / 79 passed after the plan lands, since the item directory is internally consistent).
- **Proposed fix:** Replace the HEAD equality check with an ancestry check: `git log --oneline | Select-String fb0406e` must return a hit (or `git merge-base --is-ancestor fb0406e HEAD` exits 0), phrased as 'fb0406e must be in this branch's history — it carries the RCA and the three red tests and is not on main.' Keep the `3 failed, 79 passed` assertion as the real stop condition, and note explicitly that HEAD will normally be the commit that landed this plan.

### F3 — Phase 2's authority line lands inside the copied fenced template for five of the six skills

- **Severity:** major · **Confidence:** high · **Category:** convention · **Reviewer:** code-grounded
- **Location:** `.claude/skills/scope-feature/SKILL.md:148`
- **Problem:** Phase 2 says 'Add ONE authority line beside each of the six template blockquotes ... Apply the same to scope-feature (beside :149), diagnose-bug (beside :107), make-feature-request (beside :176), make-bugfix-request (beside :130)'. I checked each: in all five cases the blockquote is the FIRST line inside a ```markdown fence (scope-feature opens at :148, diagnose-bug at :106, make-bugfix-request at :129, make-feature-request at :175, create-implementation-plan at :175). Content placed beside it is inside the block an agent copies verbatim into the artifact, so every future PROJECT_SCOPE, ROOT_CAUSE_ANALYSIS, FEATURE_REQUEST and BUGFIX_REQUEST would open with a meta-note about which test enforces its stage word. That this propagates is not hypothetical — `requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:3-6` carries exactly such an HTML comment, copied out of the template region. Only create-implementation-plan's existing precedent (:177-178) is at least an HTML comment; the plan does not tell the implementer to match that form elsewhere.
- **Proposed fix:** Prescribe placement and form explicitly: put the authority line in the PROSE immediately ABOVE the opening fence (the shape Phase 1 EDIT F already uses for implement-plan at :267), not inside it. If a note must live inside a template, require the `<!-- … -->` comment form and say so. Add a Phase 2 acceptance criterion: 'no authority line sits between a ```markdown fence opener and its template body' — checkable by eye on a five-line diff.

### F4 — Phase 2's acceptance criterion names a path template that does not resolve in the skills' own vocabulary

- **Severity:** minor · **Confidence:** high · **Category:** correctness · **Reviewer:** code-grounded
- **Location:** `tests/test_repo_structure.py:67`
- **Problem:** Phase 2's acceptance reads: 'All six .claude/skills/*/SKILL.md template blockquotes carry an adjacent line naming `requests/<track>/README.md`'s **Status grammar** as the source of truth.' Two different `<track>` vocabularies collide here. In `_declared_stage_tokens()` (tests/test_repo_structure.py:67) `track` is the DIRECTORY name — `feature-requests` / `bugfix-requests` / `calibration-findings` — so `requests/<track>/README.md` is right in test-space. But inside the skills, `<track>` is explicitly defined as `bugfix` or `feature` (implement-plan:53-54, create-implementation-plan:49-50), so writing the literal string `requests/<track>/README.md` into a skill points at `requests/bugfix/README.md`, which does not exist. An implementer copying the acceptance criterion verbatim into five skills writes five broken pointers (harmless to test_request_links, since backticked text is not a markdown link — but misleading to every cold agent, which is the harm this whole bug is about).
- **Proposed fix:** State the literal paths in the acceptance criterion instead of the template: 'each authority line names `requests/feature-requests/README.md`, `requests/bugfix-requests/README.md`, or `requests/calibration-findings/README.md` — whichever the resolved track uses — never a `<track>` template.'

### F5 — create-implementation-plan:23 carries a second copy of the two-valued, non-composing work-dir template and is absent from files_to_touch

- **Severity:** minor · **Confidence:** high · **Category:** completeness · **Reviewer:** code-grounded
- **Location:** `.claude/skills/create-implementation-plan/SKILL.md:23`
- **Problem:** Line 23 reads: "An `IMPLEMENTATION_PLAN.md` in the resolved work-dir (`requests/<track>-requests/<slug>/` — feature or bugfix, see Step 1)". This is the same non-composing template and the same two-valued framing ('feature or bugfix') that Phase 2 corrects at :49-51, but it sits in the What-this-produces section and is not named anywhere in the plan's files_to_touch entry for that file (which lists only :49-51, :56, :65-66, :171-173, :176-178). Leaving the two statements divergent inside one file is the same drift-seed the plan's own Phase 2 rationale warns about ('leaving the two back-half skills stating the same sentence differently is how the next drift starts'). The equivalent line in implement-plan (:51, 'on either track') has the same shape.
- **Proposed fix:** Add `.claude/skills/create-implementation-plan/SKILL.md:23` and `.claude/skills/implement-plan/SKILL.md:51` to Phase 2's alignment step, and add an acceptance grep: `Select-String -Path '.claude/skills/*/SKILL.md' -Pattern 'track>-requests'` returns nothing. Optionally note that both frontmatter descriptions (create-implementation-plan:13-14, implement-plan:13-14) also say 'BOTH tracks' and are deliberately left alone because they drive skill dispatch.

### F6 — The report-template line range :267-300 overshoots the fence by three lines

- **Severity:** nit · **Confidence:** high · **Category:** citation-accuracy · **Reviewer:** code-grounded
- **Location:** `.claude/skills/implement-plan/SKILL.md:297`
- **Problem:** The plan cites the IMPLEMENTATION_REPORT template as '.claude/skills/implement-plan/SKILL.md:267-300' in two places (Phase 4 step 3 and the code_references entry 'The report template runs :267-300'). Verified: the ```markdown fence opens at :267 and CLOSES at :297; :298 is blank, :299 is a `---` horizontal rule, :300 is blank. The range runs three lines past the template into the section separator. Harmless in itself, but this plan's whole premise is that a cold implementer trusts line citations literally, and Phase 4 tells them to write the report 'following the ... template (:267-300)'.
- **Proposed fix:** Change both citations to `:267-297`.

### F7 — make-bugfix-request:6 restates the retired stage chain too, and Phase 2's grep acceptance does not say whether it counts

- **Severity:** minor · **Confidence:** high · **Category:** completeness · **Reviewer:** code-grounded
- **Location:** `.claude/skills/make-bugfix-request/SKILL.md:6`
- **Problem:** Phase 2's acceptance says: "`grep -rn 'root-cause' .claude/skills/*/SKILL.md` returns only English-prose occurrences (the skill descriptions), no stage tokens and no `next:` values." I ran that grep. After Phase 1 and Phase 2 the survivors are diagnose-bug:7 ('intake -> root-cause -> reuse plan/implement'), make-bugfix-request:5 ('root-cause analysis' — genuine English), and make-bugfix-request:6 ('BUGFIX pipeline (intake -> root-cause -> reuse plan/implement)'). Gated decision 4 explicitly rules on diagnose-bug:7 and recommends leaving it, but make-bugfix-request:6 is the same construction and is never named. An implementer hitting it has no ruling and will either change it (degrading the natural-language dispatch trigger the gated decision was protecting) or leave it and wonder whether the acceptance criterion passed.
- **Proposed fix:** Extend gated decision 4 to name both sites — `.claude/skills/diagnose-bug/SKILL.md:7` and `.claude/skills/make-bugfix-request/SKILL.md:6` — and make Phase 2's acceptance concrete: 'the only surviving `root-cause` hits are diagnose-bug:7, make-bugfix-request:5 and make-bugfix-request:6, all inside frontmatter descriptions, left deliberately per the gated decision.'

### F8 — Phase 3 says to 'reuse' a regex that is function-local and cannot be imported

- **Severity:** nit · **Confidence:** high · **Category:** correctness · **Reviewer:** code-grounded
- **Location:** `tests/test_repo_structure.py:465`
- **Problem:** Phase 3's GUARD 1 step says 'Reuse its `stage_cell` regex from :465 to walk each track README's Index rows.' Verified: `stage_cell` at :465 is a local variable bound inside the body of `test_index_stage_cells_match_their_artifact_status_headers`, not a module-level constant like `SKILL_STATUS_TEMPLATE` (:41) or `STATUS_GRAMMAR` (:38). It cannot be referenced from a sibling test without hoisting it to module scope or duplicating the pattern. The files_to_touch entry gets this right ('reusing the `stage_cell` regex shape at :465'); the phase step does not, and the phase step is what the implementer follows.
- **Proposed fix:** Reword the step to 'copy the pattern at :465, or hoist it to a module-level `INDEX_STAGE_CELL` constant beside the other regexes at :38-60 and have both tests use it' — and if hoisting, say so, since it edits a line inside the invariant-A guard the plan otherwise declares untouchable.

### F9 — RCA Open Question 3 is treated as unconfirmed when the repo already documents the answer that EDIT D depends on

- **Severity:** question · **Confidence:** medium · **Category:** grounding · **Reviewer:** code-grounded
- **Location:** `requests/calibration-findings/README.md:82`
- **Problem:** The plan's conventions state 'RCA Open Question 3 (calibration routing) stays `unconfirmed` in this plan deliberately — no phase depends on it.' But Phase 1 EDIT D adds a `calibration` branch to implement-plan's track resolution precisely because the terminal must be `retuned` for that track, so a phase does depend on the claim that calibration reaches the shared back half. Two artifacts settle it: `requests/calibration-findings/README.md:82-83` lists `IMPLEMENTATION_PLAN.md  # stage 3 — includes the replay plan` and `IMPLEMENTATION_REPORT.md  # stage 4` in its own layout, and `requests/README.md:17-19` states 'The back half (`/create-implementation-plan` → `/implement-plan`) is shared and auto-detects the track from the artifact's path.' That is documented-by-contract, not merely unconfirmed-by-observation. Nothing in the fix changes as a result — but calling it unconfirmed while acting on it is exactly the epistemic mislabel CLAUDE.md's 'label your epistemics' rule targets, and the correct label makes EDIT D grounded rather than speculative.
- **Proposed fix:** Relabel: the track's ROUTING is `verified` from `requests/calibration-findings/README.md:82-83` + `requests/README.md:17-19`; what stays `unconfirmed` is only whether the track has ever been EXERCISED (its Index at :94 is the placeholder row). Cite both lines in EDIT D's rationale so the third branch has a source.

### F10 — The escalation-format citation for 'a decision you would rather park' points at the wrong file

- **Severity:** nit · **Confidence:** high · **Category:** citation-accuracy · **Reviewer:** code-grounded
- **Location:** `ESCALATIONS.md:12`
- **Problem:** The decisions entry for the parked `_done/` trade-off says 'ESCALATIONS.md:12-14 is exactly where a decision you would rather park than answer belongs.' Verified: ESCALATIONS.md:10-13 says something adjacent but different — that the file records 'what someone did while the question stayed open', with the load-bearing part being the assumption. The rule that a parked item must be 'A decision with alternatives' is at ESCALATIONS.md:17-18, and the exact phrasing quoted ('a decision you would rather park than answer now belongs in ESCALATIONS.md') is `requests/README.md:12-14`, which I confirmed verbatim. Phase 4 also cites `plan-adversarial.md:216-217` as the sole archived record of defect 2; there is a second one — `requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:3-6` carries an HTML comment saying 'The create-implementation-plan SKILL.md template says `plan`; the repo's own contract wins' — a hand-worked-around instance that strengthens the escalation's 'found, recorded, and lost' evidence.
- **Proposed fix:** Cite `requests/README.md:12-14` for the parking rule and `ESCALATIONS.md:17-18` for the 'decision with alternatives' test. Add `requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:3-6` to the E-010 body as a second archived instance.

### E2-01 — Phase 1's preflight gate pins HEAD to fb0406e — a state that cannot exist when the plan is executed

- **Severity:** blocker · **Confidence:** high · **Category:** sequencing · **Reviewer:** executability
- **Location:** `Plan → phases[0].steps[0] ("Pin the baseline BEFORE editing anything"), and onboarding.what_it_is ("BRANCH STATE, MEASURED … HEAD `fb0406e` … tree clean")`
- **Problem:** The gate requires three things simultaneously: branch = `bugfix/pipeline-status-rollover`, `git status --short` empty, and `git log --oneline -1` showing `fb0406e`. All three cannot hold when a cold implementer starts. This IMPLEMENTATION_PLAN.md is written by stage 3 and, per the repo's own pipeline (`.claude/skills/create-implementation-plan/SKILL.md:158-173`, then hand-off to `/commit`), is committed before `/implement-plan` runs — so HEAD will be the plan's own commit, not `fb0406e`. If it somehow is not committed, the plan file is untracked and `git status --short` is non-empty instead. Either way the gate fails, and the very next sentence is unconditional: "Any other number means you are on the wrong branch or something drifted — STOP and report rather than folding it in." A cold agent that trusts the plan halts before EDIT A. Verified live: HEAD is currently `fb0406e` and the stage-3 commit has not yet been made — so the described state is the authoring state, not the execution state.
- **Proposed fix:** Replace the exact-HEAD assertion with an ancestry assertion plus the counts: `git branch --show-current` must print `bugfix/pipeline-status-rollover`; `git merge-base --is-ancestor fb0406e HEAD` must exit 0; `git status --short` must be empty; and `uv run pytest -q --tb=no` must report `3 failed, 79 passed` with exactly the three named repro tests failing. State explicitly that HEAD is expected to be the commit that landed this plan, one or more commits after `fb0406e`. Keep the pytest counts as the real gate — they are branch-identifying and survive the plan's own commit (verified: 82 collected either way).

### E2-02 — Phase 4 pre-empts and duplicates /implement-plan's own Step 7, and archives IMPLEMENTATION_PLAN.md out from under the run still executing it

- **Severity:** blocker · **Confidence:** high · **Category:** sequencing · **Reviewer:** executability
- **Location:** `Plan → phases[3] (steps 3-7 and commit_note), against `.claude/skills/implement-plan/SKILL.md:251-265` (Step 7) and `:39-41` (the stage's own order: implement phases → snapshot → acceptance panel → dispose → /commit)`
- **Problem:** The plan makes "write IMPLEMENTATION_REPORT.md at `fixed` + roll all four artifacts + set the Index cell + move the directory into `_done/` + hand to /commit" a phase of the implementation. That is verbatim what the executing skill does in its own Step 7 (`implement-plan/SKILL.md:253-260`), which runs AFTER the acceptance panel (Steps 4-6). Three concrete failures follow. (a) Duplication: after Phase 4's /commit the implementer reaches Step 7 and is told to write `<work-dir>/IMPLEMENTATION_REPORT.md` and advance status again — into a `<work-dir>` that no longer exists at that path. (b) The report's spine is the acceptance ledger (`:253-254`, `:276-277`), which must cite what the panel actually ran; Phase 4 writes it before the panel exists. (c) The panel and Step 7 resolve every path from the plan's own location (`:53-55`, `:57-59`); Phase 4 has already moved `IMPLEMENTATION_PLAN.md`, `ROOT_CAUSE_ANALYSIS.md` and the intake doc into `requests/bugfix-requests/_done/pipeline-status-rollover/`, so every downstream path in the run is stale.
- **Proposed fix:** Split Phase 4. Keep a short Phase 4 that does only the ESCALATIONS.md entry (E-010) and its /commit — real, independently verifiable work. Move the report + terminal rollover + `_done/` move into an explicitly labelled "Close-out (executed as `/implement-plan` Step 7, not as a plan phase)" section, and say plainly: do NOT write the report or archive early; when the skill reaches Step 7, follow its now-fixed text, write the report at `fixed`, roll all four `*.md` plus the Index cell, and move the directory in the same edit as the link repoint. Keep the dogfood check there — it is stronger when the skill itself drives it.

### E2-03 — implement-plan:29 and :324 appear in files_to_touch but have no Phase 1 step and no acceptance criterion

- **Severity:** major · **Confidence:** high · **Category:** completeness · **Reviewer:** executability
- **Location:** `Plan → files_to_touch[1] ("reword the track-blind restatements at :29 and :324") vs phases[0].steps (EDIT A–H, which never mention them); real sites are `.claude/skills/implement-plan/SKILL.md:29` ("The feature ends at status `implemented`.") and `:324` ("the report + `implemented` status")`
- **Problem:** Both lines are verified track-blind restatements of a terminal word. The plan's `code_references` calls them out as "two track-blind restatements OUTSIDE every guarded window … which is exactly why Phase 1 fixes them", and `files_to_touch` instructs the edit — but the Phase 1 step list runs EDIT A through EDIT H and none of them touches `:29` or `:324`. No acceptance criterion covers them either: Phase 1's grep sweep looks only for stage-sense `` `plan` `` and `` `root-cause` ``, and both lines say `implemented`, so the sweep is blind to them. A cold agent executes the steps, passes every acceptance check, and leaves two instances of the exact defect class in the file whose defect this bug is about — the same silence the RCA diagnoses, reproduced by its own fix.
- **Proposed fix:** Add an explicit "EDIT I (the unguarded restatements)" to Phase 1: at `:29` change "The feature ends at status `implemented`." to name the track's terminal (e.g. "The item ends at its track's terminal stage word — `implemented` (feature), `fixed` (bugfix), `retuned` (calibration)."), and at `:324` change "the report + `implemented` status" to "the report + the track's terminal status". Add a matching acceptance line: `Select-String -Path ".claude\skills\implement-plan\SKILL.md" -Pattern 'implemented'` returns only ordinary-English uses (e.g. `:280` "the phases implemented") and no unconditional instruction to write that word.

### E2-04 — The per-phase gate omits the four node self-verification guards the edited skills instruct you to run when they change

- **Severity:** major · **Confidence:** high · **Category:** missing-step · **Reviewer:** executability
- **Location:** `Plan → testing ("THE PER-PHASE GATE": pytest / ruff check / ruff format --check / mypy) vs `.claude/skills/implement-plan/SKILL.md:303` and `:305`, `.claude/skills/create-implementation-plan/SKILL.md:241` and `:243`, `.claude/skills/scope-feature/SKILL.md:203``
- **Problem:** Every skill this plan edits carries a `## Self-verification` section whose Check lines end with an explicit instruction — e.g. implement-plan:303 "Run it whenever `acceptance_panel.js` or **this file** changes." Phase 1 changes `implement-plan/SKILL.md` and `create-implementation-plan/SKILL.md`; Phase 2 additionally changes `scope-feature/SKILL.md`. That is five guards in scope (`.claude/skills/implement-plan/tests/merge_fallback_guard.mjs`, `.../verify_batching_guard.mjs`, `.claude/skills/create-implementation-plan/tests/merge_fallback_guard.mjs`, `.../merge_failure_repro.mjs`, `.claude/skills/scope-feature/tests/merge_fallback_guard.mjs`) — I confirmed all five exist on disk. None appears anywhere in the plan's gate, acceptance lists, or risks, and none is in `.github/workflows/ci.yml`'s Python job, so skipping them is silent in both directions.
- **Proposed fix:** Add to the per-phase gate, for any phase that edits a skill carrying a Check line: `node .claude/skills/<skill>/tests/<guard>.mjs`, exit 0 required. Name them explicitly in Phase 1's acceptance (implement-plan ×2, create-implementation-plan ×2) and Phase 2's (add scope-feature ×1). Note that these guards exercise the panel JS rather than the prose, so they are expected to stay green — which makes them cheap, and makes an unexpected red genuinely informative.

### E2-05 — Phase 3's second guard is specified circularly and would be vacuous as written

- **Severity:** major · **Confidence:** high · **Category:** correctness · **Reviewer:** executability
- **Location:** `Plan → phases[2].steps[3] ("GUARD 2 … Add a test asserting the regex still parses at least one DECLARED token from each of the skills that write a concrete one")`
- **Problem:** The guard exists to stop `SKILL_STATUS_TEMPLATE` (`tests/test_repo_structure.py:41`) going vacuous when a template is rewritten as a `<placeholder>`. But the specified predicate derives its own subject set from the thing it guards: if a skill's template becomes `<placeholder>`, that skill no longer 'writes a concrete one', drops out of the iteration, and the assertion passes — exactly the failure mode it was written to catch. It is a guard-on-a-guard that shares the guard's blind spot. The plan's own risk register and decisions[3] both correctly identify the placeholder hazard, which makes this specification defect the more costly.
- **Proposed fix:** Hardcode the expected set as a module constant, mirroring the `STAGE_ADVANCING_SKILLS` idiom at `tests/test_repo_structure.py:46-51`. I verified by grep that exactly six skills carry a template today: `SKILLS_WITH_A_STATUS_TEMPLATE = ("make-feature-request", "make-bugfix-request", "scope-feature", "diagnose-bug", "create-implementation-plan", "implement-plan")`. Assert that for EACH name, `SKILL_STATUS_TEMPLATE.search(...)` finds at least one match AND every captured token is in `_declared_stage_tokens()`. The docstring should say the tuple is deliberately hardcoded so that removing or placeholdering a template reds this test rather than shrinking the corpus.

### E2-06 — Phase 2 never says whether the authority line goes inside or outside the fenced template — inside pollutes every future artifact

- **Severity:** major · **Confidence:** high · **Category:** under-specified · **Reviewer:** executability
- **Location:** `Plan → phases[1].steps[1] ("Add ONE authority line beside each of the six template blockquotes"), against the precedent at `.claude/skills/create-implementation-plan/SKILL.md:175-178` and the six template sites (`make-feature-request:176`, `make-bugfix-request:130`, `scope-feature:149`, `diagnose-bug:107`, `create-implementation-plan:176`, `implement-plan:268`)`
- **Problem:** "Beside" is ambiguous between two materially different placements. All six templates sit inside a ```markdown fence that the agent COPIES verbatim into the artifact it creates. The one precedent the plan points at — `create-implementation-plan:177-178` — is an HTML comment INSIDE the fence, so it is copied into every IMPLEMENTATION_PLAN.md. A prose line placed inside the fence would appear as body text at the top of every FEATURE_REQUEST.md, BUGFIX_REQUEST.md, PROJECT_SCOPE.md, ROOT_CAUSE_ANALYSIS.md, IMPLEMENTATION_PLAN.md and IMPLEMENTATION_REPORT.md the pipeline ever produces — a permanent repo-wide regression introduced by a documentation fix. Outside the fence it is agent instruction and never copied. Nothing in Phase 2's steps or acceptance distinguishes them, so six edits will likely land inconsistently.
- **Proposed fix:** Prescribe one placement explicitly and repeat it in the acceptance criterion. Recommended: a prose line immediately ABOVE the opening ```markdown fence (outside it), of the form "The stage word comes from the resolved track README's **Status grammar** line (`requests/feature-requests/README.md`, `requests/bugfix-requests/README.md`, `requests/calibration-findings/README.md`); `tests/test_repo_structure.py::test_every_pipeline_skill_writes_a_stage_token_some_grammar_declares` enforces it." Separately decide the fate of the existing in-fence comment at `create-implementation-plan:177-178` — leave it or lift it out — so the six end up uniform. Phase 1's EDIT F is already correctly specified as outside the fence at `:267`; make Phase 2 match it.

### E2-07 — The mechanical placement check has no runnable command, and tests/ is not importable on this project's Python path

- **Severity:** major · **Confidence:** high · **Category:** environment · **Reviewer:** executability
- **Location:** `Plan → phases[0].steps[11] ("In a scratch Python session, import `_advance_status_section` from tests/test_repo_structure.py and print the slice") vs `pyproject.toml:92` (`pythonpath = ["src"]`) and `tests/` having no `__init__.py``
- **Problem:** This step is load-bearing — the plan names it the single most likely way Phase 1 looks done and is not — yet it is the one step with no command. `uv run python -c "from test_repo_structure import _advance_status_section"` fails: the `pythonpath` setting at `pyproject.toml:92` puts only `src` on the path and applies to pytest runs, not a bare `python -c`; `tests/` is not a package. A cold agent will fumble the import or skip the check, which is the outcome the step exists to prevent. The plan asserts the check twice (steps and testing section) without ever making it executable.
- **Proposed fix:** Give the literal command, runnable from the repo root: `uv run python -c "import sys; sys.path.insert(0, 'tests'); from pathlib import Path; from test_repo_structure import _advance_status_section; [print('=====', n, '=====', _advance_status_section((Path('.claude/skills')/n/'SKILL.md').read_text(encoding='utf-8'))) for n in ('create-implementation-plan','implement-plan')]"`. Add the pass condition: each new rollover bullet's text must appear in its skill's printed slice, and the implement-plan slice must contain all three of `implemented`, `fixed`, `retuned` plus the phrase `terminal stage word`.

### E2-08 — Phase 1's absolute STOP gate contradicts the very next step's fold-in carve-out, in the likely case

- **Severity:** major · **Confidence:** high · **Category:** clarity · **Reviewer:** executability
- **Location:** `Plan → phases[0].steps[0] ("Any other number means … STOP and report rather than folding it in") vs phases[0].steps[1] ("if a FOURTH test fails … Repair it as part of this phase")`
- **Problem:** Step 1 makes any count other than `3 failed, 79 passed` an unconditional halt; step 2 then instructs the implementer to fold in a fourth failure. A cold agent reading in order acts on step 1 first. The tension is not pedantic: the fourth failure is the likely case, because if this stage-3 run followed the still-broken `create-implementation-plan/SKILL.md:170-176` it wrote the Index cell as `plan` and left `BUGFIX_REQUEST.md` / `ROOT_CAUSE_ANALYSIS.md` at `diagnosed`, reddening `test_index_stage_cells_match_their_artifact_status_headers` (`tests/test_repo_structure.py:454`). The plan anticipates that scenario but buries it behind a contradictory halt.
- **Proposed fix:** Rewrite step 1 as a decision table rather than a halt: exactly the three named repro tests failing → proceed; those three plus `test_index_stage_cells_match_their_artifact_status_headers` → the stage-3 run injected defect 1 into this item, repair it first (set every `*.md` in the item directory and the Index cell at `requests/bugfix-requests/README.md:98` to `planned`) and record it as live evidence; any OTHER failure, or the three repro tests passing → stop and report.

### E2-09 — Nothing instructs the implementer to re-read the edited SKILL.md from disk before the dogfood close-out

- **Severity:** major · **Confidence:** medium · **Category:** missing-step · **Reviewer:** executability
- **Location:** `Plan → phases[3].goal ("close the item out by following its own newly-fixed instructions — the fix dogfooding itself") and phases[3].steps[2] ("following the NOW-FIXED implement-plan Step 7 template")`
- **Problem:** The implementer is executing `/implement-plan` while editing `.claude/skills/implement-plan/SKILL.md`. Skill text is loaded into the session when the skill is invoked; a mid-run edit to the file on disk does not retroactively change what the agent is following. So the phase's central claim — that the close-out demonstrates the fix works — holds only if the agent explicitly re-reads the file. Without that instruction the likely outcome is the agent following the stale, unfixed Step 7 from context: writing the report at `implemented`, forgetting the siblings, then reading a red `test_index_stage_cells_match_their_artifact_status_headers` as evidence that Phase 1 failed. The plan even primes that misreading: "If the skill still pushes you toward `implemented`, Phase 1 EDIT E did not land and you must go back."
- **Proposed fix:** Add as the first step of the close-out: "Re-read `.claude/skills/implement-plan/SKILL.md` from disk (Read tool, the Step 7 region) before writing anything. The copy in your context predates Phase 1 and still carries the defect. The dogfood test is whether the ON-DISK text, followed literally, leaves the repo green." Add the same note for `.claude/skills/commit/SKILL.md` if it was loaded earlier in the session.

### E2-10 — Phase 3 leaves a gated decision undisposed inside a plan the executing stage requires to be `decided`

- **Severity:** question · **Confidence:** medium · **Category:** process · **Reviewer:** executability
- **Location:** `Plan → phases[2].steps[0] ("GATED — confirm with the user before building") and gated_decisions[0], against `.claude/skills/implement-plan/SKILL.md:91-95` (the disposition gate: "If it's `open` (gated decisions left undisposed), **warn loudly** that you'd be building on unmade decisions")`
- **Problem:** The pipeline's design puts gated decisions at stage 3's human gate (`create-implementation-plan/SKILL.md:149-156`), and stage 4 gates on the plan's disposition field reading `decided`. A phase whose first step is "confirm with the user before building" reintroduces an undisposed decision into a document that will be stamped `decided`. In practice the implementing agent either stops mid-run to ask — breaking the phase cadence the plan prescribes — or silently picks, which is exactly what the disposition gate exists to prevent. The same shape recurs in gated_decisions[1] (whether to park E-011 as a second escalation entry).
- **Proposed fix:** Resolve Phase 3's in/out call at the stage-3 gate and write the answer into the plan as a settled decision (the plan already recommends LAND BOTH with a good argument). If the user declines, delete Phase 3 from the plan rather than leaving it conditional. If a genuinely mid-run decision is unavoidable, set the plan's disposition field to `open` rather than `decided`, so `/implement-plan`'s Step 1 warns as designed.

### E2-11 — The plan mixes POSIX grep with PowerShell Select-String, on a Windows PowerShell shell where grep does not exist

- **Severity:** minor · **Confidence:** high · **Category:** environment · **Reviewer:** executability
- **Location:** `Plan → phases[1].acceptance[2] ("`grep -rn 'root-cause' .claude/skills/*/SKILL.md` returns only English-prose occurrences"), vs phases[0].steps[12] which correctly uses `Select-String -Path ".claude\skills\*\SKILL.md"`; environment is Windows 11 / PowerShell 5.1`
- **Problem:** `grep` is not available in this shell, and the plan's own Phase 1 step already demonstrates the correct PowerShell form — so the inconsistency reads as an oversight the implementer must resolve mid-run. A naive translation of `grep -rn` to `Get-ChildItem -Recurse | Select-String` would also sweep `.claude/skills/*/tests/*.mjs` and the panel JS, changing what "only English-prose occurrences" means. The Phase 2 acceptance criterion is therefore not directly runnable as written.
- **Proposed fix:** Normalise every command in the plan to PowerShell and state the exact expected output. For Phase 2: `Select-String -Path ".claude\skills\*\SKILL.md" -Pattern 'root-cause' -CaseSensitive` must return exactly two hits after Phase 2 — `make-bugfix-request:5` ("root-cause analysis", prose) and `make-bugfix-request:6` (the frontmatter chain, see E2-12) — with no `> **Status:**` line and no `next:` value among them. I verified the current hit set is nine lines across three skills, so this criterion is checkable.

### E2-12 — make-bugfix-request:6 carries the same frontmatter stage-chain restatement as diagnose-bug:7, but only the latter is dispositioned

- **Severity:** minor · **Confidence:** high · **Category:** completeness · **Reviewer:** executability
- **Location:** ``.claude/skills/make-bugfix-request/SKILL.md:6` ("BUGFIX pipeline (intake -> root-cause -> reuse plan/implement)") vs Plan → gated_decisions[3], which covers only `.claude/skills/diagnose-bug/SKILL.md:7``
- **Problem:** Verified by grep: the literal `root-cause` appears at `make-bugfix-request:5` (prose, "root-cause analysis") and `:6` (a stage-chain restatement identical in kind to diagnose-bug:7). The plan's gated decision reasons carefully about leaving diagnose-bug's frontmatter alone for dispatch reasons, but never mentions make-bugfix-request's. That leaves Phase 2's acceptance criterion — "returns only English-prose occurrences (the skill descriptions)" — ambiguous about whether `:6` qualifies, so an implementer either edits it (harmless but unplanned) or flags it as an unexplained residue (wasted cycles at the acceptance gate).
- **Proposed fix:** Extend gated_decisions[3] to name both sites — `.claude/skills/diagnose-bug/SKILL.md:7` and `.claude/skills/make-bugfix-request/SKILL.md:6` — with the same recommendation (leave both: they are natural-language dispatch descriptions, not graded templates, and the conformance guard does not read frontmatter). Then make Phase 2's acceptance name the two expected residual hits explicitly rather than describing them by category.

### E2-13 — Phase 3's red-first break for guard 1 also reds invariant A, so the promised "readable message" comes from two tests at once

- **Severity:** minor · **Confidence:** high · **Category:** testing · **Reviewer:** executability
- **Location:** `Plan → phases[2].steps[4] ("set one Index Stage cell to a bogus word for guard 1 … confirm it FAILS with a readable message"), against `tests/test_repo_structure.py:454-487``
- **Problem:** Setting an Index Stage cell to a bogus word necessarily makes the cell disagree with that item directory's artifacts, so `test_index_stage_cells_match_their_artifact_status_headers` (assertion at `:483`) fails simultaneously with the new guard. A full-suite run during the break shows two failures, and the acceptance ("Red-first evidence captured for BOTH: the failing output from the temporary break") does not say which output counts. There is also a subtler trap: the Index `stage_cell` capture group is `(?P<stage>\w+)` (`:465`), which does not match a hyphen — a bogus word containing one would capture only its first segment, and the failure message would name a token nobody wrote.
- **Proposed fix:** Prescribe the isolation: during the break run the new guard alone by selector (`uv run pytest tests/test_repo_structure.py::test_index_stage_cells_use_only_their_own_track_s_grammar -q`), and use a hyphen-free bogus word (e.g. `bogus`) so the captured token matches what was written. Add an acceptance line: the captured red output must name the new test, not the invariant-A guard.

### E2-14 — A risk bullet contradicts itself about where the fence-blind window ends, in the one place accuracy matters most

- **Severity:** minor · **Confidence:** high · **Category:** clarity · **Reviewer:** executability
- **Location:** `Plan → risks[1] ("…the window ends at `## 1. Onboarding` (create-implementation-plan:184) … Prose added below the fence opening is invisible.")`
- **Problem:** The two sentences disagree. The first is correct: `_advance_status_section` (`tests/test_repo_structure.py:74-80`) runs from `advance status` at `:167` to the `^##\s` match at `:184`, so the window includes `:175`–`:183` — seven lines INSIDE the ```markdown fence, including the template blockquote at `:176`. The second sentence says prose below the fence opening is invisible, which is false and would steer an implementer away from a placement that works. The same applies to implement-plan, whose window runs `:254`→`:275` and includes the fence opening at `:267` and the template at `:268` — the plan's own EDIT F depends on that being true.
- **Proposed fix:** Replace the second sentence with the accurate rule: "The window ENDS at the first `##` heading, which happens to sit inside the template fence — so text between the fence opening and that heading IS inside the window, and anything after it is not. Nothing may be added below `## 1. Onboarding` (create-implementation-plan) or `## 1. Acceptance ledger` (implement-plan) and expected to count."

### E2-15 — Two Phase 1 acceptance criteria are subjective with no rubric, in a plan whose central risk is keyword-satisfaction

- **Severity:** minor · **Confidence:** medium · **Category:** acceptance · **Reviewer:** executability
- **Location:** `Plan → phases[0].acceptance[6] ("Both new rollover bullets … are followable instructions rather than keyword insertions") and testing ("Phase 1's final acceptance is judged on followability, not on the regex")`
- **Problem:** The plan correctly identifies that `ROLLS_A_SIBLING` (`tests/test_repo_structure.py:55`) and `DERIVES_TERMINAL` (`:60`) are satisfiable by inserting a phrase, and correctly refuses to rely on the regex alone — but the substitute criterion is a judgment call with no checkable content. "Followable" is exactly the acceptance wording that lets a cold agent mark a criterion met by assertion, which the repo's own stage-4 contract forbids (`.claude/skills/implement-plan/SKILL.md:311-312`, "Every acceptance criterion is verified by execution, not asserted").
- **Proposed fix:** Turn it into a four-part checklist a reader can tick: the bullet must (1) name the concrete filenames to roll for BOTH tracks (`FEATURE_REQUEST.md` + `PROJECT_SCOPE.md`; `BUGFIX_REQUEST.md` + `ROOT_CAUSE_ANALYSIS.md`), (2) name the exact target stage word or the rule that derives it, (3) name `reviews/` as exempt, and (4) cite `tests/test_repo_structure.py::test_index_stage_cells_match_their_artifact_status_headers` as the reason. Then the criterion is verifiable by reading four things rather than by taste.

### E2-16 — The close-out does not refresh the Index Notes cell, leaving the archived row describing the bug in the present tense

- **Severity:** nit · **Confidence:** high · **Category:** convention · **Reviewer:** executability
- **Location:** ``requests/bugfix-requests/README.md:98` (Notes: "Four instances across three skills: two leave sibling statuses stale … Only the first breaks a guard") vs Plan → phases[3].steps[4] and phases[3].acceptance[3], which change only the Stage cell and the link`
- **Problem:** Both archived feature rows carry outcome-shaped notes at terminal stage (`requests/feature-requests/README.md:116` "**Unblocked item 1.3**", `:117` "Unblocks the rest of Phase 1"). This row's note is written from the diagnosis and will read as an open defect once the row says `fixed`. No test catches it — the Index guard reads only the Stage cell (`tests/test_repo_structure.py:465`) — which is precisely why it needs to be an instruction rather than a hope.
- **Proposed fix:** Add to the close-out's rollover step: rewrite the Notes cell to the outcome, e.g. "Four instances across three skills, all fixed; the conformance + rollover + terminal guards in `tests/test_repo_structure.py` now make each track README's Status grammar enforceable." Fold it into the acceptance line that already checks the row's Stage cell and link.


---

## Meta-audit findings (did the merge converge faithfully?)

### MA-01 — Phase 1's step list drops the :29/:324 track-blind restatement fixes that two planners raised as executable steps

- **Severity:** major · **Confidence:** high · **Category:** completeness
- **Location:** `merged plan phases[0].steps (EDITS A-H) vs files_to_touch['.claude/skills/implement-plan/SKILL.md']`
- **Problem:** Both planner 1 (its Phase 3 step 5) and planner 2 (its Phase 3 step 6) carried an explicit step rewording the two track-blind `implemented` restatements that sit OUTSIDE every guarded window: implement-plan/SKILL.md:29 ("The feature ends at status `implemented`.") and :324 ("the report + `implemented` status"). Both lines verified present at those exact numbers. In the merged plan the requirement survives ONLY in the files_to_touch table and in code_references — Phase 1's eight named edits (A through H) never mention :29 or :324, and no Phase 1 acceptance criterion checks them. A cold implementer executes the step list; files_to_touch reads as a scope summary, not a work item. Since no test can ever catch these two lines (the conformance guard only inspects `> **Status:**` blockquotes, and both lines are outside `_advance_status_section`'s :254->:275 window), a silent drop here leaves exactly the class of unguarded prose drift this bug is about — landed green.
- **Proposed fix:** Add an explicit "EDIT I (defect 3, part 4 — the unguarded restatements)" to Phase 1's steps: reword implement-plan/SKILL.md:29 to name the item's track terminal rather than `implemented`, and :324 to "the report + the terminal status". Add a matching Phase 1 acceptance line: "implement-plan/SKILL.md contains no sentence asserting the terminal is `implemented` unconditionally — :29 and :324 both name the per-track variation; verified by reading, since no test covers them."

### MA-02 — Phase 1 step 1's STOP instruction contradicts step 2's STOP CONDITION, on a case that is near-certain rather than hypothetical

- **Severity:** major · **Confidence:** high · **Category:** internal-consistency
- **Location:** `merged plan phases[0].steps[0] and [1]; files_to_touch['requests/bugfix-requests/pipeline-status-rollover/IMPLEMENTATION_PLAN.md']`
- **Problem:** Step 1 says the baseline "must report exactly `3 failed, 79 passed` … Any other number means you are on the wrong branch or something drifted — STOP and report rather than folding it in." Step 2 then says if a FOURTH test fails (invariant A), "Repair it as part of this phase." These are directly contradictory instructions for the same observation. Worse, the fourth failure is close to certain, not conditional: create-implementation-plan/SKILL.md:171-173 instructs setting the Index cell AND the new artifact to `plan` and says nothing about siblings (verified), while test_index_stage_cells_match_their_artifact_status_headers compares the Index cell against every `*.md` in the item directory (tests/test_repo_structure.py:479). So a stage-3 run that followed the unfixed skill leaves Index=`plan`, IMPLEMENTATION_PLAN=`plan`, BUGFIX_REQUEST=`diagnosed`, ROOT_CAUSE_ANALYSIS=`diagnosed` — invariant A red. The merged plan's own files_to_touch says the plan artifact should open at `planned`, but no step anywhere instructs setting the Index cell and the two siblings to `planned` to match it, so even the corrected form leaves the directory inconsistent unless the writer inferred the rest.
- **Proposed fix:** Replace step 1's single expected baseline with a two-case table: (a) `3 failed, 79 passed` = the directory is already internally consistent, proceed; (b) `4 failed, 78 passed` with the fourth being test_index_stage_cells_match_their_artifact_status_headers = the stage-3 run injected defect 1, repair as step 2 describes and record it as live evidence; (c) anything else = STOP. Separately, hoist the instruction out of files_to_touch into an explicit numbered step: "Set requests/bugfix-requests/README.md:98's Stage cell, BUGFIX_REQUEST.md, ROOT_CAUSE_ANALYSIS.md and IMPLEMENTATION_PLAN.md all to `planned` — the plan must not inject the defect it fixes, and all four must agree."

### MA-03 — Scope creep: an adjacent non-RCA defect is folded into an ungated Phase 2 step while simultaneously being posed as a gated decision

- **Severity:** major · **Confidence:** high · **Category:** scope-creep
- **Location:** `merged plan phases[1].steps[3] vs gated_decisions[2]`
- **Problem:** Phase 2 step 4 unconditionally instructs "Align create-implementation-plan/SKILL.md:49-51 with the three-valued resolution Phase 1 gave implement-plan:53-55." Phase 2 carries no gate marker anywhere (contrast Phase 3 step 1, which opens "GATED — confirm with the user before building"), and Phase 2's acceptance criterion 4 asserts the alignment as a hard requirement. But gated_decisions[2] poses exactly this edit as an open question for the user, and the merged plan's own analysis concedes the exposure is only "a wrong `<work-dir>` for a calibration item" — i.e. a different latent defect, not one of the RCA's four instances, and one no repro test covers. Verified: create-implementation-plan/SKILL.md:49-51 carries the same two-valued form and the same non-composing `requests/<track>-requests/<slug>/` template. A cold implementer following Phase 2's steps lands a fix for an undiagnosed second bug without anyone disposing it.
- **Proposed fix:** Mark Phase 2 step 4 "GATED — see Gated decisions #3; skip and record if declined", and make Phase 2 acceptance criterion 4 conditional on that disposition. Alternatively move the step into Phase 3 (the plan's one already-gated phase) so the gate marker and the work sit together.

### MA-04 — Planner 2's proposed guard on the stage-discovery readers is declined only in narrative prose, never surfaced as a Decision or a Gated decision

- **Severity:** major · **Confidence:** medium · **Category:** dropped-signal
- **Location:** `merged plan testing section ("Do not fabricate a fragile prose regex for them"); absent from decisions[] and gated_decisions[]`
- **Problem:** Planner 2's Phase 4 ITEM 2 proposed a narrow guard over the "at the `X` stage" discovery phrasing, asserting every captured word is in the declared vocabulary. The merged plan FIXES the four reader sites in Phase 1 EDIT H (create-implementation-plan:56, :65-66; implement-plan:61, :92-93 — all four verified verbatim) but silently drops the proposed guard, disposing of it in one sentence buried in the testing narrative. Every other decline in the merge (the `next:` guard, the `_done/` skip, the frontmatter chain) got a full decisions[] or gated_decisions[] entry. This is the most consequential decline in the plan and the one least visible: the readers are FUNCTIONAL — the merge's own convergence_map[3] says "land the token rename alone and the next pipeline run hunts the Index for a word that no longer exists" — and after Phase 1 they will be correct-but-unguarded, which is precisely the state the RCA identifies as the root cause. Declining may well be right, but it is the user's call, not a footnote.
- **Proposed fix:** Promote to a decisions[] entry (or better, a gated_decisions[] item alongside Phase 3's two guards): "Guard the four stage-discovery readers, or leave them as an honest gap?" State planner 2's proposed anchor regex, state the counter-argument (a prose regex reds on rewording), and note that Phase 1's grep sweep is the weaker manual stand-in the report must name as such.

### MA-05 — Planner 2's branch-naming drift finding is dropped from the merged plan entirely, and Phase 4 walks straight into it

- **Severity:** minor · **Confidence:** high · **Category:** dropped-signal
- **Location:** `planner 2 open_questions[4]; nothing in the merged plan; bears on phases[3].steps[2]`
- **Problem:** Planner 2 recorded that three branch conventions coexist with none written down authoritatively: implement-plan/SKILL.md:105 says `git switch -c implement/<slug>`, its report template at :272 prescribes `**Branch:** implement/<slug>`, and the live branch is `bugfix/pipeline-status-rollover` (all three verified). The merged plan carries no trace of this. Phase 4 step 3 instructs writing IMPLEMENTATION_REPORT.md "following the NOW-FIXED implement-plan Step 7 template (:267-300)" — which includes that `**Branch:** implement/<slug>` field. A cold implementer following the template literally either writes a false Branch value into a tracked artifact or, worse, renames/recreates the branch and loses the repro. It is also the same restatement-drift shape as the bug being fixed, one level up, which is exactly the observation worth preserving.
- **Proposed fix:** Add a clause to Phase 4 step 3: "The report template's `**Branch:** implement/<slug>` is a feature-track default — write the real branch name, `bugfix/pipeline-status-rollover`. Do not rename or recreate the branch; the repro exists only there." Optionally add an open question noting the three unreconciled branch conventions as adjacent, out of scope here.

### MA-06 — Phase 2's authority-line instruction loses planner 1's HTML-comment prescription, so a plain prose line lands inside the copied template fence

- **Severity:** minor · **Confidence:** medium · **Category:** cost-unrealism
- **Location:** `merged plan phases[1].steps[1]`
- **Problem:** All six template blockquotes sit INSIDE ```markdown code fences (verified: create-implementation-plan:175 opens the fence, blockquote at :176, existing HTML comment at :177-178; scope-feature fence at :148, blockquote :149; diagnose-bug fence at :106, blockquote :107; implement-plan fence at :267, blockquote :268). Planner 1 prescribed the addition verbatim as an HTML comment (`<!-- … -->`), which is the form the existing precedent at create-implementation-plan:177-178 uses and which renders invisibly in the artifact an agent copies out. The merged plan says only "Add ONE authority line beside each of the six template blockquotes" and "extend it" — losing the form. A cold agent adding a plain prose line inside the fence puts "the stage word comes from requests/<track>/README.md…" into every FEATURE_REQUEST, PROJECT_SCOPE, RCA, PLAN and REPORT the pipeline ever produces.
- **Proposed fix:** Restore the form: "Write each authority line as an HTML comment (`<!-- … -->`) inside the fence, copying the shape at create-implementation-plan/SKILL.md:177-178 — a plain line there is copied verbatim into every artifact the skill produces. Backticked paths only, never markdown links."

### MA-07 — Phase 3's helper-refactor instruction is not literally achievable as written

- **Severity:** minor · **Confidence:** high · **Category:** cost-unrealism
- **Location:** `merged plan phases[2].steps[1] and phases[2].acceptance[2]`
- **Problem:** The step says: "Refactor `_declared_stage_tokens()` (tests/test_repo_structure.py:63-71) into a per-track form plus a thin union wrapper, keeping the union call site at :280 working unchanged." tests/test_repo_structure.py:280 reads `declared = _declared_stage_tokens()` (verified). If `_declared_stage_tokens` becomes the per-track form, :280 must change (it takes no argument today); if the union keeps that name, the per-track form needs a different name and the description is backwards. Either way the acceptance criterion "`_declared_stage_tokens` is now per-track with a union wrapper, and test_every_pipeline_skill_writes_a_stage_token_some_grammar_declares still passes with its UNION semantics unchanged" is self-contradictory as stated. Small, but this is the one phase that touches Python under mypy strict, and the plan explicitly forbids editing the repro tests — an implementer resolving the ambiguity by changing :280 is arguably editing the grader.
- **Proposed fix:** Name both functions concretely: keep `_declared_stage_tokens() -> set[str]` untouched at :63-71 and at its :280 call site, and add `_declared_stage_tokens_for(track: str) -> set[str]` beside it; have the existing union function delegate to the new per-track one. Then :280 is genuinely unchanged and the acceptance criterion becomes checkable.

### MA-08 — Phase 2's acceptance grep will misfire against a token the plan deliberately keeps, and the gated decision that keeps it conflates two different things

- **Severity:** minor · **Confidence:** high · **Category:** internal-consistency
- **Location:** `merged plan phases[1].acceptance[2] and gated_decisions[3]`
- **Problem:** Phase 2 acceptance criterion 3 requires "`grep -rn 'root-cause' .claude/skills/*/SKILL.md` returns only English-prose occurrences (the skill descriptions), no stage tokens." But diagnose-bug/SKILL.md:6-7 reads "This is stage 2 of the BUGFIX pipeline (intake -> root-cause -> reuse plan/implement)" — verified. That is unambiguously a stage-chain restatement, not English prose, and gated_decisions[3] rules it out of scope. So the acceptance criterion cannot pass as written against the plan's own decision. gated_decisions[3] also justifies keeping it on the grounds that "'root cause' reads as the English phrase a user actually types" — but the natural-language trigger phrases are at :10 ("find the root cause"), a different sentence from the chain at :6-7. Planner 2 correctly separated the chain from the triggers and recommended changing only the chain; the merge collapsed the two and used the trigger argument to protect the chain.
- **Proposed fix:** Reword the acceptance criterion to exempt the frontmatter explicitly: "…returns only English-prose occurrences plus the frontmatter stage-chain at diagnose-bug/SKILL.md:6-7, which gated_decisions #4 deliberately keeps." And restate gated_decisions #4 to distinguish the two sentences: the chain at :6-7 is a genuine restatement whose only protection is that it sits in the dispatch description; the trigger phrases at :10 are English and are not at issue either way.

### MA-09 — Scope note: Phase 1 EDIT D corrects a factual premise of the DECIDED RCA and is framed only as a step, not as a scope call

- **Severity:** minor · **Confidence:** medium · **Category:** scope-creep
- **Location:** `merged plan phases[0].steps EDIT D; decisions[4]; gated_decisions[2]`
- **Problem:** The RCA's Open Question 2 answer states "Yes, and the machinery already exists" (ROOT_CAUSE_ANALYSIS.md:105-108). The merged plan's grounding correctly finds that premise false — implement-plan/SKILL.md:53-55 is two-valued and its `requests/<track>-requests/<slug>/` template cannot compose for `calibration-findings` (both verified) — and expands Phase 1 into rewriting Step 1's track/work-dir resolution. The expansion is genuinely load-bearing (without it defect 3 ships half-fixed with the guard green) and decisions[4] argues it well. What the merge does not do is name it as what it is: a correction of a factual premise inside a decided artifact, i.e. the one place this plan legitimately touches the RCA's own reasoning. Presented only as EDIT D among eight, a reader has no signal that scope moved. gated_decisions[2] muddies it further by posing the question in a form that covers both this edit (which the plan treats as required) and the Phase 2 one (which it does not).
- **Proposed fix:** Add a decisions[] entry or a one-line scope note at the top of Phase 1: "EDIT D exceeds the RCA's enumerated four instances. It corrects a factual premise in the decided RCA (Open Question 2's 'the machinery already exists'), not its verdict — grounding found the resolution two-valued. Without it the guard goes green with defect 3 unfixed for calibration." Split gated_decisions #3 so the implement-plan half is stated as required-and-argued and only the create-implementation-plan half is posed as a question.

### MA-10 — One point is restated six times across sections, in a plan whose subject is restatement drift

- **Severity:** nit · **Confidence:** high · **Category:** dedup
- **Location:** `merged plan decisions[3], risks[3], convergence_map[0], phases[0].steps EDIT F, phases[0].acceptance[3], testing section`
- **Problem:** The `<placeholder>` / SKILL_STATUS_TEMPLATE vacuity point appears in full six times — as a decision, a risk, a convergence theme, a step, an acceptance criterion, and twice in the testing prose. The section template does invite some restatement (a decision and its risk are different registers), but six full restatements of one regex fact, in a plan whose thesis is that hand-restated rules drift, is more than the structure requires. The same is true to a lesser degree of the `reviews/` carve-out (four appearances) and the fence-blind window (five). This is a readability cost, not a correctness one, but it dilutes the signal a cold implementer has to sort through in a document that is already very long.
- **Proposed fix:** Keep the full statement in exactly two places — the risk (why it is tempting) and the Phase 1 acceptance criterion (what to verify) — and reduce the other four to a one-clause cross-reference ("see Risks: placeholder vacuity"). Same treatment for the `reviews/` carve-out and the fence-blind window.

### MA-11 — Planner 1's open question about the undeclared `preflight` token is dropped without a record

- **Severity:** nit · **Confidence:** medium · **Category:** dropped-signal
- **Location:** `planner 1 open_questions[2]; merged plan uses preflight.md only as evidence, never as a question`
- **Problem:** Planner 1 raised that requests/feature-requests/_done/1.1-app-shell/reviews/preflight.md:1 carries `> **Status:** preflight` — verified — a word no grammar declares, green today only because the invariant-A guard globs non-recursively at tests/test_repo_structure.py:479, and that "nothing currently says so in writing, which is the same shape of gap this whole bug is about." The merged plan uses preflight.md four times as EVIDENCE for the `reviews/` carve-out but never carries forward the question of whether the exemption should be written down anywhere. After this fix, the carve-out will exist in two skill bullets and nowhere in the track contracts — which is a hand-restated rule with no authority, i.e. the exact pattern the fix retires everywhere else.
- **Proposed fix:** Either add one clause to the `_done/` escalation entry in Phase 4 ("panel working files under `reviews/` use their own words — `preflight` — and are exempt by the guard's non-recursive glob rather than by any written rule"), or add it as a short open question so the next reader does not rediscover it.


---

## Gated decisions as posed by the panel

### 1. Do Phase 3's hardening guards land at all, and if so both or one? They are the RCA's explicitly gated tier (its :130 — "gated not assumed") and are strictly beyond "the red repro goes green".

- **Panel recommendation:** LAND BOTH. Together they are roughly thirty lines of test and one helper refactor, and each closes a gap this exact defect walked through: the Index cell is checked against artifacts but never against the grammar (hardening (b)), and the conformance guard only reports skills it can PARSE, so a future `<placeholder>` empties it with nothing red. The measured cost of this bug — four independent discoveries, none of which fixed it — is itself the argument for guarding rather than observing. Phase 3 is the ONE phase droppable whole without weakening the Definition of Done, so declining costs nothing already delivered; but declining should be a decision, recorded in the report, not a silence.
- **Related:** `tests/test_repo_structure.py:63-71`, `tests/test_repo_structure.py:454-487`, `tests/test_repo_structure.py:41`, `requests/bugfix-requests/README.md:35-46`

### 2. `/diagnose-bug`'s three non-confirmed verdict exits — `closed-works-as-intended`, `cannot-reproduce`, `redirected-to-feature` at .claude/skills/diagnose-bug/SKILL.md:99-102 — are stage words no grammar declares. They escape the conformance guard only because they sit in inline prose rather than in a `> **Status:**` blockquote. Fix here, or park?

- **Panel recommendation:** PARK as ESCALATIONS E-011 (Phase 4), do not fix here. This is a genuine fifth instance of the same class and it should not be lost — but the RCA's verdict enumerates four, and enumerating a fifth inside the fix is adjacent to re-opening a decided stage. The cheap fix (declaring them as exits on the bugfix grammar line) is also not obviously right: `STATUS_GRAMMAR` captures the rest of the line and `BACKTICKED` would pull them into the UNION, which widens the declared vocabulary for all three tracks and weakens the guard everywhere. That is a design change, not hardening. If the user prefers to close it now, the honest shape is a follow-up BUGFIX_REQUEST rather than a widened grammar line.
- **Related:** `.claude/skills/diagnose-bug/SKILL.md:99-102`, `tests/test_repo_structure.py:38`, `tests/test_repo_structure.py:63-71`, `ESCALATIONS.md:72-79`

### 3. Phase 1 EDIT D makes implement-plan's track resolution three-valued (adding `calibration` and replacing the non-composing `requests/<track>-requests/<slug>/` template). Phase 2 applies the same edit to create-implementation-plan:49-51. Is that second edit in scope, or scope creep beyond the RCA's four instances?

- **Panel recommendation:** DO BOTH, but keep them in different phases as planned. The implement-plan edit is REQUIRED — without it, calibration resolves to `feature` and defect 3 ships half-fixed with the guard green, so it belongs in the fix phase. The create-implementation-plan edit is genuinely adjacent: that skill writes `planned` for every track, so no stage word is wrong; its only exposure is a wrong `<work-dir>` for a calibration item. But leaving the two back-half skills stating the same sentence differently is exactly how the next drift starts, and it is one sentence. Phase 2 is the right home — it is the consistency phase and it is independently green, so declining it costs nothing already delivered.
- **Related:** `.claude/skills/implement-plan/SKILL.md:53-55`, `.claude/skills/create-implementation-plan/SKILL.md:49-51`, `requests/calibration-findings/README.md:88`

### 4. Should `.claude/skills/diagnose-bug/SKILL.md`'s frontmatter description, which restates the pipeline chain as "intake -> root-cause -> reuse plan/implement" at :7, be updated to `diagnosed` along with the rest?

- **Panel recommendation:** LEAVE IT, and note the choice in the report. It is a stage-chain restatement and one planner reasonably wanted it changed — but it lives inside the description that drives skill DISPATCH, where "root cause" reads as the English phrase a user actually types ("find the root cause"), and the same paragraph carries the natural-language trigger phrases. Degrading dispatch to satisfy consistency in a non-graded sentence is a bad trade. The cost of declining is cosmetic only: the conformance guard does not see frontmatter, and no reader follows a description to find work.
- **Related:** `.claude/skills/diagnose-bug/SKILL.md:1-10`, `tests/test_repo_structure.py:41`

### 5. Phase 1 is atomic — every prose fix in one commit. The alternative is three commits, one per defect, giving per-defect reviewability at the cost of a branch whose CI is RED until the last of them (since `/commit` pushes on approval).

- **Panel recommendation:** KEEP IT ATOMIC. Two of the three planners proposed a red ladder and it is defensible — `enforce_admins: true` means nothing merges past a red check anyway — but the prescribed cadence is a green local run per phase, and the writer/reader coupling means a half-landed rename actively misdirects the next pipeline run. Grouping the steps by defect inside one phase preserves the reviewability in the diff without ever pushing a knowingly-red branch. If the user wants finer-grained history, the honest split is by FILE, not by defect, and it still cannot be green partway.
- **Related:** `tests/test_repo_structure.py:270-353`, `.claude/skills/commit/SKILL.md:138-150`, `.github/workflows/ci.yml:36-47`

### 6. Is `post-v1` the right `Bears on:` for the parked `_done/` link-scan entry? ESCALATIONS.md:75 allows only a roadmap item id, a phase name, or `post-v1` — no `unscheduled` hatch.

- **Panel recommendation:** USE `post-v1`, with eyes open. H1 `escalation-queue` is DONE (ROADMAP.md:197) and no row covers pipeline hygiene, so `post-v1` is the only honest resolvable moment. The discomfort is real — an entry parked at `post-v1` is the one least likely to be reopened, which is the failure mode the queue exists to prevent. If the user prefers a nearer moment, H2 `domain-engineer` (ROADMAP.md:198, NOT STARTED) is the next harness row that will re-read the skill corpus and is a defensible binding. Do not invent a roadmap row to give it a home.
- **Related:** `ESCALATIONS.md:72-79`, `ROADMAP.md:197-198`, `tests/test_request_links.py:41`, `requests/feature-requests/_done/1.1-app-shell/reviews/plan-adversarial.md:216-217`


---

## Reviewer summaries

```json
[
  {
    "reviewer": "code-grounded",
    "kind": "adversary",
    "summary": "Code-grounded verification of every citation in the merged plan against the live repo at `bugfix/pipeline-status-rollover` @ `fb0406e`. I read all seven cited SKILL.md files, both test modules, all three track READMEs, ESCALATIONS.md, ROADMAP.md, ci.yml, and the two archived 1.1 artifacts; I ran the suite (confirmed `3 failed, 79 passed` / 82 collected), ruff (clean), and mypy (\"16 source files\"), and I computed the four `_advance_status_section` windows programmatically rather than by eye. The plan's grounding is unusually strong: all six `> **Status:**` restatements, all four advance-status windows (167→183 and 254→275 exactly as claimed), every regex constant, the four unguarded reader sites, and the archived E2-08 finding all resolve exactly as cited. No fictional function, no invented path, no fake \"reuse\" claim. The defects I found are of three kinds: (1) two prescriptions that leave a real residual bug the plan's own logic says must be fixed — the `requests/<track>-requests/README.md` template on the same line as the work-dir template EDIT D repairs, and `create-implementation-plan:23` carrying an identical copy; (2) one instruction that, followed literally, degrades every future pipeline artifact — Phase 2's \"beside the template blockquote\" lands INSIDE the copied fenced template for five of six skills; and (3) a precondition (HEAD == fb0406e) that the plan's own commit cadence guarantees will be false when Phase 1 starts. The rest are citation-precision nits."
  },
  {
    "reviewer": "executability",
    "kind": "adversary",
    "summary": "Executability & sequencing review, grounded by reading every cited file and running the suite (`uv run pytest -q --tb=no` → 3 failed / 79 passed, 82 collected — exactly as the plan claims; the three failures are the RCA's repro). The plan's citations are unusually accurate: I re-verified `tests/test_repo_structure.py:41/46-51/52-55/59-60/63-71/74-80/270/299/330/454-487`, the `_advance_status_section` windows (create-implementation-plan 167→183, implement-plan 254→275, and exactly one `advance status` occurrence per skill), all six `> **Status:**` templates by grep, `tests/test_request_links.py:29/32/41`, `requests/*/README.md:106/86/88`, `ESCALATIONS.md:72-79/101-103` and E-009 at :225, `ROADMAP.md` H1/H2, `.github/workflows/ci.yml:36-47`, and both archived review files. I found no invented path or line number.\n\nThe phase DAG is sound: Phase 2 builds on Phase 1's edits, Phase 3 is independent, Phase 4 consumes Phase 1's fixed skills — no phase depends on later work. The atomicity argument for Phase 1 is correct and well defended, and the convention baking is strong (`/commit`-only, read-only-git subagents, no `git mv`, revert-by-editing rather than `git checkout`, repo-relative paths, and correctly-argued NOT-EXERCISED entries for resolve-by-name / append-only ledger / immutable rulesets / ADR 0008).\n\nWhat breaks a cold run is at the seams. Two structural defects would stop or corrupt execution: (1) Phase 1's preflight demands `git log --oneline -1` show `fb0406e` with a clean tree, which is impossible once this plan artifact itself lands via `/commit` — a literal reader halts before EDIT A; (2) Phase 4 hoists `/implement-plan`'s own Step 7 (report + terminal rollover + `_done/` archive) into a `/commit`-gated phase that runs before the skill's acceptance panel, duplicating Step 7 and moving `IMPLEMENTATION_PLAN.md` out from under the run still executing it. Beyond those: two edits named in `files_to_touch` (implement-plan `:29`, `:324`) have no step and no acceptance check; the per-phase gate omits the four `node .../tests/*.mjs` self-verification guards the edited skills instruct you to run on change; Phase 3's second guard is specified circularly and would be vacuous; and Phase 2 never says whether the new authority line goes inside or outside the fenced template — inside means it is copied into every artifact the pipeline produces from then on."
  },
  {
    "reviewer": "meta-audit",
    "kind": "meta_audit",
    "summary": "Meta-audit of the merge, grounded against the live repo (branch `bugfix/pipeline-status-rollover` @ `fb0406e`; baseline re-measured as 3 failed / 79 passed, matching the plan's claim). I verified every load-bearing citation the merged plan makes: the three repro tests at tests/test_repo_structure.py:270/:299/:330, the regex machinery at :38-80, invariant A at :454-487 (including the non-recursive glob at :479 and the `continue` at :476-477), the four single `advance status` occurrences and their fence-blind windows, implement-plan:29/:53-55/:61/:92-93/:268/:324, create-implementation-plan:49-51/:56/:65-66/:171-178, diagnose-bug:97/:107/:152/:154, scope-feature:136-149, all three grammar lines, ESCALATIONS.md:72-79 and E-009 at :225, ROADMAP.md:197-198, ci.yml:36-47, commit/SKILL.md:140-147, test_request_links.py:29/:32/:41, test_no_leaks.py:26/:90, and the archived E2-08 finding at plan-adversarial.md:214-217. Every one of them resolves. I also independently confirmed the merge's claim that the three panel scripts carry no stage-word literals.\n\nCONVERGENCE QUALITY. The merge is strong on the hard parts: the `<placeholder>` vacuity trap, the fence-blind `_advance_status_section` windows, the unguarded writer/reader coupling, the `reviews/` non-recursion carve-out, the `_done/`-link vacuity, and the two off-by-five citation corrections are all carried faithfully and correctly. The three declines it DOES surface (the `next:` guard, the `_done/` skip, the shared prose home) are each argued from repo evidence rather than taste, and the `next:` decline correctly overrides planner 3's counter-proposal with a verified contradiction.\n\nWHERE IT FAILS. (a) COMPLETENESS — one executable step that two planners carried (rewording the unguarded track-blind restatements at implement-plan:29 and :324) survives only in the files_to_touch table, not in Phase 1's steps or acceptance; a cold agent walking the steps will miss it, and no test can catch it (MA-01). Planner 2's branch-naming drift finding vanished entirely and Phase 4 walks into it (MA-05). Planner 2's proposed guard on the stage-discovery readers is declined in one buried narrative sentence rather than surfaced for disposition, unlike every other decline (MA-04). (b) SCOPE-CREEP — Phase 2 unconditionally lands a fix for an adjacent, undiagnosed defect (create-implementation-plan's non-composing calibration work-dir) that the plan's own gated_decisions poses as a user question, in a phase carrying no gate marker (MA-03); and Phase 1 EDIT D corrects a factual premise inside the decided RCA without naming that it does so (MA-09). (c) COST-UNREALISM — Phase 3's \"keep the union call site at :280 working unchanged\" is not achievable as literally written against the real line (MA-07), and Phase 2's authority-line step loses planner 1's HTML-comment prescription, so a plain line would land inside the copied template fence (MA-06). Plus one internal contradiction on a near-certain case (MA-02: step 1 says STOP on a fourth failure, step 2 says repair it, and the fourth failure is essentially guaranteed given how stage 3 was instructed to write this very plan) and one acceptance criterion that cannot pass against the plan's own gated decision (MA-08).\n\nNo fabricated paths or line numbers were found in the merged plan. The defects are dropped steps, ungated adjacency, and two instructions that cannot be executed literally."
  }
]
```