# Planning Panel — Raw Proposals

> Verbatim panel output, with exactly two mechanical transformations: machine-absolute
> path prefixes stripped (these files are tracked and this repo is public), and markdown
> link syntax neutralized so repo-relative citations do not read as broken links from
> this directory. No other content altered.


---

## Planner 1

- **planner:** code-grounded
- **ok:** True
- **onboarding_files:** - - **path:** requests\feature-requests\H1-escalation-queue\PROJECT_SCOPE.md
    - **why:** The decided contract. 24 numbered acceptance criteria (lines 112-194), the tiered scope (196-243), seven Decisions (352-362), and the F-33 carve-out (364-368). Consume it; do not re-open fit/goals/non-goals.
  - - **path:** ROADMAP.md
    - **why:** Both the spec of record AND the parser's input. H1 row at :197 (Blocks: 1.3, IN-PROGRESS); 'Why H1 exists' :200-204; 'Parked, not scheduled' :217-222 (the three seeds, and the paragraph AC 13 reduces to a pointer). Four distinct table shapes the parser must discriminate between.
  - - **path:** tests\test_layering.py
    - **why:** The closest existing idiom and the template for tests/test_escalations.py. Docstring :1-17 explains why the guard is a pure function proven against tmp_path (subagents have read-only git). Pure fn :38-67, message builder :70-79, red/green tmp_path proofs :85-100, real assertion :106-109, failure-message test :112-123.
  - - **path:** tests\test_repo_structure.py
    - **why:** Where the folded-in cheap wins land. test_core_documents_exist :243-246 (extend + fix its docstring, which says 'three documents' over a five-name tuple), the substring idiom :260-273, the project-map test :276-283, and — critically — test_index_stage_cells_match_their_artifact_status_headers :286-319, which forces EVERY artifact in the request dir to declare the same stage word as the Index cell.
  - - **path:** tests\test_request_links.py
    - **why:** SCANNED_TREES :24 (fold #5 extends it to root-level *.md), _scanned_files :31-37 uses tree.rglob — adding REPO_ROOT there would recurse into var/ and node_modules, so the extension needs a non-recursive root glob instead. FENCED_BLOCK :26 is the exemption ESCALATIONS.md's worked-example template relies on.
  - - **path:** requests\feature-requests\_done\1.1-app-shell\PROJECT_SCOPE.md
    - **why:** Source of two of the three recovered strays: :369 (OpenAPI snapshot test, moment 1.8) and :370 (OpenAPI→TypeScript codegen, moment 1.8). :319 is NOT a stray (finding F-02 — it is the shipped SPA-history-fallback fold).
  - - **path:** requests\feature-requests\_done\1.1-app-shell\IMPLEMENTATION_PLAN.md
    - **why:** Source of the third stray at :859-861 — but READ IT: the scope mis-describes it. It is the .gitignore:63 blanket `build/` shadowing the Phase 2 `build/build-*.py` builder pattern, with named moment 2.1 — NOT a 'client-side router seam' at moment 1.10.
  - - **path:** .claude\skills\commit\SKILL.md
    - **why:** Step 4 (:109-144) is where the drain-hole check goes (AC 15). The two rails at :133-138 are the insertion point. Also the process rule the whole plan is gated on: agents commit only through /commit.
  - - **path:** CLAUDE.md
    - **why:** The project-map fenced block :52-67 (one line added, AC 19), and the measured drift at :216 — 'Nine decisions are settled' against :57, :90, :111 which all say ten (AC 20). Line budget baseline measured today: 221 lines; AC 23 caps the increase at +1.
  - - **path:** pyproject.toml
    - **why:** The gates the plan is checkpointed against. mypy strict over BOTH src and tests (:80-84) — the new parser must be fully annotated with zero new type: ignore. ruff select includes DTZ and PTH (:58-71). ruff format --check runs in CI at line-length 100.
- **architecture_notes:** TOUCHED AREA: this is a pure process/docs + structural-test change. Nothing under src/ or app/ moves (Non-Goal 9). Verified by filesystem probe on 2026-08-15: datasets/, careers/, rulesets/, lib/ all ABSENT; src/rpg_core/ contains only __init__.py; docs/data-sources.md does not exist (this repo's equivalent is docs/data-access.md, and this feature touches no data source). So ADR 0003's append-only rule, ADR 0004's version immutability, ADR 0008's cost-side-only rule, and resolve-datasets-by-name are all genuinely N/A here — recorded so a cold implementer does not go looking. No data-contracts section applies.

CURRENT STRUCTURE OF THE SEAM

1. Root documents. Five tracked *.md at the repo root, measured: CLAUDE.md (221 lines), DESIGN.md (126), GAME_DESIGN.md (361), README.md (119), ROADMAP.md (337). ESCALATIONS.md becomes the sixth. Root is the deliberate choice (scope Decision 7) because discoverability IS the mechanism.

2. The structural-test layer. tests/ holds 8 modules, 55 tests, green on branch phase1/escalation-queue as of this planning run (`uv run pytest -q` → 55 passed). Two idioms live there:
   - "parse-a-document, assert-against-the-filesystem" (test_repo_structure.py: test_every_adr_is_listed_in_the_index :220-228, test_adr_numbers_are_unique_and_contiguous :231-240).
   - "pure function, proven red AND green against tmp_path, then applied to the real tree" (test_layering.py). This is the closer shape and the one scope core-item 5 binds. The new module mirrors its four-block layout: module docstring explaining WHY → pure parser + message builder → tmp_path red/green → the real assertion + a failure-message test.

3. THE PARSER'S HOOK POINT. ROADMAP.md mixes four table shapes in one file. MEASURED with a probe script during planning: the AC-3 discriminator — strip fenced blocks, match `^\|([^|]*)\|`, accept the stripped leading cell only if it matches `^(?:\d+\.\d+|H\d+)$` — yields exactly 38 unique ids, ZERO duplicates, and rejects 57 non-item leading cells including every `**Creation**`-style bold cell in the v1 tables (:23-35, :41-51, :75-84) and every `**[OPEN-N]**` cell in the open-questions table (:324-334). All four AC-2 known-good probes (0.1, 1.3, H2, 4.6) are present. The `|---|---|` separator rows and `| # |` header rows fall out for free. This confirms AC 3's correction of F-27/F-06: emptiness is NOT the discriminator; an id-pattern match is.

4. THE WIDENED MOMENT VOCABULARY (fold #1) has a second parse. `^### (Phase \d+|Post-v1)` over ROADMAP.md returns ['Phase 0','Phase 1','Phase 2','Phase 3','Phase 4','Post-v1'] — measured. AC 5's green fixture names `post-v1` in lowercase while the heading is `Post-v1`, so the comparison MUST be case-insensitive. Recommended shape: `known_moments(roadmap_text) -> frozenset[str]` returning the union, lowercased, and the entry's moment compared lowercased.

5. THE TWO-ARGUMENT PURE FUNCTION. AC 5 requires both tmp_path fixtures to fabricate the roadmap as well as the queue, so the green case is hermetic (finding F-07). That forces the signature: the violation finder takes the queue text/path AND the roadmap text/path — it must not reach for REPO_ROOT internally. Suggested:
     parse_entries(text: str) -> list[Entry]
     known_moments(roadmap_text: str) -> frozenset[str]
     moment_violations(queue_text: str, roadmap_text: str) -> list[tuple[str, str]]   # (entry id, offending moment)
     violation_message(violations) -> str
   The real assertion (AC 6) is the only place that reads REPO_ROOT / "ESCALATIONS.md" and REPO_ROOT / "ROADMAP.md".

6. THE FENCE TENSION (AC 11 vs AC 8). ESCALATIONS.md's header carries a fenced worked-example entry, and AC 11 requires it to parse GREEN under the same regex and field guards as real entries. But AC 8 requires E-NNN ids to be unique across the queue. Resolution baked into this plan: the document-level parse STRIPS fenced blocks (reusing test_request_links.py:26's FENCED_BLOCK pattern shape), and a separate test extracts the fenced template and runs the SAME parse_entries + field guards over it. The template takes id `E-000`, which cannot collide because it never enters the document-level parse. This also keeps test_request_links.py happy: the fenced template's example paths are exempt from the link checker by the same :26 exemption.

7. WHERE THE READ SEAMS HOOK IN (three one-line skill edits, Non-Goal 10 keeps them non-programmatic):
   - .claude/skills/make-feature-request/SKILL.md Step 2 is :74-95, a bullet list at :79-91. The read bullet appends after :91 (the project-rules-constraints bullet).
   - .claude/skills/commit/SKILL.md Step 4 is :109-144. The "two rails" bullet list at :133-138 is the insertion point — the drain check is a third rail, placed where the DONE flip actually happens.
   - .claude/skills/diagnose-bug/SKILL.md Escalation block is :132-142; the Murky exit line is :161-162. One line in each: park a queue entry naming the moment, and the "escalation" name-collision disambiguation.

8. THE CI STORY IS UNCHANGED (Non-Goal 11). .github/workflows/ci.yml:18-47 is the "Lint, types, tests" job; the new module joins it automatically via `uv run pytest`. ops/branch-protection.json and tests/test_ci_contexts.py stay untouched.

9. LATENT RED — the Index/status invariant. tests/test_repo_structure.py:286-319 asserts every *.md directly under the request dir declares the SAME stage word as the Index row cell. Precedent confirms it: all four artifacts under _done/1.1-app-shell/ say `implemented` and the Index cell says `implemented`. Today H1's Index cell and both artifacts say `scoped` — green. The moment IMPLEMENTATION_PLAN.md lands saying `plan` and the Index moves to `plan`, FEATURE_REQUEST.md and PROJECT_SCOPE.md must be bumped to `plan` in the SAME commit or this test goes red. Same again at stage 4 for `implemented`. The reviews/ subdirectory is safe — the test uses item_dir.glob("*.md"), which is non-recursive.
- **phases:** - - **name:** Phase 1 — The queue exists, and the roadmap stops duplicating it
    - **goal:** Land ESCALATIONS.md at the repo root with its boundary header, the pinned six-field entry format, a fenced worked example, and all seven seed entries — and reduce ROADMAP.md:217-222 to a pointer so the migration leaves one source of truth, not two. Satisfies scope core items 1-4, 6, 7 and folds #1, #2, #3, #6, #8.
    - **steps:** - Confirm the baseline before touching anything: `uv run pytest -q` (expect 55 passed), `uv run ruff check`, `uv run ruff format --check`, `uv run mypy`. Record `(Get-Content CLAUDE.md).Count` — it must read 221. Confirm the branch is phase1/escalation-queue and the tree is clean. Do NOT run any git command that changes the tree.
      - Create ESCALATIONS.md at the repo root. Header, in this order: (a) one paragraph on what the queue is for — record the question, the alternatives, and the assumption actually taken, then keep going; (b) the REGISTER MAP as a markdown table with five rows disambiguating this from GAME_DESIGN.md §8's [OPEN-N] (design questions, indexed by answering phase — see GAME_DESIGN.md:290), DESIGN.md §2 'Open mechanism work' (DESIGN.md:50 — unsettled mechanism math, already carrying phase/item moments), DESIGN.md §4 'Open engineering questions' (DESIGN.md:115-126 — the sharpest overlap: bullets with no ids and no named moment), ADR 0010's Stage plan (per-request ceremony, docs/decisions/0010-panels-by-default.md), and /diagnose-bug's Escalation (.claude/skills/diagnose-bug/SKILL.md:132-142 — blocking and bug-scoped); (c) the ADR-0011 trigger verbatim from scope Decision 6: the first time someone proposes absorbing [OPEN-N] or DESIGN.md §4, that is the second litigation and ADR 0011 gets written then; (d) one line recording that a ROADMAP.md `Blocks` cell stops work while a queue entry never does; (e) the F-33 carve-out in both halves — no open entry stops a stage, fails a build, or gates a merge, AND the file is not exempt from ordinary CI: a malformed entry fails tests/test_escalations.py; (f) the one-line name disambiguation: 'escalation' now means two things in this repo — this one is non-blocking and moment-scoped, /diagnose-bug's is blocking and bug-scoped.
      - Pin the entry format in the header, exactly, and write the fenced worked example that instantiates it. Prescribed shape (the regexes in Phase 2 are written against this — change one and change both): an `### E-NNN — <title>` heading followed by a bullet list of six `- **Field:** value` lines. Fields: `Bears on:` (a moment — an item id like 1.3, a harness id like H2, a phase name like Phase 3, or post-v1; REQUIRED, no bare `unscheduled` hatch per core item 4), `Parked:` (YYYY-MM-DD), `Assumed:` (what was actually done, or the literal 'none — not hit yet'), `Source:` (where it surfaced, as a path with an optional :line suffix), `Status:` (closed vocabulary: `open` | `resolved`). The worked example takes id `E-000` and lives inside a fenced block so (i) test_request_links.py:26's FENCED_BLOCK exemption covers any example path in it and (ii) it never collides with a real id in the document-level parse.
      - Add the two sections `## Open` and `## Resolved`. `## Resolved` is created empty-but-present in v1 with a one-line note that a resolved entry stays in place with its Status flipped and a resolution pointer added — mirroring the immutability ethos at docs/decisions/README.md:26-37 (an ANALOGY to ADR 0003, not an application of it: ADR 0003 governs careers/**/events.jsonl only).
      - Write the three ROADMAP.md:217-222 hypotheses as entries E-001, E-002, E-003 under `## Open`: E-001 the serviceability gate for the web UI (moment `Phase 3` — the phase whose Exit condition is 'the app is genuinely serviceable', ROADMAP.md:246-251); E-002 the design/UX specialist subagent; E-003 the autonomous stage dispatcher (moment `H2` — the existing write-capable-subagent row, ROADMAP.md:198). Each carries `Assumed:` = 'none — not hit yet' and `Source:` = ROADMAP.md:217-222. LABEL THESE MOMENTS `inferred` in the entry body, per scope risk 14 — they are this planner's and the scope panel's reading of prose, not measured, and the user disposes them.
      - Write the three recovered strays as E-004, E-005, E-006. E-004 — the OpenAPI snapshot test, moment `1.8`, Source `requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369`. E-005 — OpenAPI→TypeScript codegen, moment `1.8`, Source `:370` of the same file. E-006 — CORRECTED FROM THE SCOPE, verified by reading the file: `requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861` is NOT a client-side router seam at moment 1.10; it is `.gitignore:63`'s blanket `build/` rule silently shadowing the `build/build-*.py` builder pattern CLAUDE.md prescribes for Phase 2, and the source text itself names the moment: 'Should become an intake item rather than being rediscovered at 2.1'. So E-006's moment is `2.1`, not `1.10`. Record the correction one line in the entry so the trail survives.
      - Write E-007 — the tenth open question: ROADMAP.md:334 cites it in the open-questions table while GAME_DESIGN.md §8 stops at the ninth (measured: GAME_DESIGN.md:335 is the last such heading). Moment `post-v1`, matching the roadmap row's own Phase cell. RECORDED, NOT ANSWERED (Non-Goal 13 and scope Decision 4) — it touches ADR 0005's carve-out. CONSTRAINT FROM AC 10: the entry TITLE must not contain the literal `[OPEN-` — put the citation in the body instead.
      - Edit ROADMAP.md:217-222. Reduce the 'Parked, not scheduled' paragraph to a pointer at ESCALATIONS.md. AC 13 is explicit that the paragraph's substantive observation must be PRESERVED, not dropped: that 'serviceable' is already a v1 constraint in both directions (see ROADMAP.md:53-55) and is nowhere defined or tested — that, rather than design polish, is what a gate would be for. Keep that sentence; drop only the enumeration of the three hypotheses. Do NOT hand-edit the H1 row's Status cell at :197 — /commit owns it (scope core item 9).
      - Re-run all four gates. Confirm ESCALATIONS.md trips nothing in tests/test_no_leaks.py (it scans tracked .md via TEXT_SUFFIXES at :41-58 — no drive-letter paths, no /home//Users/ paths, no email addresses; the repo is public and these entries record real assumptions, per AC 22).
      - /commit. This is the checkpoint — never `git commit` ad hoc. /commit stages deliberately, runs the doc gate, advances the H1 row against the diff, proposes a message, asks before writing, and pushes the branch. Opening the PR stays the user's.
    - **acceptance:** - `Test-Path ESCALATIONS.md` is True and the file contains, verifiably by eye: a register map naming GAME_DESIGN.md, DESIGN.md, ADR 0010's Stage plan, and /diagnose-bug's Escalation; the ADR-0011 trigger; the Blocks-vs-entry line; the F-33 carve-out in both directions; the name-collision line.
      - Seven entries `E-001` through `E-007` exist under `## Open`, each carrying all six fields, and one fenced `E-000` worked example lives in the header.
      - `Select-String -Path ROADMAP.md -Pattern 'serviceability gate|design/UX specialist|autonomous stage dispatcher'` returns at most the pointer sentence — the three hypotheses are no longer enumerated inline (AC 13).
      - `uv run pytest -q` green (still 55 tests — the guard arrives in Phase 2), `uv run ruff check` exit 0, `uv run ruff format --check` exit 0, `uv run mypy` exit 0.
      - `(Get-Content CLAUDE.md).Count` still reads 221 — CLAUDE.md is untouched in this phase.
    - **commit_note:** H1 item 1: ESCALATIONS.md — the queue, the six-field format, and seven seed entries; ROADMAP.md's parked paragraph reduced to a pointer. Roadmap H1 stays IN-PROGRESS (the structural test is still outstanding).
  - - **name:** Phase 2 — The guard: tests/test_escalations.py
    - **goal:** Make the pointer mechanical rather than aspirational. A new pure-function test module in the tests/test_layering.py idiom, proven red AND green against tmp_path with a fabricated roadmap, then applied to the real queue. Satisfies AC 1-11 and scope core item 5.
    - **steps:** - Create tests/test_escalations.py as a NEW module (scope core item 5 — the request's constraint binds the IDIOM, and a parser guard is closer to test_layering.py than to test_repo_structure.py). Open with a module docstring in the tests/test_repo_structure.py:1-9 / tests/test_layering.py:1-17 idiom explaining WHY the guard exists: the three hypotheses failed by sitting in prose with their moments named only in the prose describing them; a pointer nothing parses is a pointer that rots. State that it is a pure function proven both ways against tmp_path because subagents have read-only git and the tracked queue must not be mutated to prove a red (the same reasoning as test_layering.py:12-16).
      - Write the module-level regexes in the tests/test_repo_structure.py:21/:30 convention (named, module-level, commented). Four of them: FENCED_BLOCK (reuse the shape at tests/test_request_links.py:26 — `^([ \t>]*)(`{3,}|~{3,}).*?^\1\2.*?$` with DOTALL|MULTILINE); ENTRY_HEADING matching `^###\s+(?P<id>E-\d{3})\s*[—-]\s*(?P<title>.+)$` MULTILINE; FIELD matching `^-\s+\*\*(?P<name>[A-Za-z ]+):\*\*\s*(?P<value>.+?)\s*$` MULTILINE; ROADMAP_ROW matching `^\|(?P<first>[^|]*)\|` and ITEM_ID matching `^(?:\d+\.\d+|H\d+)$` — this pair is AC 3's corrected discriminator and it is MEASURED, not guessed (see acceptance below).
      - Write `known_moments(roadmap_text: str) -> frozenset[str]`. Strip fenced blocks first (ROADMAP.md:289-295 is a fenced ASCII diagram). Union of: every leading table cell matching ITEM_ID; every `^### (Phase \d+)` heading capture; and the literal from the `^### Post-v1` heading. Return the union LOWERCASED — AC 5's green fixture names `post-v1` while the heading reads `Post-v1`, so case-insensitive comparison is load-bearing, not a nicety.
      - Write `parse_entries(text: str) -> list[Entry]` where Entry is a frozen dataclass (id, title, fields: dict[str,str], section). Strip fenced blocks so the E-000 template never enters the document-level parse. Split the body on `## Open` / `## Resolved` so each entry carries the section it sits in — AC 9 needs that to prove Status agrees with placement. Fully annotate: mypy is strict over `tests` as well as `src` (pyproject.toml:80-84) and zero new `# type: ignore` is allowed (AC 21).
      - Write `moment_violations(queue_text: str, roadmap_text: str) -> list[tuple[str, str]]` returning (entry id, offending moment) pairs, and `violation_message(violations) -> str` mirroring tests/test_layering.py:70-79. AC 7: the message must name the queue file, the offending entry id, the offending moment, and STATE THE RULE — every entry names a moment that exists in ROADMAP.md, because an entry with no real moment never surfaces at the item boundary that drains the queue.
      - Write the AC 5 red/green pair in the tests/test_layering.py:85-100 idiom, mutating no tracked file. RED: `tmp_path` gets a fabricated queue naming moment `9.9` and a fabricated roadmap; assert the result is EXACTLY that one violation. GREEN: `tmp_path` gets a fabricated queue naming `1.3`, `H2`, `Phase 3`, and `post-v1`, plus a fabricated roadmap that contains all four; assert the result is empty. Both fixtures fabricate the roadmap (finding F-07) so the green case is hermetic and cannot pass by accidentally reading the real one.
      - Write the AC 2 positive assertion: `known_moments` over the REAL ROADMAP.md returns a non-empty set that contains `0.1`, `1.3`, `h2`, `4.6` and has size >= 30. Deliberately NOT an exact count (finding F-28) — an exact 38 fires in every unrelated PR that adds a row. Add a comment saying so, or the next agent will 'tighten' it.
      - Write the AC 3 negative assertion: build the text of the three v1 scope tables (ROADMAP.md:23-35, :41-51, :75-84) and the open-questions table (:324-334) — either by slicing the real file between known headings, or as literal fixtures — and assert each contributes ZERO ids. The comment must say WHY: their data rows lead with `**Creation**`-style bold text, not empty cells, so emptiness is not the discriminator (findings F-27/F-06).
      - Write the AC 4 non-vacuity guard: assert `parse_entries` matched at least one entry over the real queue, failing with the exact message 'no entries parsed — the entry format drifted from the regex'. Without this, a format change silently turns every `for entry in parsed` loop into `assert not []` and the module passes forever.
      - Write the AC 6 real assertion: `moment_violations` over the real ESCALATIONS.md and the real ROADMAP.md returns []. Write the AC 7 failure-message test in the tests/test_layering.py:112-123 idiom, over a fabricated violation.
      - Write the AC 8 field-and-vocabulary guard: every parsed entry carries all SIX required fields (`Bears on:`, `Parked:`, `Assumed:`, `Source:`, `Status:`, plus an id matching `^E-\d{3}$`); ids are unique; every `Status:` value is in the closed vocabulary {open, resolved}. SIX, not five — finding F-26 caught `Source:` being required by the tests while absent from the pinned format; the format written in Phase 1 already carries it.
      - Write the AC 9 state-is-stored-once guard: the `## Open`/`## Resolved` section an entry sits in agrees with its `Status:`. Same invariant test_index_stage_cells_match_their_artifact_status_headers (tests/test_repo_structure.py:286-319) already enforces between the Index and artifact headers — cite it in the docstring.
      - Write the AC 10 register-boundary guard: no entry title contains the literal `[OPEN-`, and the queue body contains its boundary header naming `GAME_DESIGN.md`, `DESIGN.md`, ADR 0010's Stage plan, and /diagnose-bug's Escalation.
      - Write the AC 11 template guard (this criterion exists because finding F-01 caught goal 1 having no proof). Extract the fenced worked-example block from the header, run the SAME `parse_entries` and the SAME field guards over it, assert exactly one entry comes back and it passes every guard. A worker copying the template cannot produce a malformed entry — that is what 'the third move exists' reduces to mechanically.
      - Write the AC 12 seed-migration guard in the substring idiom of tests/test_repo_structure.py:260-273: assert the queue contains distinctive strings for all SEVEN seeds. Seven, not eight — finding F-02 established that _done/1.1-app-shell/PROJECT_SCOPE.md:319 is a shipped fold, not a stranded decision, so it is three strays plus three hypotheses plus the tenth-open-question orphan.
      - Run all four gates. /commit — this is the checkpoint.
    - **acceptance:** - `uv run pytest tests/test_escalations.py -q` green, with the module carrying a why-this-exists docstring.
      - The red fixture returns exactly one violation for moment `9.9`; the green fixture returns none; both run entirely against tmp_path and `git status --short` shows no tracked file modified by the test run.
      - `known_moments` over the real ROADMAP.md returns >= 30 ids including 0.1, 1.3, h2, 4.6 (MEASURED during planning: exactly 38 unique, zero duplicates, all four present).
      - All three v1 scope tables and the open-questions table each contribute zero ids (MEASURED during planning: 57 non-item leading cells rejected, including every `**Creation**`- and `**[OPEN-N]**`-style bold cell).
      - `moment_violations` over the real queue + real roadmap returns [].
      - The fenced E-000 template parses to exactly one entry passing all six field guards.
      - All seven seed substrings are present.
      - `uv run ruff check`, `uv run ruff format --check`, and `uv run mypy` all exit 0 with zero new `# type: ignore` (AC 21).
      - `uv run pytest` green overall.
    - **commit_note:** H1 item 2: tests/test_escalations.py — the parser guard. Every entry's named moment is asserted to exist in ROADMAP.md, proven red and green against tmp_path, with a non-vacuity guard so a format drift fails loudly instead of passing forever.
  - - **name:** Phase 3 — Discoverability, and the folded cheap wins
    - **goal:** Stop the queue being findable only by knowing it exists, and land the four cheap folds that ride along. Satisfies AC 17, 18, 19, 20 and folds #4, #5, #7, #9.
    - **steps:** - Extend tests/test_repo_structure.py:243-246's test_core_documents_exist to include ESCALATIONS.md, and FIX ITS DOCSTRING in the same edit (AC 17): it currently reads 'The three documents that carry the design' over a five-name tuple — it already lied before this change, and the minimalist dissent in scope Decision 7 ('a queue is not a design document') is answered by making the docstring honest rather than by leaving the file unguarded. Without the deletion guard, removing ESCALATIONS.md silently empties the whole mechanism.
      - Extend tests/test_request_links.py to root-level *.md (AC 18, fold #5). DO NOT simply add REPO_ROOT to SCANNED_TREES at :24 — `_scanned_files` at :31-37 calls `tree.rglob('*.md')`, so REPO_ROOT would recurse into var/spike/vite-probe/node_modules (700+ markdown files), .venv/, and _done/. Instead add a non-recursive root pass: keep SCANNED_TREES as the recursive trees and extend `_scanned_files` with `files.extend(REPO_ROOT.glob('*.md'))`. MEASURED 2026-08-15 by running the real `_dead_links` over all five current root documents: every one returns [] — so this costs zero cleanup today. It does mean ESCALATIONS.md's own links are checked from here on; the fenced worked example is exempt by :26.
      - Add one line inside CLAUDE.md's project-map FENCED block (:52-67), e.g. `ESCALATIONS.md      Parked decisions, each naming the moment it comes back`. Inside the fence deliberately, so it adds no link-check surface (scope core item 8).
      - Fix CLAUDE.md:216 in the same edit (AC 20, fold #9): 'Nine decisions are settled' → 'Ten'. MEASURED drift — :57 says 'ten settled calls', :90 says 'Ten ADRs', :111 says 'All ten are ADRs', and docs/decisions/README.md:43-52 lists 0001-0010. One word, zero lines.
      - VERIFY THE BUDGET IMMEDIATELY: `(Get-Content CLAUDE.md).Count` must read at most 222 (AC 23, measured baseline 221). The map line is +1 and the Nine→Ten fix is +0. Resist adding a bullet under 'Important locations' as well — that is +2 and turns the criterion red. (Note: .claude/skills/update-docs/SKILL.md:76-78 prescribes `Measure-Object -Line` for this check and it under-reports; scope risk 15 records that as a separate bugfix request, out of scope here. Use `(Get-Content …).Count`, which is what AC 23 specifies.)
      - Add one row to README.md's project-map fenced block (:55-73), in the same tree-drawing style, after the CLAUDE.md line at :60.
      - Add one line to requests/README.md beside the three-track table at :6-10 pointing at ../ESCALATIONS.md. This is the ONLY pointer location covered by the link checker today (requests/ is in SCANNED_TREES at :24), so it is the one pointer that cannot silently rot (fold #7).
      - Add the AC 19 discoverability tests to tests/test_repo_structure.py, beside test_claude_md_project_map_lists_both_new_directories at :276-283 and using the existing `_read` helper at :256-257: assert 'ESCALATIONS.md' appears in both CLAUDE.md and README.md.
      - Run all four gates. /commit — this is the checkpoint.
    - **acceptance:** - `uv run pytest tests/test_repo_structure.py -q` green with test_core_documents_exist covering ESCALATIONS.md and its docstring no longer claiming three documents over a five-name tuple.
      - `uv run pytest tests/test_request_links.py -q` green with root-level *.md scanned and no recursion into var/, .venv/, or _done/.
      - `(Get-Content CLAUDE.md).Count` <= 222.
      - `Select-String -Path CLAUDE.md -Pattern 'Nine decisions'` returns nothing.
      - 'ESCALATIONS.md' appears in CLAUDE.md's project map, README.md's project map, and requests/README.md, each substring-tested.
      - `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all green/exit 0.
    - **commit_note:** H1 item 3: discoverability — the queue in both project maps and requests/README.md, added to the core-documents guard, the link checker extended to root documents, and CLAUDE.md's measured nine-vs-ten drift fixed.
  - - **name:** Phase 4 — The read seams: stop it being inert config
    - **goal:** Give the queue three places that actually read it, answering the risk all three scopers independently called feature-killing. Satisfies AC 14, 15, 16 and scope core items 10, 11, 12 (Decisions 1, 3, 5), plus fold #8's second half.
    - **steps:** - Add ONE bullet to .claude/skills/make-feature-request/SKILL.md Step 2 (:74-95), after the project-rules-constraints bullet at :90-91 (Decision 1, AC 14). Content: if this request maps to a roadmap row, read ESCALATIONS.md and surface any open entry whose `Bears on:` names that row's moment — a decision parked against this item is context the request should carry, not rediscover. This is a READ added to a checklist; it changes no decision logic. That distinction matters: it is what keeps this inside the request's 'no skill behavior changes' non-goal, and it is why scope Decision 1 gated it only for touching a skill file.
      - Add ONE bullet to .claude/skills/commit/SKILL.md Step 4, after the two rails at :133-138 (Decision 5, AC 15). Content: when flipping a roadmap row to `DONE`, check ESCALATIONS.md for open entries naming that row and surface them before committing — an entry parked against an item goes permanently invisible the moment the item closes. Step 4 already parses roadmap rows and flips statuses (:116-131), so it is the one thing running at exactly that moment; this is why the drain hole (finding F-32) closes here and nowhere else. Keep it to one bullet — Step 3's keep-it-lightweight rule at :102-103 is explicit that a gate people route around is worse than no gate.
      - Add ONE line to .claude/skills/diagnose-bug/SKILL.md's Escalation block at :132-142 (Decision 3, AC 16): also park an ESCALATIONS.md entry naming the moment the bug bears on. POINTER ONLY — the RCA keeps its Escalation section and the evidence trail is not duplicated. This preserves the non-blocking property exactly: the bug still waits; the queue only remembers that it is waiting. Add the one-line name disambiguation here too (fold #8): 'escalation' means two things in this repo — /diagnose-bug's is blocking and bug-scoped, ESCALATIONS.md's is non-blocking and moment-scoped.
      - Add three substring tests to tests/test_escalations.py, one per seam, each asserting the bullet is present in its SKILL.md. Choose the asserted substring carefully: short enough to survive a wording tweak, distinctive enough to fail if the bullet is deleted. 'ESCALATIONS.md' alone is the safest anchor in all three files (none mentions it today — verify with `Select-String` before writing the tests).
      - CHECK THE LINK SURFACE: .claude/skills/ is in SCANNED_TREES (tests/test_request_links.py:24), so any markdown link you write in these three skill files must resolve RELATIVE TO THE SKILL FILE — `../../../ESCALATIONS.md` from `.claude/skills/<name>/SKILL.md`. A bare unlinked mention avoids the question entirely and is the lower-risk choice.
      - Run all four gates. /commit — this is the checkpoint.
    - **acceptance:** - `Select-String -Path .claude/skills/make-feature-request/SKILL.md,.claude/skills/commit/SKILL.md,.claude/skills/diagnose-bug/SKILL.md -Pattern 'ESCALATIONS.md'` returns a hit in each of the three files.
      - Three substring tests in tests/test_escalations.py prove each bullet is present; `uv run pytest tests/test_escalations.py -q` green.
      - `uv run pytest tests/test_request_links.py -q` green — any relative link added in a skill file resolves.
      - The /diagnose-bug edit is a pointer only: its Escalation section at :136-142 still carries (a) why murky, (b) verdict, (c) follow-up, unchanged.
      - `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all green/exit 0.
    - **commit_note:** H1 item 4: the read seams — /make-feature-request surfaces entries for the item it is opening, /commit surfaces open entries when a row flips to DONE, /diagnose-bug's murky hand-off gains a destination. Three substring-tested one-liners against the inert-config risk.
  - - **name:** Phase 5 — Full verification, status advance, and the user-run criterion
    - **goal:** Prove the whole acceptance contract in one pass, reconcile every status header the structural tests bind together, and hand the user the one criterion no agent may claim. Satisfies AC 21, 22, 23, 24 and scope core item 9.
    - **steps:** - Full green run from a clean shell: `uv run pytest` (expect the 55 baseline plus the new module's tests), `uv run ruff check`, `uv run ruff format --check`, `uv run mypy`. All four must be exit 0, and mypy must report zero new `# type: ignore` (AC 21).
      - Confirm AC 22 specifically: `uv run pytest tests/test_no_leaks.py -q` green. A new tracked .md is now scanned via TEXT_SUFFIXES (tests/test_no_leaks.py:41-58). The repo is PUBLIC and these entries record real assumptions taken when nobody was sure — re-read all seven entries for anything that should not be world-readable forever. test_no_leaks blocks drive-letter paths, home directories, and email addresses; it does not block indiscretion (scope risk 10).
      - Re-measure `(Get-Content CLAUDE.md).Count` <= 222 (AC 23).
      - RECONCILE THE STATUS HEADERS — this is the step that goes red if skipped. tests/test_repo_structure.py:286-319 asserts every *.md directly under requests/feature-requests/H1-escalation-queue/ declares the SAME stage word as its Index row cell in requests/feature-requests/README.md:116. Precedent: all four artifacts under _done/1.1-app-shell/ read `implemented` against an `implemented` cell. So when this work lands, FEATURE_REQUEST.md:1, PROJECT_SCOPE.md:1, and IMPLEMENTATION_PLAN.md:1 must ALL be bumped to `implemented` in the SAME commit that sets the Index cell to `implemented`. The reviews/ subdirectory is exempt — the test uses a non-recursive glob.
      - Let /commit advance ROADMAP.md's H1 row at :197 against the diff (scope core item 9 — never hand-edit it). The Deliverable cell names three things: ESCALATIONS.md, the parked-decision format, and a structural test that every entry names a real roadmap item. All three are now in the tree, so the row goes `IN-PROGRESS` → `DONE`, which unblocks `Blocks: 1.3`. Per .claude/skills/commit/SKILL.md:135-136, never mark ahead — the flip belongs in the commit that completes it, which is this one. Phase 1's header aggregate stays IN-PROGRESS: items 1.2-1.11 are all NOT STARTED and the phase Exit condition (two careers, ten box scores each, one correction, a spend, a worksheet applied) is nowhere near met.
      - The /commit run for this phase will hit its own new drain-hole bullet from Phase 4 — flipping H1 to DONE should surface any open entry naming H1. There are none (no seed names H1). That is the seam firing correctly, not a failure; note it in the commit conversation as the first live proof the mechanism works.
      - HAND AC 24 TO THE USER. It is USER-RUN and not panel-claimable: picking up item 1.3, the entries naming 1.3 surface without the user being asked anything; the user can answer an entry by editing ESCALATIONS.md alone with no build running; and the parked context reads as useful rather than as noise. NOTE HONESTLY at hand-off: no current seed names moment `1.3`. Scope risk 2 anticipated this — 'item 1.3 can start by reading the queue' only holds if something is parked against 1.3 by then, and the seven seeds make the queue non-empty on day one but do not by themselves make 1.3 the item they surface for. Say so rather than letting the criterion be read as pre-satisfied.
      - /commit — the final checkpoint. The PR stays the user's to open and merge; enforce_admins: true means nobody merges past a red check anyway.
    - **acceptance:** - `uv run pytest` green overall; `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all exit 0.
      - `uv run pytest tests/test_repo_structure.py::test_index_stage_cells_match_their_artifact_status_headers -q` green — the Index cell and all three artifact headers agree.
      - `(Get-Content CLAUDE.md).Count` <= 222.
      - ROADMAP.md:197's Status cell reads DONE, written by /commit against the diff rather than hand-edited; the Phase 1 header aggregate is still IN-PROGRESS.
      - AC 24 is handed to the user unclaimed, with the honest note that no seed currently names moment 1.3.
    - **commit_note:** H1 final: full green run, request-artifact statuses reconciled to implemented, roadmap H1 advanced to DONE by the commit gate. Blocks: 1.3 released — item 1.3 correction-by-append may now start.
- **testing:** EVERYTHING IS PROVEN BY `uv run pytest`, and every acceptance criterion in the scope except AC 24 is a command a cold agent runs to a pass/fail. The four local gates run at the end of every phase and CI re-runs the same three on the PR under the "Lint, types, tests" job (.github/workflows/ci.yml:18-47): `uv run ruff check`, `uv run ruff format --check`, `uv run mypy`, `uv run pytest -m "not network" --cov=rpg_core --cov=rpg_api`. No new CI job and no branch-protection change (Non-Goal 11) — ops/branch-protection.json and tests/test_ci_contexts.py stay untouched.

BASELINE, MEASURED DURING PLANNING on branch phase1/escalation-queue with a clean tree: `uv run pytest -q` → 55 passed. Any red before Phase 1 starts is a pre-existing problem, not something this work caused.

THE TEST MODULE'S SHAPE, and why it is that shape. tests/test_escalations.py copies tests/test_layering.py's four-block layout because a structural guard over a TRACKED file cannot prove its red case by breaking the tracked file — subagents have read-only git (CLAUDE.md's subagent rule, and test_layering.py:12-16 says so out loud). So the parser is a pure text→result function, the red and green are fabricated in tmp_path, and only one test reaches for the real ESCALATIONS.md and ROADMAP.md. AC 5 goes one step further than test_layering.py did: BOTH fixtures fabricate the roadmap as well as the queue (finding F-07), so the green case cannot pass by silently reading the real roadmap.

THREE FAILURE MODES THE SUITE IS SPECIFICALLY BUILT TO CATCH, each with its own criterion:
1. VACUITY (AC 4). `for entry in parse_entries(body): assert ...` over an empty list passes forever. The non-vacuity guard asserts at least one entry parsed and fails with a message naming the cause — "no entries parsed — the entry format drifted from the regex". This is the single most valuable test in the module, because it is the one that fires when the format changes rather than when an entry is wrong.
2. A MIS-GROUNDED DISCRIMINATOR (AC 3, findings F-27/F-06). ROADMAP.md carries four table shapes. A naive "the leading cell is non-empty" rule would swallow every `**Creation**`-style row in the v1 tables and every `**[OPEN-N]**` row in the open-questions table as if they were item ids. The corrected discriminator is an id-pattern match, and the negative test proves all four non-item tables contribute zero. MEASURED during planning by running the exact discriminator over the real file: 38 unique ids accepted, 0 duplicates, 57 non-item leading cells rejected, all four AC-2 probes present.
3. AN OVER-TIGHT ASSERTION (AC 2, finding F-28). Asserting an exact 38-id set makes the guard fire in every unrelated PR that adds a roadmap row. The assertion is `>= 30` plus four known-good ids, and the reason is written in a comment so the next agent does not "tighten" it back.

REGRESSION SAFETY. Three existing tests are extended rather than replaced, and each extension has a measured basis: test_core_documents_exist (tests/test_repo_structure.py:243-246) gains one name and loses a false docstring; test_request_links.py gains a non-recursive root pass, and `_dead_links` was run over all five current root documents during planning — every one returned [], so the extension costs zero cleanup today; test_no_leaks.py needs no change but now scans one more tracked .md by construction (TEXT_SUFFIXES at :41-58). Nothing under src/ or app/ is touched, so tests/test_api_health.py, tests/test_api_spa.py, tests/test_packaging.py, and tests/test_layering.py are untouched by construction.

THE ONE TEST THAT WILL BITE IF IGNORED. tests/test_repo_structure.py:286-319 forces every *.md directly under the request directory to declare the same stage word as its Index row cell. It is currently green (Index `scoped`, both artifacts `scoped`) and goes red the moment those drift. Phase 5 reconciles them explicitly; do not treat it as bookkeeping.

WHAT NO TEST CAN PROVE. AC 24 is marked USER-RUN in the scope and must stay unclaimed by any panel: that the entries naming 1.3 surface without the user being asked anything, that an entry can be answered by editing ESCALATIONS.md alone with no build running, and that the parked context reads as useful rather than as noise. The honest caveat to carry to the user is scope risk 2 — no seed currently names moment 1.3, so the observable signal the request was written around is not automatically satisfied by the seven seeds.
- **risks:** - THE SCOPE MIS-CITES THE THIRD RECOVERED SEED — verified by reading the file. requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861 is not a 'client-side router seam' at moment 1.10; it is `.gitignore:63`'s blanket `build/` rule shadowing the Phase 2 `build/build-*.py` builder pattern, and the source text names its own moment: 'Should become an intake item rather than being rediscovered at 2.1'. The scope's fold #2 appears to have conflated it with _done/1.1-app-shell/PROJECT_SCOPE.md:319, which says 'stops item 1.10's router from being a breaking change' — and :319 is exactly the line finding F-02 established is a shipped fold, not a stray. Record E-006 with moment 2.1. Writing it as 1.10 would still pass the guard (1.10 is a real id), which is why this must be caught by reading rather than by CI.
  - TWO OFF-BY-ONE LINE CITATIONS IN THE SCOPE'S 'Affected Area & Pointers'. 'Why H1 exists' is ROADMAP.md:200-204, not :199-203. 'Parked, not scheduled' is :217-222, not :216-221 — the scope's own Problem section (line 51) gets this right while its pointer section (line 316) does not. Trust the numbers in this plan; they were re-verified on 2026-08-15.
  - THE INDEX/STATUS INVARIANT IS A LATENT RED, and the stage-3 skill does not warn about it. tests/test_repo_structure.py:286-319 compares EVERY *.md in the request dir against the Index cell. .claude/skills/create-implementation-plan/SKILL.md Step 5 (:170-173) tells you to set the Index cell to `plan` and to open IMPLEMENTATION_PLAN.md at `plan`, but says nothing about the two sibling artifacts still reading `scoped` — which is precisely the state that fails the test. Bump all three headers together, and again at stage 4 for `implemented`.
  - AC 23's CLAUDE.md budget is +1 line and nothing more. The obvious instinct — one line in the project map AND a bullet under 'Important locations' (CLAUDE.md:76-95, where the other core documents are described) — is +2 and fails the criterion. The Nine→Ten fix at :216 is a word swap, +0. Measure with `(Get-Content CLAUDE.md).Count`, not the `Measure-Object -Line` command at .claude/skills/update-docs/SKILL.md:76-78, which under-reports (scope risk 15; its own fix is out of scope).
  - EXTENDING test_request_links.py NAIVELY IS DESTRUCTIVE. `_scanned_files` at :31-37 calls `tree.rglob('*.md')`. Adding REPO_ROOT to SCANNED_TREES at :24 would pull in 700+ markdown files under var/spike/vite-probe/node_modules/, .venv/, and — worse — the _done/ artifacts the :36 skip deliberately exempts, whose links describe the repo as it was. Add a separate non-recursive `REPO_ROOT.glob('*.md')` pass instead.
  - THE FENCED TEMPLATE VS THE UNIQUENESS GUARD. AC 11 requires the worked example to parse green under the same guards; AC 8 requires E-NNN ids to be unique. If the document-level parse does not strip fences, the template becomes an eighth entry — its moment must then be real (AC 6) and its id must not collide (AC 8). The prescribed resolution is: strip fences at document level, extract-and-parse the template separately, and give the template id E-000. Deviating from this is fine but both constraints must still hold.
  - CASE SENSITIVITY IN THE WIDENED MOMENT VOCABULARY. AC 5's green fixture names `post-v1` in lowercase; ROADMAP.md's heading reads `### Post-v1`. A case-sensitive comparison makes AC 5's green case fail while every other test passes, which reads as a fixture bug rather than a parser bug. Lowercase both sides.
  - INERTNESS — the dominant risk, named independently by all three scopers. Decisions 1, 3, and 5 (Phase 4) mitigate it; they do not eliminate it, because nothing yet WRITES entries automatically (Decision 2 defers that to its own request). Symptom to watch for later: entry E-007 is still open a year from now and nobody has added E-008.
  - UNCONFIRMED — the drain cadence itself. 'Drained at item boundaries' assumes every item boundary passes through /make-feature-request. ADR 0010's entry condition (requests/README.md:68-70) explicitly exempts typo fixes, dependency bumps, and doc edits from the pipeline, so that does not hold universally. Label it `unconfirmed` in the queue's header. Nothing in this plan is built on top of it — the /commit drain check (Decision 5) is deliberately a SECOND cadence that fires at the DONE flip precisely because the first one is not guaranteed.
  - INFERRED, NOT MEASURED — the seed moments. E-001/E-002/E-003's moments are this planner's and the scope panel's reading of ROADMAP.md prose; E-004/E-005's (1.8) were the 1.1 panel's judgment as of 2026-08-14. Label them in the entries. The user disposes; the guard only proves the moment EXISTS, never that it is the right one.
  - MIS-FILING UNDER THE NEAREST MOMENT. Hard-requiring a moment (core item 4) pressures a worker to attach an unmoored question to whatever row is closest — corrupting the drain while looking perfectly green. The widened vocabulary (item id | phase name | post-v1) reduces this but does not remove it. The header's register map is the only other defence, and it is prose.
  - PUBLIC REPO. Entries record real assumptions taken when nobody was sure, world-readable forever. tests/test_no_leaks.py blocks drive-letter paths, home directories, and email addresses — it does not block indiscretion. Re-read all seven entries before the final /commit.
  - ENTRY-ID COLLISIONS ACROSS BRANCHES (finding F-38). Sequential E-NNN in one file merges cleanly into duplicates. Unmitigated in v1 by decision — AC 8's uniqueness guard catches it at CI rather than preventing it. Acceptable while entries are few; do not try to solve it here.
  - SCOPE CREEP HAS A BINDING COST. `Blocks: 1.3` at ROADMAP.md:197 is binding, and 1.3 correction-by-append is the append-only model ADR 0003 treats as load-bearing. Every extra idea adopted here delays it. The scope already dropped five proposals (a read-only ops/escalations.py drain command, a Supersedes: field, a resolved-entry archive, a per-entry Surfaced by: field, an /update-docs drift check) and deferred three (skills that WRITE entries, the [OPEN-N] mirror guard, ADR 0011) — do not reintroduce any of them.
  - ROADMAP RENUMBERING TAX, with recent precedent: ADR 0010 just deleted two columns from every roadmap table. Item ids are hand-edited cells; a renumbering silently orphans entries. The guard turns that from silent into a red build, which is the whole point — but it also means a future renumbering PR must fix the queue in the same commit.
- **files_to_touch:** - - **path:** ESCALATIONS.md
    - **change:** CREATE (Phase 1). Root document. Boundary header with the five-row register map, the ADR-0011 trigger, the Blocks-vs-entry line, the F-33 both-halves carve-out, and the escalation name-collision line. The pinned six-field entry format plus a fenced E-000 worked example. `## Open` with seven seeds E-001..E-007; `## Resolved` present but empty with its resolved-in-place note.
  - - **path:** ROADMAP.md
    - **change:** EDIT (Phase 1) lines 217-222 only — reduce 'Parked, not scheduled' to a pointer at ESCALATIONS.md while PRESERVING the sentence that 'serviceable' is a v1 constraint in both directions and is nowhere defined or tested (AC 13). Line 197's Status cell is advanced by /commit in Phase 5, never hand-edited.
  - - **path:** tests\test_escalations.py
    - **change:** CREATE (Phase 2, extended in Phase 4). New module in the test_layering.py idiom: why-this-exists docstring; module-level FENCED_BLOCK / ENTRY_HEADING / FIELD / ROADMAP_ROW+ITEM_ID regexes; pure `parse_entries`, `known_moments`, `moment_violations`, `violation_message`; tmp_path red/green with a fabricated roadmap; the real assertion; and the field, vocabulary, section-agreement, register-boundary, template, non-vacuity, seed-migration, and (Phase 4) three seam substring tests. Fully annotated — mypy is strict over tests.
  - - **path:** tests\test_repo_structure.py
    - **change:** EDIT (Phase 3). Extend test_core_documents_exist (:243-246) with ESCALATIONS.md and fix its docstring, which claims 'the three documents' over a five-name tuple. Add an AC-19 discoverability test beside test_claude_md_project_map_lists_both_new_directories (:276-283), using the existing `_read` helper (:256-257).
  - - **path:** tests\test_request_links.py
    - **change:** EDIT (Phase 3). Extend `_scanned_files` (:31-37) with a NON-RECURSIVE `REPO_ROOT.glob('*.md')` pass. Do NOT add REPO_ROOT to SCANNED_TREES (:24) — the loop uses rglob and would pull in var/, .venv/, node_modules/, and the deliberately-exempt _done/ tree.
  - - **path:** CLAUDE.md
    - **change:** EDIT (Phase 3). One line added inside the project-map fenced block (:52-67) naming ESCALATIONS.md, and the measured drift at :216 fixed from 'Nine decisions are settled' to 'Ten'. Net +1 line against the 221-line baseline — nothing else (AC 23).
  - - **path:** README.md
    - **change:** EDIT (Phase 3). One row added to the project-map fenced block (:55-73), after the CLAUDE.md line at :60, in the same tree-drawing style.
  - - **path:** requests\README.md
    - **change:** EDIT (Phase 3). One line beside the three-track table (:6-10) pointing at ../ESCALATIONS.md. The only pointer location the link checker already covers, so the one that cannot silently rot (fold #7).
  - - **path:** .claude\skills\make-feature-request\SKILL.md
    - **change:** EDIT (Phase 4). One READ bullet appended to Step 2's list (after :90-91): surface open queue entries whose `Bears on:` names this item's moment. Adds a read to a checklist; changes no decision logic (Decision 1, AC 14).
  - - **path:** .claude\skills\commit\SKILL.md
    - **change:** EDIT (Phase 4). One bullet appended to Step 4's two rails (after :138): when flipping a roadmap row to DONE, surface open entries naming it. Closes the drain hole finding F-32 found (Decision 5, AC 15). Keep it to one bullet — Step 3's keep-it-lightweight rule at :102-103.
  - - **path:** .claude\skills\diagnose-bug\SKILL.md
    - **change:** EDIT (Phase 4). One line in the Escalation block (:132-142) telling it to also park a queue entry naming the moment, POINTER ONLY — the RCA keeps its evidence trail. Plus the one-line escalation name disambiguation (fold #8).
  - - **path:** requests\feature-requests\README.md
    - **change:** EDIT (stage 3 now, again in Phase 5). The Index row at :116 — Stage cell `scoped` → `plan` when this plan lands, → `implemented` when the work does. Must move in lockstep with every artifact header in the directory or tests/test_repo_structure.py:286-319 goes red.
  - - **path:** requests\feature-requests\H1-escalation-queue\FEATURE_REQUEST.md
    - **change:** EDIT (status header only, Phase 5). Line 1's Status blockquote bumped to match the Index cell. Body untouched — it is the intake record.
  - - **path:** requests\feature-requests\H1-escalation-queue\PROJECT_SCOPE.md
    - **change:** EDIT (status header only, Phase 5). Line 1's Status blockquote bumped to match the Index cell. Body untouched — it is decided.
  - - **path:** requests\feature-requests\H1-escalation-queue\IMPLEMENTATION_PLAN.md
    - **change:** CREATE (stage 3 deliverable). Opens at `plan · created 2026-08-15 · decided · next: implement`, bumped to `implemented` in Phase 5.
- **code_references:** - - **ref:** ROADMAP.md:197
    - **claim:** The H1 row — Deliverable names ESCALATIONS.md, the parked-decision format, and a structural test that every entry names a real roadmap item; Needs '—'; Blocks 1.3; Status IN-PROGRESS. Verified: this is the harness table's first data row.
  - - **ref:** ROADMAP.md:217-222
    - **claim:** The 'Parked, not scheduled' paragraph naming the three seed hypotheses, and the sentence AC 13 requires preserved — that 'serviceable' is a v1 constraint in both directions and is nowhere defined or tested. The scope's pointer section cites :216-221; :217-222 is correct.
  - - **ref:** ROADMAP.md:200-204
    - **claim:** 'Why H1 exists' — eight decisions in one sitting, all synchronous. The scope cites :199-203; :200-204 is correct.
  - - **ref:** ROADMAP.md:195-198
    - **claim:** The Phase 1 harness table — header, separator, H1, H2. It carries a sixth `Blocks` column the numbered item tables do not, and the AC-3 discriminator handles it because it only ever looks at the leading cell.
  - - **ref:** ROADMAP.md:23-35, :41-51, :75-84
    - **claim:** The three v1 scope tables whose data rows lead with bold text (`**Creation**`, `**Career management**`, `**Age/development multiplier**`), not empty cells. This is why AC 3 replaced 'emptiness' with an id-pattern match. Measured: all three contribute zero ids under the corrected discriminator.
  - - **ref:** ROADMAP.md:324-334
    - **claim:** The open-questions table; its data rows lead with `**[OPEN-N]**` and it therefore also contributes zero ids. Line 334 is the tenth-open-question row — the measured orphan, cited here but never written in GAME_DESIGN.md §8.
  - - **ref:** GAME_DESIGN.md:335
    - **claim:** The last `**[OPEN-N]** — …` heading in §8, confirming §8 stops at the ninth. §8 itself begins at GAME_DESIGN.md:290. This is what makes the tenth-question orphan a MEASURED claim, not an asserted one.
  - - **ref:** tests/test_layering.py:1-17
    - **claim:** The module docstring idiom AC 1 requires the new module to match, and the explicit statement of WHY the guard is a pure function proven against tmp_path: 'subagents have read-only git, and this is the very PR whose point is that the import must not be there.'
  - - **ref:** tests/test_layering.py:38-67
    - **claim:** `web_imports_under(root: Path) -> list[tuple[str, str]]` — the pure-function shape `moment_violations` copies: takes its input as an argument rather than reaching for REPO_ROOT, returns a list of violation tuples.
  - - **ref:** tests/test_layering.py:85-100
    - **claim:** The two tmp_path proofs — `test_a_web_import_is_reported` asserts an exact single-violation list, `test_a_clean_tree_reports_nothing` asserts []. This is the exact idiom AC 5 names, extended so both fixtures also fabricate the roadmap.
  - - **ref:** tests/test_layering.py:112-123
    - **claim:** `test_the_failure_message_cites_the_architecture_record` — the AC 7 template: build a message from a fabricated violation and assert it names the record and the offending item.
  - - **ref:** tests/test_repo_structure.py:243-246
    - **claim:** `test_core_documents_exist` — its docstring reads 'The three documents that carry the design, each doing one job' over a five-name tuple (GAME_DESIGN.md, ROADMAP.md, DESIGN.md, README.md, CLAUDE.md). AC 17 extends the tuple to six and fixes the docstring in the same edit.
  - - **ref:** tests/test_repo_structure.py:260-273
    - **claim:** `test_no_document_still_claims_there_is_no_application` — the substring-assertion idiom AC 12's seed-migration guard is written in, including its note on lowercasing before comparison.
  - - **ref:** tests/test_repo_structure.py:276-283
    - **claim:** `test_claude_md_project_map_lists_both_new_directories` — the AC 19 discoverability tests sit beside this and reuse the `_read` helper defined at :256-257.
  - - **ref:** tests/test_repo_structure.py:286-319
    - **claim:** `test_index_stage_cells_match_their_artifact_status_headers` — asserts each `item_dir.glob('*.md')` artifact's Status stage word equals the Index row's Stage cell. Non-recursive, so reviews/ is exempt. This is the test that goes red if the plan's status headers drift from the Index cell.
  - - **ref:** tests/test_request_links.py:24
    - **claim:** `SCANNED_TREES = (REPO_ROOT / 'requests', REPO_ROOT / '.claude' / 'skills')` — fold #5 extends the scan to root-level *.md, but through a separate non-recursive pass, because :33-36 calls `tree.rglob('*.md')`.
  - - **ref:** tests/test_request_links.py:26
    - **claim:** `FENCED_BLOCK` — the fenced-content exemption the ESCALATIONS.md worked-example template relies on, and the regex shape the escalation parser reuses to strip fences before the document-level parse.
  - - **ref:** tests/test_request_links.py:40-64
    - **claim:** `_dead_links` — run during planning over all five current root documents; every one returned [], confirming AC 18's 'costs zero cleanup' as measured rather than assumed. It strips `file.py:123` suffixes at :57, so `Source:` citations with line numbers resolve.
  - - **ref:** tests/test_no_leaks.py:41-58
    - **claim:** `TEXT_SUFFIXES` includes '.md', so a new tracked root document is scanned by construction for drive-letter paths, home directories, and email addresses — the AC 22 concern, on a public repo.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369
    - **claim:** 'OpenAPI snapshot test | drop | Out — asserts nothing the health test doesn't; right moment is 1.8. Record the pointer in 1.8's request' — seed E-004, moment 1.8, verified verbatim.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:370
    - **claim:** 'OpenAPI→TypeScript codegen | drop | Out — machinery for one two-field object; the typed fetch wrapper is the seam it slots into at 1.8' — seed E-005, moment 1.8, verified verbatim.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:319
    - **claim:** 'It is what stops item 1.10's router from being a breaking change' — the SPA history fallback, a SHIPPED fold, not a stranded decision (finding F-02). This is almost certainly the line the scope's fold #2 conflated with IMPLEMENTATION_PLAN.md:859-861.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861
    - **claim:** CORRECTION: this is NOT a client-side router seam at moment 1.10. Verbatim: 'Out of scope but worth recording before item 2.1: .gitignore:63's blanket build/ will silently shadow the build/build-*.py builder pattern CLAUDE.md prescribes for Phase 2. Should become an intake item rather than being rediscovered at 2.1.' Seed E-006's moment is 2.1. Grep for 'router' in that file returns only :109, :247, :301, :469, :830 — all FastAPI APIRouter, none a parked decision.
  - - **ref:** requests/feature-requests/README.md:116
    - **claim:** The Index row for H1-escalation-queue, Stage cell currently `scoped`. This cell and every artifact Status header in the directory must move together.
  - - **ref:** .claude/skills/make-feature-request/SKILL.md:74-95
    - **claim:** Step 2 'Ground it in the repo', a bullet list at :79-91 ending with the project-rules-constraints bullet. The AC 14 read bullet appends after :91. The scope cites :74-92; the step actually runs to :95.
  - - **ref:** .claude/skills/commit/SKILL.md:109-144
    - **claim:** Step 4 'Roadmap status' — the diff-signal table at :118-122 (where 'the Deliverable column is fully satisfied' → DONE), the two rails at :133-138, and 'Stage ROADMAP.md by path if you edited it' at :143-144. The AC 15 drain bullet is a third rail after :138.
  - - **ref:** .claude/skills/diagnose-bug/SKILL.md:132-142
    - **claim:** The murky-cause Escalation block with its (a) why murky / (b) verdict / (c) follow-up structure. AC 16 adds one pointer line here; the evidence trail is not duplicated into the queue.
  - - **ref:** .claude/skills/diagnose-bug/SKILL.md:161-162
    - **claim:** 'Murky → the Escalation section is the deliverable; the bug waits for the follow-up.' The hand-off that today has nowhere to go — the dead end Decision 3 gives a destination.
  - - **ref:** docs/decisions/0010-panels-by-default.md:125-128
    - **claim:** 'Require the user to approve a stage plan on every item. Rejected: it makes the human a gate on the routine path, which is precisely the synchronous-interruption tax roadmap item H1 exists to remove.' An accepted ADR already assumes this mechanism exists.
  - - **ref:** docs/decisions/README.md:26-37
    - **claim:** The Rules section — 'Don't edit an accepted ADR to reflect a change of mind' and the contiguity rule. The immutability ethos the resolved-entries-stay-in-place rule borrows by ANALOGY. ADR 0003's append-only rule governs careers/**/events.jsonl only and is not engaged here.
  - - **ref:** ops/README.md:30-37
    - **claim:** 'This file is inert until it is re-applied' — the failure mode this feature's headline risk is named after, and the reason Decisions 1, 3, and 5 (the read seams) were promoted from gated to core.
  - - **ref:** CLAUDE.md:216
    - **claim:** 'Check the ADRs before proposing anything structural. Nine decisions are settled…' — MEASURED drift against :57 ('ten settled calls'), :90 ('Ten ADRs'), and :111 ('All ten are ADRs'). AC 20, one word.
  - - **ref:** CLAUDE.md:52-67
    - **claim:** The project-map fenced block, ending with 'var/ GITIGNORED — read-model, caches, scratch'. One line added inside the fence, so it adds no link-check surface. File measured at 221 lines today; AC 23 caps the increase at +1.
  - - **ref:** README.md:55-73
    - **claim:** The project-map fenced tree block, CLAUDE.md at :60. One row added in the same style.
  - - **ref:** requests/README.md:6-10
    - **claim:** The three-track table (feature-requests / bugfix-requests / calibration-findings). Fold #7's one-line pointer sits beside it, and requests/ is already in SCANNED_TREES so this link is checked.
  - - **ref:** DESIGN.md:115-126
    - **claim:** §4 'Open engineering questions' — four bullets with no ids and no named moment. The sharpest overlap the register map must draw a boundary against; §2 'Open mechanism work' is at DESIGN.md:50 and its subsections already carry phase/item moments.
  - - **ref:** pyproject.toml:80-84
    - **claim:** `[tool.mypy] strict = true`, `warn_unreachable = true`, `files = ["src", "tests"]` — strict typing applies to the new test module, so AC 21's 'fully annotated, zero new type: ignore' is enforced by the toolchain rather than by discipline.
  - - **ref:** .github/workflows/ci.yml:18-47
    - **claim:** The 'Lint, types, tests' job running ruff check, ruff format --check, mypy, and pytest. The new module joins it automatically — no new job, so ops/branch-protection.json and tests/test_ci_contexts.py stay untouched (Non-Goal 11).
  - - **ref:** .claude/skills/update-docs/SKILL.md:76-78
    - **claim:** 'The line budget. CLAUDE.md stays under 200 lines. Check it: (Get-Content CLAUDE.md | Measure-Object -Line).Lines' — the command scope risk 15 records as under-reporting, and a stated budget the file already exceeds at 221. Out of scope; use AC 23's (Get-Content …).Count instead.
  - - **ref:** tests/conftest.py:1-53
    - **claim:** Shared API fixtures only (client_factory, client, built_spa_dist) — nothing the escalation guard needs, and nothing this change perturbs. Confirms tests/test_escalations.py can be a standalone pure-text module.
- **open_questions:** - The moments for E-001, E-002, and E-003 are INFERRED, not measured — Phase 3, an unnamed item, and H2 respectively are this planner's reading of ROADMAP.md:217-222's prose plus the scope panel's. The guard proves only that the moment EXISTS. The user should sanity-check them at first read; changing one is a one-line edit with no migration.
  - AC 24's observable signal is not automatically satisfied. No seed names moment `1.3`, so 'picking up item 1.3, the entries naming 1.3 surface' has nothing to surface today. Scope risk 2 anticipated this. Does the user want an eighth seed parked against 1.3 (the obvious candidate: whether a superseding correction event carries the full replacement box score or a delta), or is the criterion judged on the mechanism rather than on 1.3 specifically? This is a user call, not a planner call — and adding a seed is cheap either way.
  - Does the tenth-open-question orphan get its ROADMAP.md:334 row left in place while E-007 is open, or does the row come out now? The plan leaves it — Non-Goal 13 says recorded, not answered, and scope Decision 4 keeps a GAME_DESIGN.md decision off the critical path of a binding Blocks: 1.3. Flagging it because a reader may expect the migration to tidy the roadmap the way AC 13 does for the three hypotheses; it deliberately does not.
  - Should the three seam substring tests live in tests/test_escalations.py (this plan's choice, keeping the feature's guards cohesive) or in tests/test_repo_structure.py beside the other document-substring assertions? Scope core item 5 binds only the PARSER guard to a new module, and fold #4 explicitly appends to test_repo_structure.py, so either placement is sanctioned. Purely a legibility call.
  - The exact substring each seam test asserts is a durability trade this plan does not fully pin. 'ESCALATIONS.md' is the safest anchor — it appears in none of the three skill files today — but it also passes if someone leaves the filename in a sentence that no longer instructs anyone to read the queue. A longer phrase catches that and breaks on a wording tweak. The plan recommends the filename; the implementer may tighten it if they can find a phrase worth the brittleness.

---

## Planner 2

- **planner:** sequencing
- **ok:** True
- **onboarding_files:** - - **path:** requests\feature-requests\H1-escalation-queue\PROJECT_SCOPE.md
    - **why:** The decided contract. 24 acceptance criteria, 7 Decisions, 12 core scope items, 16 risks. Consume it; do not re-open fit, goals, non-goals, or acceptance. Read the 'Affected Area & Pointers' block (:311-350) before touching anything — but see code_references for the three of its citations that need correcting.
  - - **path:** requests\feature-requests\H1-escalation-queue\FEATURE_REQUEST.md
    - **why:** Context only. Carries the user-settled constraints that scoping inherited: parking never blocks (:55), drained at item boundaries (:57), H1 blocks 1.3 (:100).
  - - **path:** ROADMAP.md
    - **why:** The parser's input AND the spec of record. H1 row at :197 names the three deliverables; 'Why H1 exists' at :200-204; the three seed hypotheses at :217-222 (the paragraph this feature reduces to a pointer). Also the four table shapes the parser must discriminate.
  - - **path:** tests\test_layering.py
    - **why:** The closest idiom, and the one to copy structurally. Docstring :1-17 (why a pure function proven against tmp_path — subagents have read-only git); pure guard :38-67; message builder :70-79; red/green tmp_path proofs :85-100; real assertion :106-109; failure-message test :112-123.
  - - **path:** tests\test_repo_structure.py
    - **why:** Module docstring idiom :1-9; module-level regex convention :21 and :30; test_core_documents_exist :243-246 (to extend, docstring to fix); the substring-assertion idiom :256-283; test_index_stage_cells_match_their_artifact_status_headers :286-319 (the invariant AC 9 mirrors — and a pre-flight landmine, see risks).
  - - **path:** tests\test_request_links.py
    - **why:** SCANNED_TREES :24 and _scanned_files :31-37 (to extend to root *.md); FENCED_BLOCK :26 (reuse this regex to keep the queue's worked-example template out of parse_entries); _dead_links :40-64; the _done/ skip :36.
  - - **path:** pyproject.toml
    - **why:** The gates the plan is measured against: ruff line-length 100 with DTZ/PTH/A/N selected (:53-77), mypy strict over BOTH src and tests (:80-84), pytest testpaths/addopts (:87-96). Strict mypy over tests/ is why every new helper and test needs a full annotation.
  - - **path:** .claude\skills\commit\SKILL.md
    - **why:** Step 4 (:109-144) is both the drain-hole edit site (Decision 5) and the mechanism that advances the H1 row. The status column is maintained by /commit against the diff — never hand-edit ROADMAP's Status cells.
  - - **path:** requests\feature-requests\README.md
    - **why:** The pipeline contract: status grammar (:106), the artifact status blockquote form (:104), the Index (:112-117), and what 'testable' means here (:63-79) — including the user-run carve-out AC 24 relies on.
- **architecture_notes:** SHAPE OF THE CHANGE. This is a docs-and-guards feature. Nothing under `src/` or `app/` is touched (scope non-goal 9), no endpoint, no DTO, no dataset, no ledger event. Verified by filesystem probe on 2026-08-15: `datasets/manifest.json`, `careers/`, `rulesets/`, and `lib/` do not exist, and `src/rpg_core/` contains only `__init__.py`. The resolve-datasets-by-name rule, ADR 0003's append-only rule, ADR 0004's ruleset immutability, and ADR 0008's cost-side-only rule are therefore genuinely N/A here — recorded so a cold implementer does not go looking for a seam to honor them at.

There is no `docs/data-sources.md` in this repo; the equivalent document is `docs/data-access.md`, and this feature reads no external source and spends no cloud money. The planning instruction "verify an unconfirmed source claim before building on it" still binds, but it binds on a different class of belief: the scope carries one UNCONFIRMED claim (risk 13 — that the drain cadence actually fires, because ADR 0010's entry condition exempts doc edits) and one INFERRED claim (risk 14 — the seed moments are the panels' judgment, not measurement). Phase 0 exists to discharge what can be discharged by measurement and to put the rest in front of the user before entries pin them.

TWO PIECES, ONE FILE EACH.

1. `ESCALATIONS.md` at the repo root — a human document with a machine-checkable shape. Its header is the load-bearing deliverable (scope Fit Verdict, "Fragmentation"): a register map drawing the boundary against the five existing parking mechanisms, at the point of use. Its body is `## Open` / `## Resolved`, each holding `### E-NNN — <title>` entries carrying six fields.

2. `tests/test_escalations.py` — a NEW module (scope core 5), not an append to `test_repo_structure.py`. Structurally it is `test_layering.py` with a different subject: pure module-level functions, proven RED and GREEN against `tmp_path` fakes, then applied once to the real files. That idiom is chosen for a specific reason stated in `tests/test_layering.py:11-17` — subagents have read-only git, so you cannot prove a guard red by breaking a tracked file and reverting.

THE PARSER, CONCRETELY. Two layers, because AC 2 and AC 3 test different things.

- `_parse_item_ids(text) -> set[str]`: take the first cell of every `|`-delimited row and keep it only when it fullmatches `\d+\.\d+|H\d+`. This is the F-27/F-06 correction: emptiness is NOT the discriminator, because the v1 scope tables' data rows lead with `**Creation**`-style bold text. MEASURED 2026-08-15 against the real `ROADMAP.md`: this rule returns exactly 38 ids — `0.1`-`0.6`, `1.1`-`1.11`, `H1`, `H2`, `2.1`-`2.8`, `3.1`-`3.5`, `4.1`-`4.6` — and the four non-item tables (`:23-35`, `:41-51`, `:75-84`, `:324-334`) contribute zero. AC 2's four known-good ids are all present and 38 clears the `>= 30` floor with eight rows of headroom.
- `parse_valid_moments(text) -> set[str]` = those ids, plus phase names parsed from `^###\s+(Phase \d+)\b` (five headers at `:144`, `:163`, `:224`, `:246`, `:261`), plus the literal `post-v1` (grounded in `ROADMAP.md:277`'s `### Post-v1` header and the `post-v1` cells at `:330` and `:334`). This is the folded-in widened vocabulary and it is what lets the serviceability-gate seed name `Phase 3`.

THE ONE NON-OBVIOUS COUPLING. The header carries a fenced worked-example entry that AC 11 requires to parse green under the *same* regexes as real entries. If `parse_entries` runs over the raw file, that example becomes a phantom eighth entry and collides with the uniqueness guard (AC 8) and the section-agreement guard (AC 9). Resolve it by stripping fenced blocks before parsing — reuse the `FENCED_BLOCK` regex already proven at `tests/test_request_links.py:26` — and expose a `_fenced_blocks(text) -> list[str]` helper that hands the template body to the AC 11 test on its own. Getting this wrong is the single most likely way the module ships subtly broken.

WHY THIS ORDER. The parser is the highest-uncertainty piece (risk 5: four table shapes, one regex, one file) and it is verifiable with no queue in existence, so it goes first and alone. The queue file goes second, because writing prose against a proven parser is cheap and writing a parser against finished prose is not. The link-checker extension goes third and NOT earlier — extending `test_request_links.py` to root documents before `ESCALATIONS.md` exists turns every pointer added in the same commit into a dead link and a red build. The three skill seams go last because they are the only edits that touch files outside this feature's blast radius, and because that commit is the one whose diff satisfies H1's Deliverable cell in full — which is when `/commit` Step 4 is entitled to flip the row to `DONE`.
- **phases:** - - **name:** Phase 0 — Pre-flight: verify the inherited beliefs, measure the baseline
    - **goal:** Discharge by measurement every belief a later phase would otherwise build on, and put the two that cannot be measured in front of the user before any entry pins them. Produces no tracked change of its own except one possible status-header sync.
    - **steps:** - Run the four gates and record the baseline: `uv run pytest -q`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy`. All four are expected green at HEAD.
      - PRE-FLIGHT LANDMINE. `tests/test_repo_structure.py:286-319` asserts the Index Stage cell equals the Status header of EVERY `*.md` in the item directory (`glob`, not `rglob`, so `reviews/` is exempt). Landing `IMPLEMENTATION_PLAN.md` at stage `plan` makes three files disagree. If that test is red, fix it by setting all of `requests/feature-requests/README.md`'s H1 Index cell (:116), `H1-escalation-queue/FEATURE_REQUEST.md:1`, and `H1-escalation-queue/PROJECT_SCOPE.md:1` to the same stage word. Precedent: all four `_done/1.1-app-shell/*.md` carry `implemented`. This is bookkeeping, not a scope change.
      - VERIFY the parser's input (risk 5). Confirm against the real file that the id discriminator returns 38 ids and that the four non-item tables contribute zero. One-liner: `$t=Get-Content ROADMAP.md; ($t | %{ if ($_ -match '^\|\s*([^|]+?)\s*\|') { $matches[1] } } | ? { $_ -match '^(\d+\.\d+|H\d+)$' }).Count`. Expect 38. If it is not 38, the roadmap changed under this plan and AC 2's floor of 30 must be re-checked before writing the test.
      - VERIFY the three recovered seeds by opening them (risk 14, and a correction the scope's pointer list needs). `_done/1.1-app-shell/PROJECT_SCOPE.md:369` = OpenAPI snapshot test, 'right moment is 1.8'. `:370` = OpenAPI→TypeScript codegen, 'the seam it slots into at 1.8'. `IMPLEMENTATION_PLAN.md:859-861` is NOT the client-side router seam the scope's Folded-in item 2 labels it — it reads 'Out of scope but worth recording before item 2.1: .gitignore:63's blanket build/ will silently shadow the build/build-*.py builder pattern CLAUDE.md prescribes for Phase 2.' Its moment is 2.1. Confirm `.gitignore:63` really is `build/` (it is). The router text is at `PROJECT_SCOPE.md:319`, which finding F-02 already ruled a shipped fold, not a stranded decision — so the count stays three strays; only the third one's subject and moment change.
      - VERIFY the `[OPEN-10]` orphan (folded-in item 3). `GAME_DESIGN.md` §8 starts at :290 and its last entry is `[OPEN-9]` at :335; `ROADMAP.md:334` cites `[OPEN-10]` with phase `post-v1, with aging`. Confirmed orphan; its moment is `post-v1`.
      - MEASURE the CLAUDE.md budget for AC 23: `(Get-Content CLAUDE.md).Count` — expect 221. Use this command, not `update-docs/SKILL.md:76-77`'s `Measure-Object -Line`, which under-reports (scope risk 15, out of scope to fix).
      - MEASURE AC 18's claim: there are exactly five root-level `*.md` files today (CLAUDE.md, DESIGN.md, GAME_DESIGN.md, README.md, ROADMAP.md). Import `_dead_links` from `tests/test_request_links.py` in a throwaway REPL and confirm it returns `[]` for each, so the extension in Phase 3 costs zero cleanup.
      - ASK THE USER about the two things measurement cannot settle. (a) The three ROADMAP hypotheses' moments are this panel's reading of prose, not measurement (risk 14). Proposed and grounded: serviceability gate → `Phase 3` (`ROADMAP.md:248` — Phase 3 proves 'the app is genuinely serviceable'); design/UX specialist → `Phase 3`; autonomous stage dispatcher → `H2` (`ROADMAP.md:198`, the write-capable-subagent row). Every later entry inherits whatever is chosen. (b) Scope risk 2 says the seven seeds make the 1.3 observable signal true on day one — but none of the seven names `1.3` (they name Phase 3, Phase 3, H2, 1.8, 1.8, 2.1, post-v1). Surface this now; do not invent a 1.3 entry to make the signal fire.
    - **acceptance:** - All four gates green at HEAD, or red only on `test_index_stage_cells_match_their_artifact_status_headers` and green after the status-header sync.
      - The id-discriminator one-liner returns 38, and spot-checking it returns nothing for lines in `ROADMAP.md:23-35`, `:41-51`, `:75-84`, `:324-334`.
      - The three stray sources have been opened and the third one's moment recorded as `2.1`, with its subject the `.gitignore` blanket-`build/` shadowing.
      - `(Get-Content CLAUDE.md).Count` == 221, and `_dead_links()` returns `[]` for all five root documents.
      - The user has confirmed or corrected the three inferred moments, and has disposed the 1.3-signal gap (accept, or park one genuine 1.3 entry during the build).
    - **commit_note:** Usually nothing to commit — this phase reads and measures. If the status-header sync was needed, carry those three one-line edits into Phase 1's `/commit` rather than committing them alone. Never `git commit` ad hoc; `/commit` is the only sanctioned path.
  - - **name:** Phase 1 — The parser, proven against the roadmap and against tmp_path
    - **goal:** Land `tests/test_escalations.py` carrying the pure parser and every assertion that does not require `ESCALATIONS.md` to exist. This isolates the feature's highest-uncertainty piece and proves it before a byte of prose is written.
    - **steps:** - Create `tests/test_escalations.py`. Module docstring in the `tests/test_layering.py:1-17` / `tests/test_repo_structure.py:1-9` idiom (AC 1): why the guard exists (an entry naming a moment that does not exist is an entry that will never surface), and why the parser is a pure function proven both ways against `tmp_path` — cite the read-only-git reason `test_layering.py:11-17` states, because it is the same reason.
      - Module-level constants in the `tests/test_repo_structure.py:21` / `:30` convention: `REPO_ROOT = Path(__file__).resolve().parents[1]`, `ROADMAP = REPO_ROOT / "ROADMAP.md"`, `QUEUE = REPO_ROOT / "ESCALATIONS.md"`, `ITEM_ID = re.compile(r"\d+\.\d+|H\d+")`, `TABLE_ROW = re.compile(r"^\|\s*([^|]+?)\s*\|", re.MULTILINE)`, `PHASE_HEADING = re.compile(r"^###\s+(Phase \d+)\b", re.MULTILINE)`, `EXTRA_MOMENTS = frozenset({"post-v1"})` with a comment citing `ROADMAP.md:277`.
      - Write `_parse_item_ids(text: str) -> set[str]` — first cell of every table row, kept only on `ITEM_ID.fullmatch(cell)`. Comment the F-27/F-06 correction inline: emptiness is not the discriminator, the v1 scope tables lead with bold text.
      - Write `parse_valid_moments(text: str) -> set[str]` returning `_parse_item_ids(text) | phase names | EXTRA_MOMENTS`.
      - AC 2 — positive assertion: over the real `ROADMAP.md`, assert the set is non-empty, is a superset of `{"0.1", "1.3", "H2", "4.6"}`, has `len >= 30`, and that every parsed *id* fullmatches `ITEM_ID`. Do NOT assert an exact 38-id set — finding F-28 showed that fires in unrelated PRs.
      - AC 3 — negative assertion: feed `_parse_item_ids` inline fixture strings that reproduce each non-item shape (a bold-leading v1-scope row, a plain-text out-of-scope row, a `**[OPEN-N]**` row) and assert `set()` each time. Comment each fixture with the real line range it mirrors. Do NOT slice the real file by line number — hard-coded line offsets are the same brittleness F-28 rejected.
      - Write `parse_entries(text: str) -> list[Entry]` with `Entry` a `NamedTuple` carrying `entry_id`, `title`, `bears_on`, `parked`, `assumed`, `source`, `status`, `section`. Strip fenced blocks FIRST using the `FENCED_BLOCK` regex copied from `tests/test_request_links.py:26`, so the header's worked example is not parsed as a real entry. Expose `_fenced_blocks(text: str) -> list[str]` for Phase 2's AC 11 test.
      - Write `unknown_moments(entries: list[Entry], valid: set[str]) -> list[tuple[str, str]]` and `violation_message(violations: list[tuple[str, str]]) -> str`. The message names `ESCALATIONS.md`, the offending entry id, the offending moment, and states the rule (AC 7) — model it on `tests/test_layering.py:70-79`.
      - AC 5 — red/green against `tmp_path`, in the `tests/test_layering.py:85-100` idiom. BOTH fixtures fabricate the roadmap as well as the queue, so the green case is hermetic (finding F-07). Red: a queue naming `9.9` returns exactly `[("E-001", "9.9")]`. Green: a queue naming `1.3`, `H2`, `Phase 3`, and `post-v1` returns `[]`.
      - AC 7 — a failure-message test in the `test_the_failure_message_cites_the_architecture_record` idiom (`tests/test_layering.py:112-123`): build the message from a fabricated violation and assert it contains the file name, the entry id, the moment, and the rule sentence.
      - Do NOT add any test that reads `QUEUE` in this phase — the file does not exist yet. The constant is declared and unused-by-tests on purpose; if ruff flags it, it is because the constant is genuinely unused, so declare it in Phase 2 instead.
    - **acceptance:** - `uv run pytest tests/test_escalations.py -q` is green.
      - `uv run pytest -q` is green overall — no other module regressed.
      - `uv run ruff check`, `uv run ruff format --check`, and `uv run mypy` all exit 0, with zero new `# type: ignore` (AC 21). mypy is strict over `tests` per `pyproject.toml:80-84`, so every function and every test carries a full annotation including `-> None`.
      - The red proof genuinely bites: temporarily edit the RED fixture string inside the test to name `1.3` instead of `9.9` and watch the test fail, then restore it. Fixture strings are inside the test module, so this touches no tracked document.
      - `Select-String tests/test_escalations.py -Pattern 'QUEUE'` shows the queue is not read by any test yet — the phase is honestly self-contained.
      - Reversible: the only file added is one test module. Deleting it returns the tree to HEAD.
    - **commit_note:** `/commit`. Message frames it as the guard, not the feature: the parser and its red/green proof, no queue yet. `/commit` Step 4 leaves `ROADMAP.md`'s H1 row at `IN-PROGRESS` — the Deliverable cell names three things and only part of one exists. Do not hand-edit the Status cell. `/commit` pushes the branch; the PR stays the user's.
  - - **name:** Phase 2 — The queue: header, pinned format, seven seeds, and the queue-side guards
    - **goal:** Land `ESCALATIONS.md` with a header that draws the register boundary, a format pinned by a worked example that the guard itself validates, and seven real entries — then turn on every assertion that reads it.
    - **steps:** - Create `ESCALATIONS.md` at the repo ROOT (scope core 1 — discoverability is the mechanism; the three hypotheses failed precisely by being where nobody looks).
      - Write the boundary header for the worker about to add an entry. It must carry: what the queue is for; a REGISTER MAP table disambiguating it from `GAME_DESIGN.md` §8 `[OPEN-N]` (:290), `DESIGN.md` §2 'Open mechanism work' (:50), `DESIGN.md` §4 'Open engineering questions' (:115), ADR 0010's Stage plan, and `/diagnose-bug`'s Escalation; the ADR-0011 trigger verbatim from Decision 6 ('the first time someone proposes absorbing [OPEN-N] or DESIGN.md §4, that is the second litigation and ADR 0011 gets written then'); one line recording that a `Blocks` cell stops work while a queue entry never does; the F-33 carve-out (parking never blocks a DECISION, but a malformed entry fails `tests/test_escalations.py` exactly as malformed markdown fails any other guard); and the name-collision one-liner ('escalation' now means two things — one non-blocking and moment-scoped, one blocking and bug-scoped).
      - Pin the format in a fenced worked example: `### E-NNN — <title>` plus six required fields — `Bears on:` (a moment: item id, phase name, or `post-v1`), `Parked:` (a date), `Assumed:` (what was actually done, or an explicit 'none — not hit yet'), `Source:` (where it surfaced), `Status:` (closed vocabulary — `open` | `resolved`). The example must use a REAL id and a REAL moment so it parses green under the production regexes (AC 11). Six fields, not five — finding F-26.
      - Add `## Open` and `## Resolved` sections. `Status:` is the single source of truth (AC 9, finding F-08); the section an entry sits in must agree with it, the same invariant `test_index_stage_cells_match_their_artifact_status_headers` (`tests/test_repo_structure.py:286-319`) enforces between the Index and artifact headers.
      - Write the seven seed entries under `## Open`. Three ROADMAP hypotheses (serviceability gate for the web UI; design/UX specialist; autonomous stage dispatcher) with the moments the user confirmed in Phase 0, `Source: ROADMAP.md` 'Parked, not scheduled'. Three `_done/1.1-app-shell/` strays: OpenAPI snapshot test → moment `1.8`, `Source: requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369`; OpenAPI→TypeScript codegen → moment `1.8`, `:370`; the `.gitignore:63` blanket-`build/` shadowing of the Phase-2 `build/build-*.py` pattern → moment `2.1`, `Source: .../IMPLEMENTATION_PLAN.md:859-861`. And `[OPEN-10]` (does the offseason training payout scale with age) → moment `post-v1`, `Source: ROADMAP.md` open-questions table.
      - Label the seed epistemics honestly. The recovered strays' moments are INFERRED (the 1.1 panel's judgment as of 2026-08-14) and the hypotheses' moments are a reading of prose. RECORD, do not decide — non-goal 13 binds especially on `[OPEN-10]`, which touches ADR 0005's carve-out.
      - Add the queue-side tests to `tests/test_escalations.py`: AC 4 non-vacuity (assert the entry regex matched at least one entry, failing with exactly 'no entries parsed — the entry format drifted from the regex'); AC 6 the real assertion (`unknown_moments` over the real queue and the real roadmap is empty, failing with `violation_message`); AC 8 field and vocabulary guards (all six fields on every entry; ids match `^E-\d{3}$` and are unique); AC 9 section-agrees-with-status; AC 10 register boundary (no entry title contains `[OPEN-`, and the header names all four other registers); AC 11 the worked example parses green through `_fenced_blocks` under the same regexes and field guards; AC 12 seven seed substrings, in the substring idiom of `test_no_document_still_claims_there_is_no_application` (`tests/test_repo_structure.py:260-273`).
      - LEAK SWEEP BY HAND before handing off. `tests/test_no_leaks.py:61-74` builds its file list from `git ls-files`, which lists the INDEX — so an untracked new `ESCALATIONS.md` is invisible to it locally and only gets scanned once `/commit` stages it (and on CI). Run `Select-String ESCALATIONS.md -Pattern '\b[A-Za-z]:[\\/]'` and an email pattern manually. The repo is public and entries record real assumptions (risk 10).
    - **acceptance:** - `uv run pytest tests/test_escalations.py -q` is green, including all seven queue-side tests.
      - The non-vacuity guard demonstrably bites: copy the queue into `tmp_path`, mangle a field name in the COPY, and confirm `parse_entries` over it returns `[]` and the AC 4 assertion would fire. Never mutate the tracked file to prove this — read-only git.
      - `len(parse_entries(QUEUE.read_text()))` == 7 and every entry's `Bears on:` value is in `parse_valid_moments(ROADMAP.read_text())`.
      - `_fenced_blocks` yields the worked-example body and it parses to exactly one well-formed entry — the template a worker copies cannot produce a malformed entry (AC 11, the mechanical content of goal 1).
      - Manual leak sweep on `ESCALATIONS.md` returns nothing.
      - `uv run pytest -q`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all green.
      - Reversible: two files (one new document, one grown test module); no other tracked file touched.
    - **commit_note:** `/commit`. `ESCALATIONS.md` is a new tracked root document, so `/commit`'s doc-drift step will notice — that is expected and Phase 3 is where the maps catch up. `ROADMAP.md`'s H1 row stays `IN-PROGRESS`; the paragraph reduction and the read seams are still missing.
  - - **name:** Phase 3 — De-duplicate the source, make it discoverable, fold in the measured doc fixes
    - **goal:** Stop the migration leaving two sources of truth, put the queue on both project maps, and land the four cheap folds that were measured rather than guessed. This is the phase that must follow Phase 2, not precede it.
    - **steps:** - AC 13 — reduce `ROADMAP.md:217-222` to a pointer at `ESCALATIONS.md`. PRESERVE the paragraph's substantive observation, which is not a hypothesis and must not be dropped: that 'serviceable' is already a v1 constraint in both directions (see §v1) and is nowhere defined or tested. The three hypotheses must no longer be enumerated inline.
      - AC 19/20/23 — `CLAUDE.md`: add EXACTLY ONE line inside the project-map fenced block (which starts at :51), naming `ESCALATIONS.md`. Inside the fence, so it adds no link-check surface. Separately fix :216's 'Nine decisions are settled' to 'Ten' — a word, not a line (`CLAUDE.md:57` and `docs/decisions/README.md:52` already say ten; ADR 0010 is accepted). No other CLAUDE.md addition — the budget is +1 line and nothing more.
      - AC 19 — `README.md`: add one line inside the fenced tree block at :55-73, in the `├── NAME   description` style of :57-60.
      - Folded-in item 7 — `requests/README.md`: one line beside the three-track table (:6-10), pointing at `../ESCALATIONS.md`. This is the only pointer location the link checker covers today, so it is the one that cannot silently rot.
      - AC 17 — `tests/test_repo_structure.py`: extend `test_core_documents_exist` (:243-246) to include `ESCALATIONS.md`, and fix its docstring in the same edit — it says 'The three documents that carry the design' over a five-name tuple, and is about to be a six-name tuple.
      - AC 19 — add a project-map substring test in the `test_claude_md_project_map_lists_both_new_directories` idiom (`tests/test_repo_structure.py:276-283`), asserting `ESCALATIONS.md` appears in both `CLAUDE.md` and `README.md`, with a failure message that says why (an agent onboarding from the map would not know the queue exists).
      - AC 18 — `tests/test_request_links.py`: extend the scan to root-level `*.md`. IMPLEMENT IT AS `REPO_ROOT.glob("*.md")` added to the file list in `_scanned_files` (:31-37), NOT by appending `REPO_ROOT` to `SCANNED_TREES` (:24). The loop calls `rglob("*.md")`, so a root entry would walk `.venv/`, `app/node_modules/`, `out/`, and `var/` — hundreds of vendored documents with dead links. Dedupe the result with `sorted(set(files))`.
      - Re-verify the CLAUDE.md budget after the edits: `(Get-Content CLAUDE.md).Count` must be at most 222.
    - **acceptance:** - `uv run pytest tests/test_repo_structure.py tests/test_request_links.py -q` is green.
      - `_dead_links()` returns `[]` for all SIX root documents, `ESCALATIONS.md` included — the new pointers in `requests/README.md` and `ROADMAP.md` resolve.
      - `_scanned_files()` returns a list whose length grew by exactly six and contains no path under `.venv`, `node_modules`, `out`, or `var`. Assert this by eye once — a scan that silently walks the virtualenv passes today and times out later.
      - `Select-String ROADMAP.md -Pattern 'design/UX specialist'` returns nothing — the hypotheses are no longer enumerated inline (AC 13) — while `Select-String ROADMAP.md -Pattern 'serviceable'` still returns the preserved observation.
      - `(Get-Content CLAUDE.md).Count` <= 222 (AC 23, baseline 221 measured in Phase 0).
      - `uv run pytest -q`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all green.
      - ORDERING CHECK, state it in the commit: this phase is only safe after Phase 2. Extending the link checker to root documents while `ESCALATIONS.md` did not exist would make every pointer added here a dead link and the build red.
    - **commit_note:** `/commit`. Touches `ROADMAP.md`, so expect `/commit` Step 4 to look hard at the H1 row — it should still read `IN-PROGRESS`, because the read seams (Decisions 1, 3, 5, promoted to core scope items 10-12) have not landed. If `/commit` proposes `DONE` here, say no and cite the Deliverable cell.
  - - **name:** Phase 4 — The three read seams: the answer to the inertness risk
    - **goal:** Give the queue seams that actually read it, so it is not the 'inert until it is re-applied' failure `ops/README.md:30-37` documents for `branch-protection.json`. This is the commit whose diff satisfies H1's Deliverable in full.
    - **steps:** - Decision 1 / AC 14 — `.claude/skills/make-feature-request/SKILL.md` Step 2 (:74-92): add ONE bullet telling intake to surface `ESCALATIONS.md` entries whose `Bears on:` names this item's moment. Step 2 is already the ground-it-in-the-repo checklist (:79-91), so this adds a read to a checklist and changes no decision logic. Keep it one bullet — the request's 'no skill behavior changes' non-goal is only brushed, not broken.
      - Decision 5 / AC 15 — `.claude/skills/commit/SKILL.md` Step 4: add a third rail beside the two at :133-138 ('Never mark ahead' / 'Never mark down silently') — when flipping a roadmap row to `DONE`, surface any open queue entry naming that moment. This closes finding F-32's drain hole: an open entry naming 1.3 goes permanently invisible the moment 1.3 closes. Step 4 already parses roadmap rows and flips statuses (:116-131), so it is the one thing running at exactly that moment. Honor the skill's keep-it-lightweight rule — one bullet, not a procedure.
      - Decision 3 / AC 16 — `.claude/skills/diagnose-bug/SKILL.md`: add ONE line to the Escalation template (:136-142) telling the RCA to also park a queue entry naming the moment the bug bears on. The RCA keeps its Escalation section — the evidence trail is not duplicated; two mechanisms, one index. Add the name-disambiguation line near the murky exit (:161-162). Both edits sit inside or beside a fenced block, which `tests/test_request_links.py:26` exempts, so they add no link surface.
      - Add three substring tests to `tests/test_escalations.py`, one per seam, each asserting the distinctive phrase is present in the named SKILL.md and failing with a message that says what the bullet is for and why deleting it re-creates the inert-config failure. Choose substrings that are specific enough to break if the bullet is rewritten to mean something else, and short enough not to break on reflowing.
      - Prove each seam test bites without touching a tracked file: copy the skill file to `tmp_path`, delete the bullet in the COPY, and confirm the assertion helper reports it. Factor the check as a small pure function taking the text, not the path, so this is possible — the same reason `tests/test_layering.py:38` takes a `root` argument.
    - **acceptance:** - `uv run pytest tests/test_escalations.py -q` is green, including the three seam tests.
      - Each seam test fails when its bullet is removed from a `tmp_path` copy — proven, not asserted. No tracked skill file is ever edited-and-reverted (read-only git).
      - `uv run pytest -q`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all exit 0.
      - `uv run pytest tests/test_request_links.py -q` still green — the `/diagnose-bug` edit lives inside the fenced template and adds no link surface.
      - The three edits total roughly five lines across three skill files. If a seam grew into a procedure, it overshot Decision 2, which explicitly deferred teaching any skill to WRITE entries to its own request.
    - **commit_note:** `/commit`. This is the diff that satisfies H1's Deliverable cell (`ROADMAP.md:197`) in full: the queue, the format, and the structural test all exist and are wired. `/commit` Step 4 flips H1 `IN-PROGRESS` → `DONE` against the diff — never hand-edited, per `ROADMAP.md:139-142`. The Phase 1 header aggregate stays `IN-PROGRESS` (items 1.2-1.11 are untouched). `/commit` pushes the branch; opening and merging the PR stays the user's.
  - - **name:** Phase 5 — Acceptance hand-off (USER-RUN) and the honest gaps
    - **goal:** Run the whole gate once more, then hand the two things a panel cannot claim to the user, with the one place the scope's own reasoning does not hold surfaced rather than smoothed over.
    - **steps:** - Full local gate one final time on the assembled branch: `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy`. AC 22 in particular means the whole suite, not the new module — `tests/test_no_leaks.py` now scans a tracked `ESCALATIONS.md` (it is in the index after Phase 2's commit) and `tests/test_request_links.py` now scans six root documents.
      - AC 24 is USER-RUN and NOT panel-claimable, per `requests/feature-requests/README.md:76-79`. Hand the user three checks: picking up item 1.3, do the entries naming 1.3 surface without the user being asked anything; can the user answer an entry by editing `ESCALATIONS.md` alone with no build running; does the parked context read as useful rather than as noise.
      - SURFACE THE GAP HONESTLY. Scope risk 2 says 'The seven seeds make this true on day one' for the 1.3 observable signal — but none of the seven seeds names `1.3`. They name `Phase 3`, `Phase 3`, `H2`, `1.8`, `1.8`, `2.1`, and `post-v1`. So AC 24's first check has nothing to surface unless something is parked against 1.3 between now and then. Present this as a decision, not a defect: accept that the 1.3 signal is carried by the `/make-feature-request` seam rather than by a pre-seeded entry, or park one genuine 1.3 question during the build (item 1.3 is correction-by-append under ADR 0003 — real questions exist there). Do not fabricate an entry to make a criterion green.
      - Record the two epistemic labels that survive into the shipped artifact: risk 13 UNCONFIRMED (the drain cadence assumes every item boundary passes through `/make-feature-request`, which ADR 0010's entry condition does not guarantee for doc edits) and risk 14 INFERRED (the seed moments). Both belong in the implementation report, not smoothed away.
      - Hand off to `/commit` for anything still uncommitted, then stop. The PR — opening it and merging it — is the user's.
    - **acceptance:** - All four gates green on the assembled branch.
      - Every one of AC 1-23 has a named command or a named test function that proves it; AC 24 is explicitly marked user-run and left unclaimed.
      - The 1.3-signal gap has been surfaced to the user and disposed one way or the other, with the disposition recorded.
      - `ROADMAP.md`'s H1 row reads `DONE` and `Blocks: 1.3` is discharged — item 1.3 may start.
      - No file under `src/` or `app/` appears anywhere in `git diff --stat main...HEAD` (scope non-goal 9), and `ops/branch-protection.json` and `tests/test_ci_contexts.py` are untouched (non-goal 11 — the new tests join the existing 'Lint, types, tests' job).
    - **commit_note:** `/commit` for any remainder. Nothing here merges: agents never push `main`, never force-push, never amend. `/commit` pushes the feature branch and the PR stays the user's — and `enforce_admins: true` means nobody merges past a red check anyway.
- **testing:** EVERY GATE IS LOCAL AND FREE. This feature adds no CI job (scope non-goal 11) — the new module joins the existing `Lint, types, tests` job in `.github/workflows/ci.yml:18-47`, so `ops/branch-protection.json` and `tests/test_ci_contexts.py` stay untouched. Nothing here spends cloud money and nothing hits a network, so no `-m network` selector is involved.

THE FOUR COMMANDS, run at the end of every phase, matching CI exactly (`.github/workflows/ci.yml:36-47`):
  uv run ruff check
  uv run ruff format --check
  uv run mypy
  uv run pytest

PER-PHASE SELECTORS:
  Phase 0  — `uv run pytest -q` (baseline only; expect green, or red solely on `tests/test_repo_structure.py::test_index_stage_cells_match_their_artifact_status_headers`).
  Phase 1  — `uv run pytest tests/test_escalations.py -q`, then the full suite. Nothing else can regress: the only file added is a test module.
  Phase 2  — `uv run pytest tests/test_escalations.py -q` plus `uv run pytest tests/test_no_leaks.py -q`. Note the leak guard is blind to the still-untracked queue file until `/commit` stages it (`tests/test_no_leaks.py:61-74` lists `git ls-files`), so a manual `Select-String` sweep for drive-letter paths and emails is part of the phase, not optional.
  Phase 3  — `uv run pytest tests/test_repo_structure.py tests/test_request_links.py -q`, then the full suite. This is the phase most likely to go red elsewhere, because it edits four documents and widens a scanner.
  Phase 4  — `uv run pytest tests/test_escalations.py -q` and `uv run pytest tests/test_request_links.py -q` (the `/diagnose-bug` edit sits inside a fenced block that `tests/test_request_links.py:26` exempts; confirm rather than assume).
  Phase 5  — `uv run pytest` whole-suite, plus the user-run checks.

HOW THE GUARD IS PROVEN RATHER THAN ASSERTED. Three distinct anti-vacuity mechanisms, because a structural test over a parsed list is trivially vacuous when the format drifts:
  1. AC 4 non-vacuity — `parse_entries` returning `[]` is an explicit FAILURE with the message "no entries parsed — the entry format drifted from the regex", not a silently-passing `assert not []`.
  2. AC 5 red/green — the parser is proven to REPORT a bad moment and to stay silent on four good ones, against fabricated `tmp_path` queue AND roadmap, so the green case is hermetic (finding F-07). This is `tests/test_layering.py:85-100` transplanted.
  3. AC 3 negative — the id discriminator is proven to reject the three non-item table shapes and the open-questions table, using inline fixtures that mirror the real rows rather than line-number slices of the real file.

REGRESSION SAFETY, BY BLAST RADIUS. The only pre-existing test modules this feature edits are `tests/test_repo_structure.py` (one function extended, one docstring corrected, one function added) and `tests/test_request_links.py` (one file-list widened). Both edits are additive to their assertions and both are proven by running the module directly. Everything else in the diff is a new file or a document edit, and the document edits are each covered by a substring test landing in the same phase. The four documents edited in Phase 3 are all inside fenced blocks except `requests/README.md` and `ROADMAP.md`'s pointer, and both of those are covered by `tests/test_request_links.py` from that phase forward.

THE ONE MEASUREMENT THAT IS NOT A PYTEST. AC 23's line budget is a shell measurement, `(Get-Content CLAUDE.md).Count`, baseline 221 measured 2026-08-15. Do not use `update-docs/SKILL.md:76-77`'s `Measure-Object -Line` form — it under-reports by not counting blank lines, which is scope risk 15 and out of scope to fix here.

WHAT THE TESTS DELIBERATELY DO NOT COVER. Nothing programmatic reads the queue at runtime (non-goal 10) — no API route, no importable runtime parser, no UI. The three read seams are prose bullets in skill files, proven by substring assertions, and a substring assertion proves the bullet is present, not that an agent obeyed it. That residue is scope risk 1, mitigated and not eliminated, and it is honest to say so in the implementation report rather than to claim the seams are enforced.
- **risks:** - THE THIRD RECOVERED SEED IS MIS-LABELLED IN THE SCOPE. `PROJECT_SCOPE.md`'s Folded-in item 2 and its Affected Area block both call `_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861` 'the client-side router seam (moment 1.10)'. Read, those lines say: 'Out of scope but worth recording before item 2.1: .gitignore:63's blanket build/ will silently shadow the build/build-*.py builder pattern CLAUDE.md prescribes for Phase 2.' The subject is the gitignore/builder collision and the moment is 2.1. The router text is at `PROJECT_SCOPE.md:319`, which finding F-02 already ruled a shipped fold rather than a stranded decision. Record the seed as it actually reads; the count of three strays (AC 12's 'seven') is unaffected.
  - AC 18 READ LITERALLY IS A TRAP. 'SCANNED_TREES extended to root-level *.md' invites appending `REPO_ROOT` to the tuple at `tests/test_request_links.py:24` — but `_scanned_files` (:31-37) calls `rglob("*.md")` on each entry, so a root entry walks `.venv/`, `app/node_modules/`, `out/`, and `var/`, pulling in hundreds of vendored documents with dead links. Implement it as `REPO_ROOT.glob("*.md")` (non-recursive) added to the file list, deduped with `sorted(set(...))`.
  - THE WORKED EXAMPLE COLLIDES WITH THE UNIQUENESS GUARD. AC 11 requires the header's fenced template to parse green under the SAME regexes as real entries; AC 8 requires entry ids to be unique and AC 9 requires the section to agree with `Status:`. If `parse_entries` runs over the raw file, the template is a phantom entry that trips both. Strip fenced blocks before parsing (reuse `tests/test_request_links.py:26`'s `FENCED_BLOCK`) and hand the template body to the AC 11 test through a separate `_fenced_blocks` helper.
  - THE LEAK GUARD IS BLIND TO THE NEW FILE UNTIL IT IS STAGED. `tests/test_no_leaks.py:61-74` builds its scan list from `git ls-files`, i.e. the index. A freshly written, untracked `ESCALATIONS.md` is not scanned locally — the guard only bites once `/commit` stages it and on CI. The repo is public and entries record real assumptions taken when nobody was sure (scope risk 10), so sweep the file by hand before handing off. Do not run `git add` to work around this: agents commit only through `/commit`.
  - PRE-FLIGHT: THE INDEX/STATUS INVARIANT WILL BE RED THE MOMENT THE PLAN LANDS. `tests/test_repo_structure.py:286-319` asserts the Index Stage cell equals the Status header of EVERY `*.md` in the item directory. Adding `IMPLEMENTATION_PLAN.md` at stage `plan` beside a `FEATURE_REQUEST.md` and `PROJECT_SCOPE.md` still reading `scoped` makes three files disagree. Precedent: all four `_done/1.1-app-shell/*.md` carry the same word. Sync all three plus the Index row at `requests/feature-requests/README.md:116` before diagnosing a phantom bug.
  - THE 1.3 OBSERVABLE SIGNAL IS NOT SEEDED. Scope risk 2 asserts 'The seven seeds make this true on day one', but none of the seven names `1.3` — they name Phase 3, Phase 3, H2, 1.8, 1.8, 2.1, and post-v1. AC 24's first user-run check therefore has nothing to surface. Surface this to the user as a decision (accept the seam as the proof, or park one genuine 1.3 question during the build); do not fabricate an entry to make a criterion green.
  - PHASE ORDER IS LOAD-BEARING BETWEEN 2 AND 3. Extending the link checker to root documents before `ESCALATIONS.md` exists turns the new pointers in `requests/README.md` and `ROADMAP.md` into dead links and the build goes red in the same commit that adds them. Create the file first; widen the scanner second.
  - THE CLAUDE.md BUDGET IS +1 LINE, TOTAL. AC 23 allows exactly one added line versus a measured baseline of 221. That budget is entirely consumed by the project-map entry. Any second addition — a rules bullet, a 'how to help' line about parking — breaks the criterion. The 'Nine'→'Ten' fix at `CLAUDE.md:216` is a word substitution and costs zero lines.
  - ROADMAP RENUMBERING SILENTLY ORPHANS ENTRIES (scope risk 6, with precedent — ADR 0010 just deleted two columns from every table). Item ids are hand-edited cells and the guard checks existence, not stability. Unmitigated in v1; the guard catches the orphan at CI rather than preventing it.
  - ENTRY-ID COLLISIONS ACROSS BRANCHES (finding F-38). Sequential `E-NNN` in one file merges cleanly into duplicates. AC 8's uniqueness guard catches it at CI rather than preventing it. Acceptable while entries are few — do not build a mitigation this item did not scope.
  - UNCONFIRMED — the drain cadence. 'Drained at item boundaries' assumes every item boundary passes through `/make-feature-request`, which ADR 0010's entry condition does NOT guarantee for doc edits (`requests/README.md:68-70` routes typo fixes, dependency bumps, and doc edits around the pipeline entirely). The whole cadence rests on this. Carry the `unconfirmed` label into the implementation report rather than quietly upgrading it.
  - INFERRED — the seed moments. The recovered strays carry moments that were the 1.1 panel's judgment as of 2026-08-14, and the three hypotheses' moments are the scoping panel's reading of prose. Phase 0 puts them to the user precisely because every later entry inherits the format and the precedent they set.
  - THE PARSER'S POSITIVE ASSERTION MUST NOT BE AN EXACT SET. Finding F-28: an exact 38-id assertion fires in every unrelated PR that adds a roadmap row. `len >= 30` with four named known-good ids is the deliberate looseness, and 38 measured today gives eight rows of headroom.
  - FRAGMENTATION IS MITIGATED ONLY BY PROSE. This is the sixth place a question can be parked, and `DESIGN.md` §4 (:115, 'Open engineering questions') is the sharpest overlap — bullets with no ids and no named moment, literally what the queue is minus the discipline. Nothing mechanical can distinguish a build-time decision from an engineering question. The header's register map is the whole mitigation, which is why it is core scope and not an enhancement.
  - SCOPE CREEP HAS A BINDING COST. `Blocks: 1.3` is real (`ROADMAP.md:197`), and 1.3 is the append-only correction model ADR 0003 treats as load-bearing. Decision 2 already deferred teaching skills to WRITE entries to its own request; if a seam in Phase 4 starts growing into a procedure, it has overshot.
- **files_to_touch:** - - **path:** tests\test_escalations.py
    - **change:** NEW (Phase 1, grown in Phases 2 and 4). Module docstring in the `tests/test_layering.py:1-17` idiom. Pure functions: `_parse_item_ids`, `parse_valid_moments`, `parse_entries`, `_fenced_blocks`, `unknown_moments`, `violation_message`. Tests: roadmap positive/negative (AC 2, 3), tmp_path red/green (AC 5), failure message (AC 7), non-vacuity (AC 4), the real assertion (AC 6), field/vocabulary/uniqueness (AC 8), section-agrees-with-status (AC 9), register boundary (AC 10), worked example (AC 11), seven seed substrings (AC 12), three skill-seam substrings (AC 14, 15, 16). Fully annotated — mypy is strict over `tests` per `pyproject.toml:84`.
  - - **path:** ESCALATIONS.md
    - **change:** NEW at repo root (Phase 2). Boundary header with the register map, the ADR-0011 trigger, the Blocks-vs-entry line, the F-33 carve-out, the name-collision one-liner; a fenced worked-example entry that parses green; `## Open` and `## Resolved` sections; seven seed entries each carrying the six required fields.
  - - **path:** ROADMAP.md
    - **change:** Phase 3: reduce `:217-222` ('Parked, not scheduled.') to a pointer at `ESCALATIONS.md`, preserving the 'serviceable is a v1 constraint in both directions and is nowhere defined or tested' observation. The H1 row's Status cell at `:197` is advanced by `/commit` against the diff in Phase 4 — never hand-edited (`ROADMAP.md:139-142`).
  - - **path:** CLAUDE.md
    - **change:** Phase 3: exactly ONE added line inside the project-map fenced block (opens at `:51`) naming `ESCALATIONS.md`; and `:216`'s 'Nine decisions are settled' → 'Ten' (a word, zero lines). Budget: `(Get-Content CLAUDE.md).Count` must not exceed 222.
  - - **path:** README.md
    - **change:** Phase 3: one line inside the fenced project-map tree block at `:55-73`, in the `├── NAME   description` style of `:57-60`.
  - - **path:** requests\README.md
    - **change:** Phase 3: one line beside the three-track table at `:6-10` pointing at `../ESCALATIONS.md`. This is the only pointer location covered by the link checker today, so it is the one that cannot silently rot.
  - - **path:** tests\test_repo_structure.py
    - **change:** Phase 3: extend `test_core_documents_exist` (`:243-246`) to include `ESCALATIONS.md` and fix its docstring in the same edit ('The three documents that carry the design' over a five-name tuple, about to be six). Add a project-map substring test in the `test_claude_md_project_map_lists_both_new_directories` idiom (`:276-283`).
  - - **path:** tests\test_request_links.py
    - **change:** Phase 3: widen the scan to root-level `*.md` by adding `REPO_ROOT.glob("*.md")` to the file list in `_scanned_files` (`:31-37`) — NOT by appending `REPO_ROOT` to `SCANNED_TREES` (`:24`), which would rglob into `.venv/` and `node_modules/`. Dedupe with `sorted(set(files))`.
  - - **path:** .claude\skills\make-feature-request\SKILL.md
    - **change:** Phase 4: one bullet in Step 2 (`:74-92`) telling intake to surface `ESCALATIONS.md` entries whose `Bears on:` names this item's moment. Decision 1 — a read added to a checklist, no decision logic changed.
  - - **path:** .claude\skills\commit\SKILL.md
    - **change:** Phase 4: a third rail beside the two at `:133-138` — when flipping a roadmap row to `DONE`, surface open queue entries naming it. Decision 5, closing finding F-32's drain hole. Step 4 already parses roadmap rows (`:116-131`), so it is the one thing running at that exact moment.
  - - **path:** .claude\skills\diagnose-bug\SKILL.md
    - **change:** Phase 4: one line inside the Escalation template (`:136-142`) telling the RCA to also park a queue entry naming the moment the bug bears on, plus the name-disambiguation line near the murky exit (`:161-162`). Decision 3 — a pointer only; the RCA keeps its Escalation section.
  - - **path:** requests\feature-requests\README.md
    - **change:** Pre-flight (Phase 0) and terminal: the H1 Index row's Stage cell at `:116` must match the Status header of every artifact in `H1-escalation-queue/`, per `tests/test_repo_structure.py:286-319`.
  - - **path:** requests\feature-requests\H1-escalation-queue\FEATURE_REQUEST.md
    - **change:** Pre-flight (Phase 0): Status blockquote at `:1` synced to the same stage word as the Index cell and the sibling artifacts. Bookkeeping only — the content is decided and not re-opened.
  - - **path:** requests\feature-requests\H1-escalation-queue\PROJECT_SCOPE.md
    - **change:** Pre-flight (Phase 0): Status blockquote at `:1` synced likewise. No other edit — this artifact is DECIDED and is consumed, not revised.
- **code_references:** - - **ref:** ROADMAP.md:197
    - **claim:** The H1 row — `escalation-queue`, Deliverable 'ESCALATIONS.md, the parked-decision format, and a structural test that every entry names a real roadmap item', Blocks 1.3, Status IN-PROGRESS. VERIFIED verbatim. This is the cell /commit flips to DONE in Phase 4.
  - - **ref:** ROADMAP.md:200-204
    - **claim:** 'Why H1 exists' — eight synchronous decisions in one sitting during item 1.1. VERIFIED. Note the scope's Affected Area cites `:199-203`; line 199 is blank and the paragraph runs 200-204.
  - - **ref:** ROADMAP.md:217-222
    - **claim:** The 'Parked, not scheduled.' paragraph naming the three seed hypotheses AND the 'serviceable ... is nowhere defined or tested' observation AC 13 requires preserved. VERIFIED at both endpoints (:217 opens, :222 closes with 'design polish, is what a gate would be for.').
  - - **ref:** ROADMAP.md — id-shaped leading cells
    - **claim:** MEASURED 2026-08-15: the discriminator `^(\d+\.\d+|H\d+)$` over the first cell of every table row returns exactly 38 ids (0.1-0.6, 1.1-1.11, H1, H2, 2.1-2.8, 3.1-3.5, 4.1-4.6). AC 2's known-good ids 0.1, 1.3, H2, 4.6 are all present; 38 clears the `>= 30` floor with eight rows of headroom.
  - - **ref:** ROADMAP.md:23-35, :41-51, :75-84, :324-334
    - **claim:** The four non-item tables. Leading cells are `**Creation**`, `**Career management**`, `**Age/development multiplier**` / plain text, and `**[OPEN-2]**` — bold or plain text, NOT empty. VERIFIED, which is exactly finding F-27/F-06's correction: emptiness is not the discriminator. All four contribute zero ids under the id-pattern rule.
  - - **ref:** ROADMAP.md:277
    - **claim:** `### Post-v1` — the header that grounds `post-v1` as a real moment in the widened vocabulary rather than a magic string. VERIFIED. `:330` and `:334` also carry `post-v1` phase cells.
  - - **ref:** ROADMAP.md:144, :163, :224, :246, :261
    - **claim:** The five `### Phase N — …` headers a `^###\s+(Phase \d+)\b` regex must match. VERIFIED, including `### Phase 4 — Career → **v1** — **NOT STARTED**`, which still matches on the `Phase 4` prefix.
  - - **ref:** ROADMAP.md:248
    - **claim:** Phase 3 'Proves: it works over 82 games, not 10 — and the app is genuinely serviceable.' VERIFIED — this is what grounds `Phase 3` as the serviceability-gate seed's moment.
  - - **ref:** ROADMAP.md:139-142
    - **claim:** 'This table is maintained by /commit … Statuses are a record of what landed, not a plan — don't mark an item DONE ahead of its commit.' VERIFIED. Why no phase hand-edits the H1 Status cell.
  - - **ref:** tests/test_layering.py:1-17
    - **claim:** The docstring idiom AC 1 requires, and the stated reason for the pure-function-plus-tmp_path shape: 'The obvious alternative — add `import fastapi` to the core and revert it — is not available: subagents have read-only git.' VERIFIED.
  - - **ref:** tests/test_layering.py:85-100
    - **claim:** The red/green tmp_path proofs AC 5 mirrors — `test_a_web_import_is_reported` asserts an exact single-violation list, `test_a_clean_tree_reports_nothing` asserts `== []`. VERIFIED.
  - - **ref:** tests/test_layering.py:70-79, :112-123
    - **claim:** `violation_message` and `test_the_failure_message_cites_the_architecture_record` — the 'failure message is the record' pattern AC 7 mirrors, asserting the message contains the citation and the specific offender. VERIFIED.
  - - **ref:** tests/test_repo_structure.py:243-246
    - **claim:** `test_core_documents_exist` — docstring says 'The three documents that carry the design' over a FIVE-name tuple (GAME_DESIGN, ROADMAP, DESIGN, README, CLAUDE). VERIFIED: the docstring already lies, which is Decision 7's answer to the minimalist dissent, and AC 17 fixes it in the same edit that adds the sixth name.
  - - **ref:** tests/test_repo_structure.py:286-319
    - **claim:** `test_index_stage_cells_match_their_artifact_status_headers` — the invariant AC 9 mirrors, and the pre-flight landmine. It globs (not rglobs) `*.md` in the item dir, so `reviews/` is exempt, and asserts every artifact's Status word equals the Index cell. VERIFIED, plus the precedent: all four `_done/1.1-app-shell/*.md` carry `implemented`.
  - - **ref:** tests/test_repo_structure.py:260-273
    - **claim:** `test_no_document_still_claims_there_is_no_application` — the substring-assertion idiom AC 12's seed test copies. VERIFIED, including its lesson about case: it compares lowercased on purpose.
  - - **ref:** tests/test_repo_structure.py:276-283
    - **claim:** `test_claude_md_project_map_lists_both_new_directories` — the exact idiom AC 19's project-map test copies, including a failure message that explains what an onboarding agent would not know. VERIFIED.
  - - **ref:** tests/test_request_links.py:26
    - **claim:** `FENCED_BLOCK = re.compile(r"^([ \t>]*)(`{3,}|~{3,}).*?^\1\2.*?$", re.DOTALL | re.MULTILINE)` — the fenced-block stripper to reuse so the queue's worked example is not parsed as a real entry, and the reason the /diagnose-bug edit inside a fence adds no link surface. VERIFIED.
  - - **ref:** tests/test_request_links.py:24, :31-37
    - **claim:** `SCANNED_TREES` and `_scanned_files`, which calls `tree.rglob("*.md")` per entry. VERIFIED — this is why AC 18 must be implemented as a non-recursive `REPO_ROOT.glob("*.md")` rather than by adding REPO_ROOT to the tuple.
  - - **ref:** tests/test_no_leaks.py:61-74
    - **claim:** `_tracked_text_files()` builds its list from `git ls-files -z`, i.e. the index. VERIFIED — so an untracked new ESCALATIONS.md is invisible to the leak guard locally until /commit stages it.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369
    - **claim:** 'OpenAPI snapshot test | drop | Out — asserts nothing the health test doesn't; right moment is 1.8. Record the pointer in 1.8's request.' VERIFIED verbatim — seed 4, moment 1.8, and the row itself says the pointer was supposed to be recorded somewhere.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:370
    - **claim:** 'OpenAPI→TypeScript codegen | drop | Out — machinery for one two-field object; the typed fetch wrapper is the seam it slots into at 1.8.' VERIFIED — seed 5, moment 1.8.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861
    - **claim:** CORRECTION. Reads: 'Out of scope but worth recording before item 2.1: `.gitignore:63`'s blanket `build/` will silently shadow the `build/build-*.py` builder pattern CLAUDE.md prescribes for Phase 2. Should become an intake item rather than being rediscovered at 2.1.' It is NOT the client-side router seam the scope labels it, and its moment is 2.1, not 1.10.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:319
    - **claim:** 'It is what stops item 1.10's router from being a breaking change' — the router text finding F-02 correctly ruled a shipped fold, not a stranded decision. VERIFIED; it is not a seed.
  - - **ref:** .gitignore:63
    - **claim:** `build/` — the blanket rule the corrected third seed is about. VERIFIED (`:62` is `dist/`, `:68` is `app/dist/`).
  - - **ref:** GAME_DESIGN.md:290, :335
    - **claim:** §8 'Open questions' opens at :290 and its last entry is `[OPEN-9]` at :335. VERIFIED — with ROADMAP.md:334 citing `[OPEN-10]`, the orphan is measured, and its phase cell 'post-v1, with aging' grounds the seed's moment.
  - - **ref:** DESIGN.md:50, :115
    - **claim:** §2 'Open mechanism work' and §4 'Open engineering questions' — the two registers the header's register map must disambiguate against, §4 being the sharpest overlap. VERIFIED.
  - - **ref:** docs/decisions/0010-panels-by-default.md:125-128
    - **claim:** 'Require the user to approve a stage plan on every item. Rejected: it makes the human a gate on the routine path, which is precisely the synchronous-interruption tax roadmap item H1 exists to remove.' VERIFIED verbatim — an accepted ADR already assumes this mechanism.
  - - **ref:** docs/decisions/README.md:26-34
    - **claim:** The Rules block — 'Don't edit an accepted ADR to reflect a change of mind … The value here is the record of what was believed at the time.' VERIFIED. This is the immutability ethos the resolved-entries-stay rule borrows by ANALOGY; ADR 0003's append-only rule governs `careers/**/events.jsonl` only and is not engaged.
  - - **ref:** .claude/skills/commit/SKILL.md:109-144
    - **claim:** Step 4 'Roadmap status' — the table of diff signals at :118-122, the two rails at :133-138 ('Never mark ahead' / 'Never mark down silently'), and 'Stage ROADMAP.md by path if you edited it' at :143. VERIFIED — the drain bullet joins the rails at :138.
  - - **ref:** .claude/skills/make-feature-request/SKILL.md:74-92
    - **claim:** Step 2 'Ground it in the repo' — the checklist of what to look up, ending at the project-rules bullet (:90-91). VERIFIED — this is where the read bullet lands, adding a lookup rather than changing decision logic.
  - - **ref:** .claude/skills/diagnose-bug/SKILL.md:136-142
    - **claim:** The `## Escalation (murky cause — RCA panel deferred)` fenced template with its (a)/(b)/(c) bullets, and :161-162 the murky exit ('the Escalation section is the deliverable; the bug waits'). VERIFIED — the pointer line lands in the template, inside a fence, so it adds no link-check surface.
  - - **ref:** ops/README.md:30-37
    - **claim:** 'This file is inert until it is re-applied. Editing branch-protection.json changes nothing on GitHub.' VERIFIED — the named failure mode the inertness risk borrows, and the reason the three read seams are core scope rather than enhancements.
  - - **ref:** pyproject.toml:80-84
    - **claim:** `[tool.mypy] strict = true`, `warn_unreachable = true`, `files = ["src", "tests"]`. VERIFIED — strict applies to the new test module, so every function and every test needs a full annotation and AC 21 forbids new `# type: ignore`.
  - - **ref:** .github/workflows/ci.yml:36-47
    - **claim:** The four gate steps: `ruff check --output-format=github`, `ruff format --check`, `mypy`, `pytest -m "not network" --cov=rpg_core --cov=rpg_api`. VERIFIED — the local commands mirror these, and the new tests join the existing 'Lint, types, tests' job with no CI change.
  - - **ref:** CLAUDE.md:216
    - **claim:** 'Check the ADRs before proposing anything structural. **Nine** decisions are settled' — VERIFIED drift, while `CLAUDE.md:57` and `docs/decisions/README.md:41-52` both say ten (ADR 0010 is accepted). AC 20's one-word fix.
  - - **ref:** CLAUDE.md — line count
    - **claim:** MEASURED 2026-08-15: `(Get-Content CLAUDE.md).Count` == 221, matching AC 23's stated baseline exactly. The budget allows 222 after the change.
  - - **ref:** README.md:55-73
    - **claim:** The project map is a fenced tree block (`├── GAME_DESIGN.md` … `└── var/`). VERIFIED — the queue's row goes inside the fence, adding no link-check surface, in the same style as :57-60.
  - - **ref:** requests/README.md:6-10
    - **claim:** The three-track table (feature-requests / bugfix-requests / calibration-findings). VERIFIED — the folded-in one-line pointer goes beside it, and it is the only pointer location the link checker already covers.
  - - **ref:** requests/feature-requests/README.md:76-79
    - **claim:** 'Criteria that only a human can prove … must be marked user-run so the acceptance panel doesn't claim them.' VERIFIED — the contract AC 24 invokes and why Phase 5 hands it to the user unclaimed.
  - - **ref:** src/rpg_core/
    - **claim:** Contains only `__init__.py`; `datasets/`, `careers/`, `rulesets/`, and `lib/` do not exist. VERIFIED by directory listing 2026-08-15 — which is why the dataset-by-name, append-only-ledger, ruleset-immutability, and cost-side-only conventions are N/A to this change and no data-contracts section applies.
- **open_questions:** - THE 1.3 SIGNAL HAS NO SEED. Scope risk 2 claims 'The seven seeds make this true on day one', but none of the seven names `1.3` — the moments are Phase 3, Phase 3, H2, 1.8, 1.8, 2.1, post-v1. AC 24's first user-run check ('picking up item 1.3, the entries naming 1.3 surface') therefore has nothing to surface. Decision needed at the Phase 0 gate: accept that the signal is carried by the `/make-feature-request` seam, or park one genuine 1.3 question (correction-by-append under ADR 0003 has real ones) during the build. Do NOT manufacture an entry purely to make a criterion green.
  - THE THIRD RECOVERED SEED'S SUBJECT AND MOMENT. The scope labels `_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861` 'the client-side router seam, moment 1.10'; the lines actually record the `.gitignore:63` blanket-`build/` shadowing of the Phase-2 builder pattern, moment 2.1. Confirm the corrected reading before writing the entry. (The seed COUNT of three strays and AC 12's 'seven' are unaffected either way, so this is a correction, not a re-scope.)
  - THE THREE INFERRED MOMENTS. Serviceability gate → `Phase 3`, design/UX specialist → `Phase 3`, autonomous stage dispatcher → `H2`. These are a reading of prose, not measurement (scope risk 14), and every later entry inherits the precedent. Confirm or correct at the Phase 0 gate, before entries pin them.
  - WHERE THE THREE SKILL-SEAM SUBSTRING TESTS LIVE. This plan puts them in `tests/test_escalations.py` (cohesive: they are about the queue's wiring) while AC 17 and AC 19 stay in `tests/test_repo_structure.py`. The alternative — all substring assertions in the Documentation section of `test_repo_structure.py` — is equally idiomatic. Not load-bearing; pick one and be consistent.
  - THE `Status:` CLOSED VOCABULARY. AC 8 requires one but does not name the values. `open` / `resolved` is the obvious pair and matches the two-section format (core scope 7). Confirm before writing, since a later widening is an edit to every entry.
  - THE RESOLVED-ENTRY POINTER SHAPE. Core scope 7 says a resolved entry stays with a `Status:` flip and 'a resolution pointer'. Whether that pointer is a seventh field or lives inside the `Assumed:`/body text is unsettled. Recommendation: keep it in the body, because AC 8 pins SIX required fields and finding F-26 exists precisely because a seventh field appeared in the tests but not the format.
  - UNCONFIRMED AND NOT DISCHARGEABLE HERE — whether the drain cadence actually fires. `requests/README.md:68-70` routes doc edits, typo fixes, and dependency bumps around the pipeline entirely, so 'every item boundary passes through /make-feature-request' is a belief, not a fact. It is the assumption the whole cadence rests on and no phase in this plan can verify it; it can only be observed once entries have sat through a few item boundaries. Carry the `unconfirmed` label into the implementation report.

---

## Planner 3

- **planner:** domain-convention
- **ok:** True
- **onboarding_files:** - - **path:** requests\feature-requests\H1-escalation-queue\PROJECT_SCOPE.md
    - **why:** The decided contract. 24 numbered acceptance criteria (lines 112-194), the tiered Scope list (196-243), the seven Decisions table (352-362) and the F-33 carve-out (364-368). Consume it; do not re-open fit, goals, or non-goals.
  - - **path:** ROADMAP.md
    - **why:** Both the spec of record AND the parser's input. Line 197 is the H1 row (Blocks: 1.3, IN-PROGRESS); 200-204 is 'Why H1 exists'; 217-222 is the 'Parked, not scheduled' paragraph that AC 13 reduces to a pointer. Lines 144/163/224/246/261/277 are the `### Phase N` / `### Post-v1` headings the widened moment vocabulary needs.
  - - **path:** tests\test_layering.py
    - **why:** The idiom `tests/test_escalations.py` must copy, near-exactly. Docstring 1-17 (why a pure function proven against tmp_path: subagents get read-only git), pure parser 38-67, `violation_message` 70-79, red/green tmp_path proofs 85-100, the real assertion 106-109, the failure-message-is-the-record test 112-123.
  - - **path:** tests\test_repo_structure.py
    - **why:** Two edits land here (AC 17, AC 19) and three conventions come from here: module docstring 1-9, module-level regex constants at 21 and 30, `test_core_documents_exist` 243-246 (docstring says 'three' over a five-name tuple), the substring-assertion idiom 260-283, and `test_index_stage_cells_match_their_artifact_status_headers` 286-319 — the test that forces every artifact header in the item dir to agree with the Index cell.
  - - **path:** tests\test_request_links.py
    - **why:** AC 18 extends this. `SCANNED_TREES` at 24, the `FENCED_BLOCK` exemption regex at 26 (reused by the new parser), `_scanned_files` 31-37 (rglob over DIRECTORIES — the reason root `*.md` needs a non-recursive glob, not a new tree entry), `_dead_links` 40-64.
  - - **path:** requests\feature-requests\_done\1.1-app-shell\IMPLEMENTATION_PLAN.md
    - **why:** The gold-standard plan and three live precedents: lines 1-6 settle that the status token is `planned` (not the SKILL template's `plan`); 157-161 is the 'confirm the branch — do NOT create one' opening step; 62-64 records that CI has no feature-branch push trigger. Lines 859-861 are ALSO one of the seven seeds — and the scope mis-describes them (see code_references).
  - - **path:** requests\feature-requests\_done\1.1-app-shell\PROJECT_SCOPE.md
    - **why:** Source of two recovered strays: line 369 (OpenAPI snapshot test, moment 1.8) and 370 (OpenAPI→TypeScript codegen, moment 1.8), plus 442-444 (the `.gitignore` blanket-`build/` stray, moment 2.1). Line 318-319 is the SPA history fallback — finding F-02 correctly ruled it a shipped fold, NOT a stray.
  - - **path:** CLAUDE.md
    - **why:** Two edits (AC 19, AC 20) and the budget gate (AC 23). Project-map fence 51-67; line 53 'ROADMAP.md v1 boundary, five phases, 36 work items'; line 216 'Nine decisions are settled' — the measured drift, contradicted by lines 57, 90, and 111 which all say ten. Measured 2026-08-15: `(Get-Content CLAUDE.md).Count` = 221.
  - - **path:** pyproject.toml
    - **why:** The gates AC 21 names. ruff config 53-77 (line-length 100, `PTH` forces pathlib over open(), `I` forces isort order); mypy `strict = true` over `files = ["src", "tests"]` at 80-84 — the new test module is fully type-checked; pytest config 87-96.
  - - **path:** .claude\skills\commit\SKILL.md
    - **why:** AC 15's target. Step 4 'Roadmap status' at 109-144 — the diff→status table at 118-122 and the 'never mark ahead / never mark down silently' rails at 133-138 are exactly where the drain-hole bullet belongs. Also the only sanctioned commit path; every phase below ends here.
- **architecture_notes:** SHAPE OF THE CHANGE. This is a process/docs feature with a single Python artifact. Nothing under `src/` or `app/` is touched (scope non-goal 9), no CI job is added (non-goal 11), and no runtime consumer exists beyond pytest (non-goal 10). The deliverable is four things: one new root document (`ESCALATIONS.md`), one new test module (`tests/test_escalations.py`), three one-line seams in existing skill files, and a set of substring-proved discoverability edits.

THE PARSER IS THE ONLY REAL ENGINEERING. `tests/test_escalations.py` is a pure-function guard in the `tests/test_layering.py` idiom: module-level functions taking a `Path`, proven RED and GREEN against `tmp_path` fabrications (test_layering.py:85-100), then applied once to the real files (test_layering.py:106-109), with the failure message itself treated as the record (test_layering.py:70-79 and its guard at 112-123). The reason that idiom exists is stated in its own docstring at test_layering.py:12-16: subagents have read-only git, so you cannot prove a guard red by mutating a tracked file and reverting.

TWO INPUTS, ONE INVARIANT. `known_moments(ROADMAP.md)` produces the legal vocabulary; `parse_entries(ESCALATIONS.md)` produces `(entry_id, moment)` pairs; the guard is set membership, and the violation list is what the failure message renders.

I PROTOTYPED THE PARSER AND MEASURED IT (2026-08-15, read-only script in scratchpad, no repo file touched). Row regex `^\|\s*(?P<lead>[^|]*?)\s*\|` plus id regex `^(?:\d+\.\d+|H\d+)$` over the real `ROADMAP.md` yields EXACTLY 38 unique ids (0.1-0.6, 1.1-1.11, 2.1-2.8, 3.1-3.5, 4.1-4.6, H1, H2), zero duplicates, and rejects all 57 other leading cells — including every row of the four non-item tables: `**Creation**`-style cells (23-35), `**Career management**`-style (41-51), the mixed bold/plain out-of-scope rows (75-84), and `**[OPEN-2]**`-style (324-334). Every table's `#`/`Surface`/`---` header and separator rows are rejected too. AC 2's `>= 30` floor and its four known-good ids (0.1, 1.3, H2, 4.6) all hold with headroom; AC 3's discriminator is confirmed to be id-pattern-match, not emptiness.

THE WIDENED VOCABULARY IS ALSO ROADMAP-DERIVED, not a hardcoded list. `### Phase 0` (144), `### Phase 1` (163), `### Phase 2` (224), `### Phase 3` (246), `### Phase 4` (261) and `### Post-v1` (277) are harvested by `^###\s+(Phase \d+|Post-v1)\b`. Measured total legal moments today = 38 ids + 5 phases + post-v1 = 44. Compare CASEFOLDED, because AC 5's green fixture names `post-v1` while the heading reads `Post-v1`.

THE FENCE IS THE SEAM THAT MAKES AC 11 AND AC 9 COMPATIBLE. AC 11 requires the header's fenced worked-example entry to parse green under the SAME regex and field guards as real entries; AC 9 requires every entry to sit in a `## Open` or `## Resolved` section agreeing with its `Status:`. A single whole-file parse cannot satisfy both — the example lives in neither section. Resolution: make `parse_entries(text: str)` a pure text function, then call it twice. (a) On the queue body with fenced blocks STRIPPED (reuse the `FENCED_BLOCK` regex idiom at test_request_links.py:26) — that is the real-entry population, and the section-agreement and uniqueness guards run over it. (b) On the extracted fenced example alone — that is AC 11. Give the example the reserved id `E-000`, which then cannot collide with `E-001…` because it never appears outside the fence.

STATE IS STORED ONCE (AC 9, finding F-08). `Status:` is the single source of truth; the section an entry sits in is DERIVED and merely asserted to agree — the same invariant `test_index_stage_cells_match_their_artifact_status_headers` (test_repo_structure.py:286-319) already enforces between the Index and artifact headers. Do not add a second status field.

THE LINK-CHECKER EXTENSION HAS A TRAP. `_scanned_files()` (test_request_links.py:31-37) iterates `SCANNED_TREES` and calls `tree.rglob("*.md")`. Adding `REPO_ROOT` to that tuple would recursively sweep `app/node_modules/`, `.venv/`, and `research/`. AC 18 means ROOT-LEVEL files only: extend `_scanned_files()` with a separate non-recursive `REPO_ROOT.glob("*.md")`, and update the module docstring (1-16) to say root documents are now scanned.

WHERE THE NEW FILE GETS SCANNED, AND WHERE IT DOESN'T. Once AC 18 lands, `ESCALATIONS.md` is link-checked, so every markdown link in it must resolve from repo root (verified targets: `ROADMAP.md`, `GAME_DESIGN.md`, `DESIGN.md`, `docs/decisions/0010-panels-by-default.md`, `.claude/skills/diagnose-bug/SKILL.md`, `requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md` — all exist; `_dead_links` strips a `:442-444` citation suffix at line 57 and skips fenced blocks at line 41). The CLAUDE.md and README.md project-map entries sit INSIDE fenced blocks, so they add zero link surface — which is exactly why scope core item 8 puts them there.

DATA CONTRACTS: N/A, VERIFIED BY PROBE, NOT ASSUMED. I ran `Test-Path` on 2026-08-15: `datasets/` False, `careers/` False, `rulesets/` False, `lib/` False, `build/` False, `ESCALATIONS.md` False; `src/rpg_core/` contains only `__init__.py`. There is no `datasets/manifest.json`, so there is no logical name to register and no grain to declare or prove. `docs/data-sources.md` does not exist either — this repo's equivalent is `docs/data-access.md`. ADR 0003's append-only rule and ADR 0004's version-immutability rule are not engaged by this change (no ledger, no ruleset). The plan therefore carries NO data-contracts section, and the convention lens is pointed at project-convention correctness instead (see the Conventions notes under phases and risks).
- **phases:** - - **name:** Phase 0 — Pre-flight (no commit)
    - **goal:** Start from a known-good, measured baseline on the right branch, so every later 'this went green' claim is a delta rather than an assumption.
    - **steps:** - Run `git branch --show-current` (read-only). It must print `phase1/escalation-queue` — the branch already carrying commit 5ccec37 'Open the intake request for H1 escalation-queue'. Do NOT create a branch; `git switch -c` here forks a duplicate off the same tip. This is the same opening step the 1.1 plan used at IMPLEMENTATION_PLAN.md:157-161.
      - Record the baseline green: `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy`. All four must exit 0 BEFORE you touch anything.
      - Re-measure the CLAUDE.md budget with the command AC 23 names: `(Get-Content CLAUDE.md).Count`. Expect 221 (measured 2026-08-15). Do NOT use `Measure-Object -Line` — scope risk 15 records that `update-docs/SKILL.md:76-77` prescribes it and it under-reports.
      - Read, in order: ROADMAP.md:195-222; tests/test_layering.py in full; tests/test_repo_structure.py:243-246 and 276-319; tests/test_request_links.py:24-64; requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369-370 and 442-444; requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861.
      - Read the open_questions list in this plan. Two of them (the corrected third stray, and the two seeds whose moments the roadmap never names) change what Phase 1 writes. If the user has not disposed them, take the plan's stated default and record it in the entry's own `Assumed:` field — that is precisely what the field is for.
    - **acceptance:** - `git branch --show-current` prints `phase1/escalation-queue`.
      - `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all exit 0 on an unmodified tree.
      - `(Get-Content CLAUDE.md).Count` is recorded as the baseline for AC 23 (expected 221).
    - **commit_note:** No commit — nothing changed. This phase exists so the later 'green' claims are deltas, not first observations.
  - - **name:** Phase 1 — ESCALATIONS.md: header, format, seven seeds; ROADMAP paragraph reduced to a pointer
    - **goal:** Land the queue itself — the boundary header (the load-bearing deliverable), the pinned six-field format, a fenced worked example, and seven real seed entries — and stop ROADMAP.md being a second source for three of them.
    - **steps:** - Create `ESCALATIONS.md` at the REPO ROOT. Root is not a style call: scope Decision 7 records that all three scopers chose it because the three hypotheses failed precisely by living somewhere nobody looks.
      - Write the boundary header FIRST, for a worker about to add an entry. It must carry: (a) what the queue is for — a decision recorded instead of asked, with the assumption actually taken written down; (b) a REGISTER MAP table disambiguating it from the five existing parking places — `GAME_DESIGN.md` §8 `[OPEN-N]` (design questions, indexed by answering phase), `DESIGN.md` §2 'Open mechanism work' (line 50), `DESIGN.md` §4 'Open engineering questions' (line 115), ADR 0010's Stage plan section (per-request ceremony), and `/diagnose-bug`'s Escalation (a murky-cause hand-off, `.claude/skills/diagnose-bug/SKILL.md:132-142`); (c) the ADR-0011 trigger verbatim from scope Decision 6 — 'the first time someone proposes absorbing [OPEN-N] or DESIGN.md §4, that is the second litigation and ADR 0011 gets written then'; (d) one line recording that a ROADMAP `Blocks` cell stops work while a queue entry NEVER does; (e) the F-33 carve-out, both halves — parking never blocks a stage, a build, or a merge, AND the queue file is not exempt from ordinary CI, so a malformed entry fails `tests/test_escalations.py` like any other guard; (f) the name-collision one-liner: 'escalation' now means two things in this repo — non-blocking and moment-scoped here, blocking and bug-scoped in `/diagnose-bug`.
      - Pin the entry format, six required fields, exactly. Heading: `### E-NNN — <question title>`. Body, one bullet each: `- **Bears on:** <moment>` · `- **Parked:** <YYYY-MM-DD>` · `- **Assumed:** <what was actually done, or "none — not hit yet">` · `- **Source:** <where it surfaced>` · `- **Status:** open`. Six, not five — finding F-26 caught that `Source:` was required by the tests while missing from the pinned format; every entry carries provenance uniformly, never conditionally.
      - State the legal moment vocabulary in the header: a roadmap item id (`1.3`, `H2`), a phase name (`Phase 3`), or `post-v1`. State explicitly that there is NO bare `unscheduled` hatch, and that a genuinely unmoored question belongs in `DESIGN.md` §4 or `GAME_DESIGN.md` §8 instead — that sentence is the mitigation for risk 8 (mis-filing under the nearest moment).
      - Put the WORKED EXAMPLE in a fenced code block inside the header, using reserved id `E-000`, and make it a plausible entry rather than lorem ipsum. It must be byte-legal under the same regex and field guards as real entries — AC 11 exists because the panel found goal 1 otherwise had no proof. Do not put `E-000` anywhere outside the fence.
      - Add `## Open` and `## Resolved` sections. Document in the header that a resolved entry STAYS, flipping `Status:` to `resolved`, moving to `## Resolved`, and gaining a resolution pointer — mirroring the ADR immutability ethos at `docs/decisions/README.md:20` and 26-34. Note this is an ANALOGY to that ethos, not an application of ADR 0003, whose append-only rule governs `careers/**/events.jsonl` only.
      - Write the seven seeds under `## Open`, ids E-001..E-007, each `Status: open`, each `Parked:` today. Seeds 1-3 from `ROADMAP.md:217-222`: the web-UI serviceability gate (moment `Phase 3`; its `Assumed:` should record that 'serviceable' is a v1 constraint in both directions and is nowhere defined or tested), the design/UX specialist, and the autonomous stage dispatcher. Seeds 4-6 recovered from `_done/1.1-app-shell/`: the OpenAPI snapshot test (moment `1.8`, `Source: requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369`), OpenAPI→TypeScript codegen (moment `1.8`, `Source: …PROJECT_SCOPE.md:370`), and the `.gitignore:63` blanket `build/` rule that will shadow the `build/build-*.py` builder pattern (moment `2.1`, `Source: …PROJECT_SCOPE.md:442-444` and `…IMPLEMENTATION_PLAN.md:859-861`). Seed 7 is the `[OPEN-10]` orphan (moment `post-v1`; recorded, NOT answered — scope non-goal 13 and Decision 4).
      - CORRECTION YOU MUST APPLY, do not silently follow the scope's wording: the scope's Affected Area calls the third stray 'the client-side router seam' at moment `1.10`, citing `IMPLEMENTATION_PLAN.md:859-861`. I read those lines. They are the `.gitignore` blanket-`build/` item, whose named moment is `2.1` ('worth recording before item 2.1'), duplicated at `PROJECT_SCOPE.md:442-444`. There is no stranded router-seam decision in the 1.1 artifacts — the router mention at `PROJECT_SCOPE.md:318-319` is the SPA history fallback, which SHIPPED, exactly as finding F-02 ruled. Seed it as the `2.1` gitignore item.
      - LABEL THE EPISTEMICS in the entries themselves, per CLAUDE.md's 'Label your epistemics' rule and scope risk 14. The moments for seeds 4-6 are INFERRED from the 1.1 panel's judgment of 2026-08-14; the moments for seeds 1-3 are this planner's/panel's reading of prose, and ROADMAP.md:217-222 names NO moments at all for the design/UX specialist or the autonomous stage dispatcher. Say `inferred` in each `Assumed:` field. Do not present any of them as measured.
      - Edit `ROADMAP.md:217-222`. Replace the enumerating paragraph with a POINTER to `ESCALATIONS.md`, and PRESERVE its substantive observation — that 'serviceable' is already a v1 constraint in both directions (see §v1) and is nowhere defined or tested. Delete the enumeration of the three hypotheses; that deletion is the whole point of AC 13, and leaving it is how the migration ends with two sources of truth. Do NOT touch the H1 row at line 197 — `/commit` owns statuses (ROADMAP.md:139-142).
      - Proofread for the public repo before you finish: `tests/test_no_leaks.py` blocks drive-letter paths (regex at line 26), `/home/`|`/Users/` paths (28), and email addresses (29). It scans TRACKED files via `git ls-files` (61-74), so an untracked new file is invisible to it and `uv run pytest` will pass VACUOUSLY on `ESCALATIONS.md` until `/commit` stages it. Read the entries with your own eyes; scope risk 10 is that entries record real assumptions taken when nobody was sure, world-readable forever.
    - **acceptance:** - `ESCALATIONS.md` exists at the repo root, opens with the boundary header, and the header names all five other registers: `GAME_DESIGN.md`, `DESIGN.md` (§2 and §4), ADR 0010's Stage plan, and `/diagnose-bug`'s Escalation.
      - Seven entries E-001..E-007 exist under `## Open`, each carrying all six fields, each `Bears on:` naming one of: a roadmap item id, `Phase N`, or `post-v1`. Hand-check every moment against ROADMAP.md — the mechanical guard arrives in Phase 2.
      - Grep proves all seven distinctive strings are present: `serviceability gate`, `design/UX specialist`, `autonomous stage dispatcher`, `OpenAPI snapshot test`, `OpenAPI→TypeScript codegen`, `blanket ` + backtick-build, and `[OPEN-10]`.
      - `ROADMAP.md` contains `ESCALATIONS.md` and NO LONGER contains `autonomous stage dispatcher` or `design/UX specialist`; the 'serviceable is nowhere defined or tested' observation survives.
      - No entry TITLE contains `[OPEN-`.
      - `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all exit 0 (no Python changed; this proves nothing regressed).
    - **commit_note:** feat(H1): add ESCALATIONS.md — boundary header, six-field entry format, seven seeds; ROADMAP's parked paragraph becomes a pointer. /commit will flip H1 to IN-PROGRESS against the diff (it is already IN-PROGRESS at ROADMAP.md:197).
  - - **name:** Phase 2 — tests/test_escalations.py: the parser guard
    - **goal:** Make the pointer mechanical rather than aspirational — a pure, fully-typed parser guard proven red AND green against tmp_path, whose failure message is the record.
    - **steps:** - Create `tests/test_escalations.py` as a NEW module (scope core item 5 — the request's constraint binds the IDIOM, and a parser guard is closer to `test_layering.py` than to an append onto `test_repo_structure.py`).
      - Open with a module docstring in the `tests/test_repo_structure.py:1-9` / `tests/test_layering.py:1-17` idiom explaining WHY the guard exists: an entry naming a moment that does not exist is an entry nothing will ever surface, which is the exact failure the queue was built to end. Say, as test_layering.py:12-16 does, that it is a pure function proven against `tmp_path` because subagents have read-only git.
      - Add `from __future__ import annotations` (every test module in this repo has it) and module-level constants in the `test_repo_structure.py:21,30` convention: `REPO_ROOT = Path(__file__).resolve().parents[1]`, `QUEUE = REPO_ROOT / "ESCALATIONS.md"`, `ROADMAP = REPO_ROOT / "ROADMAP.md"`, `TABLE_ROW = re.compile(r"^\|\s*(?P<lead>[^|]*?)\s*\|")`, `ITEM_ID = re.compile(r"^(?:\d+\.\d+|H\d+)$")`, `PHASE_HEADING = re.compile(r"^###\s+(?P<name>Phase \d+|Post-v1)\b", re.MULTILINE)`, `ENTRY = re.compile(r"^###\s+(?P<id>E-\d{3})\s+—\s+(?P<title>.+)$", re.MULTILINE)`, `FIELD = re.compile(r"^-\s+\*\*(?P<field>Bears on|Parked|Assumed|Source|Status):\*\*\s+(?P<value>.+)$", re.MULTILINE)`, `REQUIRED_FIELDS = frozenset({"Bears on", "Parked", "Assumed", "Source", "Status"})`, `STATUS_VALUES = frozenset({"open", "resolved"})`, and a `FENCED_BLOCK` copy of the regex at `tests/test_request_links.py:26`.
      - Write the pure functions, all fully annotated (mypy is `strict = true` over `files = ["src", "tests"]`, pyproject.toml:80-84 — zero new `# type: ignore`, per AC 21): `known_moments(roadmap: Path) -> set[str]` (casefolded ids from table rows plus phase/post-v1 headings), `parse_entries(text: str) -> list[Entry]` operating on TEXT not a path so it can be called on both the queue body and the extracted example, `unknown_moments(queue: Path, roadmap: Path) -> list[tuple[str, str]]`, and `violation_message(violations) -> str`.
      - AC 2 — parser POSITIVE assertion: over the real ROADMAP.md, `known_moments` is non-empty, contains `0.1`, `1.3`, `h2`, `4.6` (casefolded), and `len(...) >= 30`. Deliberately NOT an exact set: finding F-28 showed an exact assertion fires in unrelated PRs whenever a roadmap row is added. I measured 44 today (38 ids + 5 phases + post-v1), so the floor has real headroom.
      - AC 3 — parser NEGATIVE assertion. The discriminator is ID-PATTERN MATCH, not emptiness: the v1 scope tables lead with `**Creation**`-style bold cells, not empty ones. Prove all four non-item tables contribute zero ids. Slice the real ROADMAP.md by HEADING ANCHOR rather than by line number — `### In scope — the engine` (line 21), `### In scope — the application` (37), `### Out of scope` (74), `## Open questions, by the phase that answers them` (322), each up to the next heading — and assert the id set of each slice is empty. Anchors, not line numbers, because hardcoded ranges are the same brittleness F-28 rejected; the assertion is still against the real file, so the AC's intent is met.
      - AC 4 — NON-VACUITY. Assert `parse_entries` matched at least one entry on the real queue, failing with exactly: `no entries parsed — the entry format drifted from the regex`. Without this, a format change turns every `for entry in parsed: assert …` into `assert not []` and the guard passes forever (scope risk 4).
      - AC 5 — proven RED and GREEN against `tmp_path`, mutating no tracked file, in the `test_layering.py:85-100` idiom. Both fixtures fabricate THE ROADMAP TOO, so the green case is hermetic (finding F-07). Red: a fake roadmap with rows `1.3`/`H2` and a `### Phase 3` heading, plus a fake queue whose single entry bears on `9.9` → returns exactly `[("E-001", "9.9")]`. Green: a fake queue with four entries naming `1.3`, `H2`, `Phase 3`, `post-v1` against the same fake roadmap → returns `[]`. Note the green case only passes if comparison is CASEFOLDED — the heading reads `Post-v1` and the entry reads `post-v1`.
      - AC 6 — THE REAL ASSERTION: `unknown_moments(QUEUE, ROADMAP)` over the actual files returns `[]`, asserted with `violation_message(...)` as the message.
      - AC 7 — the failure message IS the record, mirroring `test_layering.py:112-123`. It must name the queue file (`ESCALATIONS.md`), the offending entry id, the offending moment, and STATE THE RULE — that every entry names a real roadmap moment because the queue is drained at item boundaries, so an unmoored entry is structurally invisible. Test the message directly on a fabricated violation, as test_layering.py does.
      - AC 8 — field and vocabulary guards over the real queue: every entry carries all SIX required fields (the five `**Field:**` bullets plus a title whose id matches `^E-\d{3}$`); ids are unique; every `Status:` value is in `STATUS_VALUES`.
      - AC 9 — state stored ONCE. Parse the `## Open` and `## Resolved` sections, and assert the section an entry sits in agrees with its `Status:`. `Status:` is the single source of truth; the section is derived and merely checked — the same invariant `test_index_stage_cells_match_their_artifact_status_headers` (test_repo_structure.py:286-319) enforces between the Index and artifact headers. Do not introduce a second status field.
      - AC 10 — register-boundary guard: no parsed entry TITLE contains `[OPEN-` (scope the check to titles, not the whole file — the header's register map legitimately names `[OPEN-N]`), and the queue body contains `GAME_DESIGN.md`, `DESIGN.md`, `Stage plan`, and `Escalation`.
      - AC 11 — goal 1 proven, not assumed. Extract the header's fenced worked example, run `parse_entries` and the SAME field guards over it, and assert it yields exactly one entry with all six fields and a legal `Status:`. A worker copying the template cannot produce a malformed entry — that is what 'the third move exists' reduces to mechanically.
      - AC 12 — seed migration proven, in the substring idiom of `test_no_document_still_claims_there_is_no_application` (test_repo_structure.py:260-273): assert the queue contains all SEVEN distinctive strings. Seven, per finding F-02's correction — three hypotheses, three recovered strays, one `[OPEN-10]` orphan.
      - AC 13 — no duplicate source: assert `ROADMAP.md` contains `ESCALATIONS.md` and does NOT contain `autonomous stage dispatcher` or `design/UX specialist`.
      - Ruff will bite if you are careless: `PTH` forbids bare `open()` (use `Path.read_text(encoding="utf-8")`, as every existing test does), `I` enforces import order, `RUF`/`B` catch the rest, and `ruff format --check` is a real CI gate (`.github/workflows/ci.yml:40`).
    - **acceptance:** - `uv run pytest tests/test_escalations.py -q` is green, and the module carries the explanatory docstring (AC 1).
      - The red fixture returns exactly one violation and the green fixture returns none, both against `tmp_path` fabrications of BOTH files — no tracked file is mutated to prove either (AC 5).
      - Deliberately break it once to prove it is not vacuous: temporarily add a `Bears on: 9.9` entry to a tmp_path copy — never to the tracked file — and confirm the message names ESCALATIONS.md, the entry id, `9.9`, and the rule.
      - `uv run ruff check`, `uv run ruff format --check`, and `uv run mypy` all exit 0 with the new module present, with zero new `# type: ignore` (AC 21).
      - `uv run pytest` green overall (AC 22).
    - **commit_note:** feat(H1): add tests/test_escalations.py — every entry's named moment must exist in ROADMAP.md, proven red and green against tmp_path
  - - **name:** Phase 3 — The three read seams
    - **goal:** Kill the headline risk. All three scopers independently named INERTNESS — structurally identical to the 'inert until it is re-applied' failure `ops/README.md:30-37` documents for `branch-protection.json` — as feature-killing. Three one-line seams give the queue something that actually reads it.
    - **steps:** - AC 14 / Decision 1 — `/make-feature-request` gets the READ seam. Add ONE bullet to Step 2 'Ground it in the repo' (`.claude/skills/make-feature-request/SKILL.md:74-95`, alongside the existing bullets at 79-91): when grounding a roadmap item, open `ESCALATIONS.md` and surface any open entry whose `Bears on:` names this item's moment. This lands exactly where the request's observable signal fires — 'item 1.3 can start by reading what was parked against it'. It adds a READ to a checklist and changes no decision logic.
      - AC 15 / Decision 5 — `/commit` gets the DRAIN-HOLE check. Add ONE bullet to Step 4 'Roadmap status' (`.claude/skills/commit/SKILL.md:109-144`), near the diff→status table at 118-122: when flipping a row to `DONE`, surface any open `ESCALATIONS.md` entry naming that row, because an entry naming 1.3 goes permanently invisible the moment 1.3 closes (finding F-32). Step 4 already parses roadmap rows and flips statuses — it is the one thing running at exactly that moment. Respect the skill's keep-it-lightweight posture: one bullet, no new sub-procedure.
      - AC 16 / Decision 3 — `/diagnose-bug` gets a POINTER, not a migration. Add ONE line to its Escalation section (`.claude/skills/diagnose-bug/SKILL.md:132-142`, and/or the murky exit at 161-162) telling it to ALSO park an `ESCALATIONS.md` entry naming the moment the bug bears on. The RCA keeps its Escalation section — the evidence trail is not duplicated. This preserves the non-blocking property exactly: the bug still waits; the queue only remembers that it is waiting.
      - Fold 8 — the name disambiguation. Add a one-liner in `/diagnose-bug` (mirroring the one already in the queue header from Phase 1) noting that 'escalation' now means two things: a non-blocking, moment-scoped queue entry, and a blocking, bug-scoped RCA hand-off.
      - LINKS MUST RESOLVE. `.claude/skills/` is already scanned by `tests/test_request_links.py` (SCANNED_TREES at line 24). From `.claude/skills/<name>/SKILL.md`, the queue is `../../../ESCALATIONS.md` — the same depth as the existing `../../../requests/feature-requests/README.md` links. Verify by running `uv run pytest tests/test_request_links.py -q`.
      - Add three substring tests to `tests/test_escalations.py`, in the `test_repo_structure.py:260-283` idiom, each asserting the seam's presence in its SKILL.md and each carrying a docstring saying WHY — a seam nobody reads is the inert-config failure, and a substring test is the cheapest thing that notices its removal. Pick stable substrings (`ESCALATIONS.md` plus a short distinctive phrase), not whole sentences.
    - **acceptance:** - `uv run pytest tests/test_escalations.py -q` green, now including three seam substring tests that each fail if the corresponding bullet is deleted (verify by deleting one in a scratch copy, not in the tracked file).
      - `uv run pytest tests/test_request_links.py -q` green — every link added to the three SKILL.md files resolves.
      - `/diagnose-bug`'s Escalation section still contains its (a)/(b)/(c) structure at 136-141 — the pointer is ADDITIVE, not a replacement (Decision 3).
      - No skill WRITES an entry anywhere (Decision 2 — deferred to its own request). Grep the three diffs to confirm each is a read/surface/park-by-hand instruction only.
      - `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all exit 0.
    - **commit_note:** feat(H1): give the escalation queue three read seams — /make-feature-request surfaces entries at intake, /commit checks the drain hole on DONE, /diagnose-bug parks a pointer
  - - **name:** Phase 4 — Discoverability and the folded doc fixes
    - **goal:** Make the queue findable from the two maps a cold agent actually onboards from, protect it from silent deletion, and clear the measured doc drift — all inside a one-line CLAUDE.md budget.
    - **steps:** - AC 17 / fold 4 — extend `test_core_documents_exist` (`tests/test_repo_structure.py:243-246`) to include `ESCALATIONS.md`, AND fix its docstring in the same edit. It currently reads 'The three documents that carry the design' over a five-name tuple; it becomes six. Decision 7 records that this deletion guard is real protection — without it, removing the file silently empties the mechanism — and that fixing the already-lying docstring answers the minimalist's dissent.
      - AC 18 / fold 5 — extend `tests/test_request_links.py` to root-level `*.md`. Do NOT add `REPO_ROOT` to `SCANNED_TREES` (line 24): `_scanned_files()` (31-37) calls `rglob` on each tree and would sweep `app/node_modules/`, `.venv/`, and `research/`. Add a non-recursive `REPO_ROOT.glob("*.md")` instead, and update the module docstring (1-16) to say root documents are scanned. MEASURED BY ME 2026-08-15 by importing `_dead_links` and running it over all five root documents: CLAUDE.md `[]`, DESIGN.md `[]`, GAME_DESIGN.md `[]`, README.md `[]`, ROADMAP.md `[]`. This costs zero cleanup — confirming the scope's measurement independently.
      - AC 19 / core 8 — one line INSIDE `CLAUDE.md`'s project-map fenced block (51-67), placed next to `ROADMAP.md` at line 53. Inside the fence so it adds no link-check surface, which now matters because of AC 18. One line, e.g. `ESCALATIONS.md      Parked decisions — each names the roadmap moment it bears on`.
      - AC 19 continued — one row in `README.md`'s project-map fenced block (53-73), matching the tree-glyph style, after the `ROADMAP.md` line at 58.
      - AC 20 / fold 9 — fix the measured drift at `CLAUDE.md:216`: 'Nine decisions are settled' becomes 'Ten'. Lines 57 ('ten settled calls'), 90 ('Ten ADRs'), and 111 ('All ten are ADRs') already say ten, and `docs/decisions/README.md:43-52` lists ten accepted ADRs. One word, zero line delta.
      - Fold 7 — one line in `requests/README.md` beside the three-track table (lines 6-10), linking `[ESCALATIONS.md] (../ESCALATIONS.md)`. This is the ONE pointer location already covered by the link checker today, so it is the one pointer that cannot silently rot.
      - AC 23 — BUDGET. Re-run `(Get-Content CLAUDE.md).Count`. Baseline 221, ceiling 222. The map line is +1 and the 'Nine'→'Ten' fix is +0. Do NOT also add a prose bullet about the queue to CLAUDE.md's 'Important locations' or 'How to help' — that is the obvious way to blow the budget, and the map line plus the queue's own header already carry the content.
      - Add a `test_project_maps_list_the_escalation_queue` to `tests/test_repo_structure.py` in the exact idiom of `test_claude_md_project_map_lists_both_new_directories` (276-283), asserting `ESCALATIONS.md` appears in both `CLAUDE.md` and `README.md`.
    - **acceptance:** - `uv run pytest tests/test_repo_structure.py -q` green with the extended `test_core_documents_exist` (six names) and its corrected docstring (AC 17).
      - `uv run pytest tests/test_request_links.py -q` green with root-level `*.md` scanned, and the scan demonstrably reaches root docs — confirm by pointing a scratch copy of a root doc at a nonexistent target and seeing it fail, then discarding the scratch copy.
      - `ESCALATIONS.md` appears in CLAUDE.md's project-map fence, README.md's project-map fence, and `requests/README.md`, each substring-tested (AC 19, fold 7).
      - `CLAUDE.md:216` says 'Ten decisions are settled' (AC 20).
      - `(Get-Content CLAUDE.md).Count` <= 222 — at most one line over the 221 baseline (AC 23).
      - `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all exit 0.
    - **commit_note:** feat(H1): make the escalation queue discoverable — both project maps, requests/README, the deletion guard, and root docs joined to the link checker
  - - **name:** Phase 5 — Close out: report, status rollover, and the user-run acceptance
    - **goal:** Land the item honestly — every criterion evidenced, the artifact trail consistent, and the one criterion only the user can prove marked as theirs rather than claimed.
    - **steps:** - Write `requests/feature-requests/H1-escalation-queue/IMPLEMENTATION_REPORT.md` as an acceptance ledger: all 24 criteria, each with the command run and its result. Mark AC 24 USER-RUN and do NOT claim it — the scope tags it 'USER-RUN, not panel-claimable'.
      - Roll ALL FOUR artifact status blockquotes in the item directory to `implemented` in the SAME commit — FEATURE_REQUEST.md, PROJECT_SCOPE.md, IMPLEMENTATION_PLAN.md, IMPLEMENTATION_REPORT.md — and set the Index row in `requests/feature-requests/README.md` (line 116) to match. `test_index_stage_cells_match_their_artifact_status_headers` (test_repo_structure.py:286-319) compares the Index cell against EVERY `*.md` in the directory, so a partial rollover fails CI. Precedent: all four 1.1 artifacts read `implemented`. Use the token from the README grammar (line 106), not the SKILL template's — `_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:3-6` settles that the repo's own contract wins.
      - Per the track README (lines 96-100), move the directory once into `requests/feature-requests/_done/H1-escalation-queue/` and repoint the Index link. That move also exempts it from the link checker and from `test_every_live_intake_artifact_declares_a_stage_plan` (test_repo_structure.py:185-217), both by design.
      - Do NOT hand-edit `ROADMAP.md`'s H1 Status at line 197. `/commit` Step 4 owns it and advances it against the staged diff (ROADMAP.md:139-142, commit/SKILL.md:109-144). Once H1 is DONE, the `Blocks: 1.3` cell releases and 1.2/1.3 can proceed.
      - Run the full local gate one last time, then `/commit`. Note that `.github/workflows/ci.yml:3-7` triggers only on push-to-main, pull_request, and workflow_dispatch — so a feature-branch push runs ZERO workflows. Local green is the only signal until the user opens the PR. `/commit` pushes the branch; the PR stays the user's.
      - Re-run `uv run pytest` AFTER `/commit` has staged and committed. `tests/test_no_leaks.py` scans `git ls-files` output (61-74), so the leak guards on `ESCALATIONS.md` are vacuous until the file is tracked. This is the run that actually proves AC 22 for the new document.
      - Hand the user AC 24 explicitly: pick up item 1.3, confirm the entries naming 1.3 surface without being asked anything, confirm an entry can be answered by editing the queue alone with no build running, and judge whether the parked context reads as useful rather than as noise.
    - **acceptance:** - `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` all exit 0 (AC 21, AC 22).
      - `uv run pytest tests/test_repo_structure.py::test_index_stage_cells_match_their_artifact_status_headers -q` green after the rollover — Index cell and all four artifact headers agree.
      - A post-commit `uv run pytest` is green, which is the first run in which `tests/test_no_leaks.py` actually sees `ESCALATIONS.md`.
      - `IMPLEMENTATION_REPORT.md` records all 24 criteria with evidence, with AC 24 marked USER-RUN and unclaimed.
      - `/commit` advanced `ROADMAP.md` H1 to DONE against the diff — not hand-edited.
      - USER-RUN (AC 24): the user confirms, on picking up 1.3, that the parked entries surface unprompted and read as useful.
    - **commit_note:** feat(H1): land the escalation queue — acceptance report, artifacts rolled to implemented, H1 archived to _done/. /commit advances the H1 row and releases Blocks: 1.3
- **testing:** THE WHOLE VERIFICATION IS PYTEST + RUFF + MYPY, run locally at every phase boundary and re-run by CI on the PR. There is no new CI job (scope non-goal 11) — the tests join the existing `Lint, types, tests` job (`.github/workflows/ci.yml:19`), so `ops/branch-protection.json` and `tests/test_ci_contexts.py` stay untouched.

Per-phase gate, every phase, no exceptions: `uv run pytest` · `uv run ruff check` · `uv run ruff format --check` · `uv run mypy` — all four exit 0 — then `/commit`. `ruff format --check` is a real CI step (ci.yml:40), not a nicety, and mypy is `strict = true` over `files = ["src", "tests"]` (pyproject.toml:80-84), so the new test module is fully type-checked and AC 21 forbids any new `# type: ignore`.

THE GUARD'S OWN PROOF STRATEGY IS THE INTERESTING PART. `tests/test_escalations.py` follows `tests/test_layering.py` exactly: the parser is a pure module-level function so it can be proven RED and GREEN inside one `uv run pytest`, against `tmp_path` fabrications, without ever editing a tracked file. That is not stylistic — test_layering.py:12-16 states the reason: subagents have read-only git, so the obvious alternative (introduce the violation, observe the failure, revert) is not available. Both AC 5 fixtures fabricate the ROADMAP as well as the queue, so the green case is hermetic and cannot be broken by an unrelated roadmap edit (finding F-07).

THE ANTI-VACUITY LAYER IS THE THING MOST LIKELY TO BE SKIPPED, AND IT IS THE POINT. Three separate guards defend against a test that passes forever while checking nothing: AC 4 (assert at least one entry parsed, with the literal message `no entries parsed — the entry format drifted from the regex`), AC 2's non-empty + `>= 30` floor on the moment vocabulary, and AC 11 (the header's own worked example must parse under the same regex). A cold implementer should verify each by breaking it deliberately IN A SCRATCH COPY — never in a tracked file — and confirming the failure is what the message says it is.

TWO MEASUREMENTS I VERIFIED MYSELF RATHER THAN INHERITING (2026-08-15, read-only, nothing modified):
(1) I imported `_dead_links` from `tests/test_request_links.py` and ran it over all five root documents. Every one returned `[]`. AC 18's claim that extending the link checker to root `*.md` costs zero cleanup is CONFIRMED independently of the scope.
(2) I prototyped the roadmap parser with `^\|\s*([^|]*?)\s*\|` + `^(?:\d+\.\d+|H\d+)$` and got exactly 38 unique ids with zero duplicates, and zero ids from any of the four non-item tables (23-35, 41-51, 75-84, 324-334) or from any header/separator row. AC 2's `>= 30` and AC 3's id-pattern discriminator are CONFIRMED against the real file.

REGRESSION SAFETY. The change adds one root document, one test module, three one-line skill bullets, two project-map lines, one word in CLAUDE.md, and two test extensions. The only edits that can regress anything already green are the two test extensions — `test_core_documents_exist` (a strictly larger tuple, so it can only newly fail if `ESCALATIONS.md` is absent) and `_scanned_files()` (measured clean above). Nothing under `src/` or `app/` is touched, so `test_api_health.py`, `test_api_spa.py`, `test_packaging.py`, and `test_layering.py` cannot be affected.

ONE ORDERING TRAP TO PLAN AROUND. `tests/test_no_leaks.py` scans the git index via `git ls-files` (61-74), so an UNTRACKED `ESCALATIONS.md` is invisible to it and `uv run pytest` passes vacuously on the new document. AC 22's leak coverage only becomes real once `/commit` stages the file. Therefore: re-run `uv run pytest` AFTER the Phase 1 commit, and proofread the entries by eye in the meantime — the repo is public, the file records real assumptions taken when nobody was sure, and the leak guards catch drive-letter paths and emails but not indiscretion.

AC 24 IS USER-RUN AND MUST NOT BE CLAIMED. Whether the parked context reads as useful rather than as noise when the user picks up item 1.3 is exactly the kind of criterion `requests/feature-requests/README.md:76-79` says must be marked user-run so the acceptance panel does not claim it.
- **risks:** - THE SCOPE MIS-DESCRIBES ONE OF THE SEVEN SEEDS, AND A COLD AGENT WILL COPY IT VERBATIM. `PROJECT_SCOPE.md:232` calls the third recovered stray 'the client-side router seam' at `IMPLEMENTATION_PLAN.md:859-861`, moment `1.10`. I read those lines: they are the `.gitignore:63` blanket-`build/` rule that will shadow the Phase 2 `build/build-*.py` builder pattern, whose named moment is `2.1`, duplicated at `_done/1.1-app-shell/PROJECT_SCOPE.md:442-444`. There is no stranded router decision — the router mention at `PROJECT_SCOPE.md:318-319` is the SPA history fallback, which shipped, exactly as finding F-02 ruled. Seeding the wrong content under the wrong moment would make the queue's day-one content wrong in the one place it is supposed to be authoritative. Mitigation: Phase 1 prescribes the corrected seed explicitly.
  - TWO OF THE SEVEN SEEDS HAVE NO MOMENT ANYWHERE IN THE REPO. `ROADMAP.md:217-222` says each hypothesis has 'a named moment to re-decide' but names none of them. The web-UI serviceability gate maps defensibly to `Phase 3` (its exit is 'the app is genuinely serviceable', ROADMAP.md:248). The design/UX specialist and the autonomous stage dispatcher have no named moment at all. Hard-requiring a moment (scope core item 4) then pressures the implementer to attach them to whatever row is nearest — scope risk 8, corrupting the drain while looking correct. Mitigation: choose, label the choice `inferred` in the entry's own `Assumed:` field, and let the user redirect. That is what the field is for.
  - INERTNESS — the dominant risk, named independently by all three scopers. At the roadmap's three literal deliverables nothing writes to and nothing reads the queue, structurally identical to the 'inert until it is re-applied' failure `ops/README.md:30-37` documents for `branch-protection.json`. Phase 3's three seams are the mitigation and they are NOT elimination: nothing writes entries automatically (Decision 2 defers that to its own request). If Phase 3 is trimmed under time pressure, the feature ships inert.
  - A NAIVE WHOLE-FILE PARSE MAKES AC 11 AND AC 9 MUTUALLY UNSATISFIABLE. The fenced worked example must parse under the same guards as real entries (AC 11) but sits in neither `## Open` nor `## Resolved` (AC 9), and its id would collide with the uniqueness guard (AC 8). Mitigation, prescribed in Phase 2: `parse_entries` takes TEXT, is called once on the fence-stripped body and once on the extracted example, and the example uses reserved id `E-000` that never appears outside the fence. An implementer who writes a single `parse(path)` will hit this and may 'fix' it by weakening AC 11, which would delete the only proof goal 1 has.
  - THE LINK-CHECKER EXTENSION CAN SWEEP THE WHOLE REPO. `_scanned_files()` (test_request_links.py:31-37) calls `rglob` on every entry of `SCANNED_TREES`. Adding `REPO_ROOT` to that tuple recursively scans `app/node_modules/`, `.venv/`, and `research/` — slow at best, spuriously red at worst. AC 18 means root-level files only: a separate non-recursive `REPO_ROOT.glob("*.md")`.
  - AN EXACT-SET ASSERTION ON ROADMAP IDS WILL FIRE IN UNRELATED PRs. Finding F-28 caught this and AC 2 deliberately asserts non-empty + known-good members + `>= 30` instead. A well-meaning implementer who 'tightens' it to `== 38` (the value I measured today) hands every future roadmap row a failing build. Likewise, hardcoding line ranges for AC 3 re-introduces the same brittleness — slice by heading anchor.
  - CASE MISMATCH SILENTLY FAILS THE GREEN FIXTURE. The roadmap heading reads `### Post-v1` (line 277) while AC 5's green fixture names `post-v1`. Compare casefolded, or the hermetic green case goes red for a reason that looks like a parser bug.
  - A PARTIAL STATUS ROLLOVER FAILS CI IN A CONFUSING WAY. `test_index_stage_cells_match_their_artifact_status_headers` (test_repo_structure.py:286-319) compares the Index cell against EVERY `*.md` in the item directory. Adding `IMPLEMENTATION_REPORT.md` at `implemented` while the other three still read `planned` fails with a message about the Index, not about the report. All four flip in one commit; precedent is 1.1, where all four artifacts read `implemented`.
  - THE STAGE TOKEN IS `planned`, NOT `plan`. The stage-3 SKILL.md template says `plan`; `requests/feature-requests/README.md:106` says the grammar is `intake → scoped → planned → implemented`; `_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:3-6` records that the repo's own contract wins and that the doc gate checks Index rows against these headers. Use `planned`.
  - THE CLAUDE.md BUDGET IS ONE LINE AND THE TEMPTATION IS THREE. Measured baseline 221 (`(Get-Content CLAUDE.md).Count`, 2026-08-15); AC 23 allows 222. The project-map fence line is the whole allowance — adding a prose bullet under 'Important locations' or 'How to help' blows it. Also do NOT measure with `Measure-Object -Line`: scope risk 15 records that `update-docs/SKILL.md:76-77` prescribes it and it under-reports.
  - THE LEAK GUARD IS VACUOUS BEFORE THE COMMIT. `tests/test_no_leaks.py` scans `git ls-files` (61-74). A green `uv run pytest` on an untracked `ESCALATIONS.md` proves nothing about AC 22. Re-run after `/commit`. And note the file is world-readable forever: `WINDOWS_PATH` (line 26) and `EMAIL` (line 29) catch paths and addresses, not indiscretion.
  - NO CI RUNS ON A FEATURE-BRANCH PUSH. `.github/workflows/ci.yml:3-7` triggers on push-to-main, pull_request, and workflow_dispatch only. Until the user opens the PR, local green is the ONLY signal — which is why every phase's gate is all four commands, not just pytest.
  - FRAGMENTATION IS UNMITIGATED EXCEPT BY PROSE. This is the sixth place a question can be parked, and `DESIGN.md` §4 (line 115, 'Open engineering questions') is the sharpest overlap — bullets with no ids and no named moment, literally what the queue is for minus the discipline. Only the header's register map separates them, and it is prose. Decision 6's written ADR-0011 trigger is the escalation path: the first proposal to absorb `[OPEN-N]` or `DESIGN.md` §4 is the second litigation, and the ADR gets written then.
  - ROADMAP RENUMBERING SILENTLY ORPHANS ENTRIES, WITH PRECEDENT. Item ids are hand-edited cells and ADR 0010 just deleted two columns from every table. A renumbering breaks `Bears on:` pointers — though the AC 6 guard turns that from silent into a red build, which is the actual protection.
  - ENTRY-ID COLLISIONS ACROSS BRANCHES (finding F-38). Sequential `E-NNN` in one file merges cleanly into duplicates. Unmitigated in v1 by decision: AC 8's uniqueness guard catches it at CI rather than preventing it. Acceptable while entries are few.
  - UNCONFIRMED — THE DRAIN CADENCE MAY NEVER FIRE. 'Drained at item boundaries' assumes every item boundary passes through `/make-feature-request`, which ADR 0010's entry condition does NOT guarantee for doc edits (`requests/README.md:68-70` exempts typo fixes, dependency bumps, and doc edits from the pipeline entirely). This is the assumption the whole cadence rests on, and it is labeled `unconfirmed` in the scope. The Phase 3 `/commit` drain check partially hedges it, because `/commit` runs on every commit regardless of pipeline entry.
  - SCOPE CREEP HAS A BINDING COST. `Blocks: 1.3` (ROADMAP.md:197) is binding, and 1.3 `correction-by-append` is the append-only correction model ADR 0003 treats as load-bearing. Every extra fold delays the ledger core. The deferred items — skills that WRITE entries (Decision 2), the `[OPEN-N]` mirror guard (Decision 4), ADR 0011 (Decision 6) — stay deferred.
- **files_to_touch:** - - **path:** ESCALATIONS.md
    - **change:** NEW, at the repo root. Boundary header (purpose · register map against the five other parking places · ADR-0011 trigger · `Blocks`-vs-parking line · the F-33 carve-out, both halves · the escalation name-collision line), the pinned six-field entry format, a fenced worked example with reserved id `E-000`, and `## Open` / `## Resolved` sections holding seven seed entries E-001..E-007.
  - - **path:** tests\test_escalations.py
    - **change:** NEW module (not an append to test_repo_structure.py — scope core item 5). Module docstring in the test_repo_structure.py:1-9 / test_layering.py:1-17 idiom; module-level regex constants; pure `known_moments` / `parse_entries` / `unknown_moments` / `violation_message`; AC 2-11 assertions including tmp_path red+green fixtures that fabricate BOTH files; AC 12/13 seed and no-duplicate-source substring tests; and (Phase 3) the three skill-seam substring tests.
  - - **path:** ROADMAP.md
    - **change:** Lines 217-222: replace the 'Parked, not scheduled' paragraph's enumeration of the three hypotheses with a pointer to ESCALATIONS.md, PRESERVING the observation that 'serviceable' is a v1 constraint in both directions and is nowhere defined or tested. Do NOT touch the H1 row at line 197 — /commit owns Status.
  - - **path:** .claude\skills\make-feature-request\SKILL.md
    - **change:** Step 2 'Ground it in the repo' (74-95, bullets at 79-91): add ONE bullet — when grounding a roadmap item, surface open ESCALATIONS.md entries whose `Bears on:` names this item's moment. AC 14 / Decision 1. Link as ../../../ESCALATIONS.md.
  - - **path:** .claude\skills\commit\SKILL.md
    - **change:** Step 4 'Roadmap status' (109-144, near the diff→status table at 118-122): add ONE bullet — when flipping a row to DONE, surface open entries naming that row. AC 15 / Decision 5, closing finding F-32's drain hole. Keep it to one bullet per the skill's lightweight posture.
  - - **path:** .claude\skills\diagnose-bug\SKILL.md
    - **change:** Escalation section (132-142) and/or the murky exit (161-162): add ONE line telling it to also park an ESCALATIONS.md entry naming the moment the bug bears on — a POINTER, not a migration; the (a)/(b)/(c) structure stays. Plus fold 8's one-line name disambiguation. AC 16 / Decision 3.
  - - **path:** tests\test_repo_structure.py
    - **change:** Two edits plus one new test. (1) `test_core_documents_exist` (243-246): add ESCALATIONS.md to the tuple and fix the docstring — it says 'three documents' over what is currently a five-name tuple (AC 17). (2) New `test_project_maps_list_the_escalation_queue` in the exact idiom of `test_claude_md_project_map_lists_both_new_directories` (276-283), asserting the queue is named in both CLAUDE.md and README.md (AC 19).
  - - **path:** tests\test_request_links.py
    - **change:** Extend `_scanned_files()` (31-37) with a NON-RECURSIVE `REPO_ROOT.glob("*.md")` for root documents — do NOT add REPO_ROOT to SCANNED_TREES (line 24), whose entries are rglob'd. Update the module docstring (1-16) to say root documents are scanned. Measured clean today: _dead_links over all five root docs returns []. AC 18.
  - - **path:** CLAUDE.md
    - **change:** Two one-line edits. (1) Inside the project-map fenced block (51-67), beside ROADMAP.md at line 53, add the ESCALATIONS.md row — inside the fence so it adds no link-check surface (AC 19). (2) Line 216: 'Nine decisions are settled' → 'Ten', matching lines 57, 90, 111 and the ten accepted ADRs at docs/decisions/README.md:43-52 (AC 20). Net +1 line: 221 → 222, at the AC 23 ceiling.
  - - **path:** README.md
    - **change:** Project-map fenced block (53-73): add one tree-glyph row for ESCALATIONS.md after the ROADMAP.md line at 58 (AC 19).
  - - **path:** requests\README.md
    - **change:** One line beside the three-track table (6-10) linking [ESCALATIONS.md] (../ESCALATIONS.md) — parked decisions live outside the tracks. Fold 7: the only pointer location the link checker already covers today, so the one pointer that cannot silently rot.
  - - **path:** requests\feature-requests\README.md
    - **change:** Index row for H1-escalation-queue (line 116): Stage cell → `planned` when the plan lands, → `implemented` at close-out, with the link repointed into `_done/H1-escalation-queue/` after the archive move.
  - - **path:** requests\feature-requests\H1-escalation-queue\IMPLEMENTATION_REPORT.md
    - **change:** NEW, Phase 5. Acceptance ledger over all 24 criteria with the command run and its result for each. AC 24 marked USER-RUN and left unclaimed.
  - - **path:** requests\feature-requests\H1-escalation-queue\FEATURE_REQUEST.md
    - **change:** Status blockquote rolled to `planned` then `implemented`, in lockstep with the sibling artifacts and the Index cell — test_index_stage_cells_match_their_artifact_status_headers compares the Index against every *.md in the directory.
  - - **path:** requests\feature-requests\H1-escalation-queue\PROJECT_SCOPE.md
    - **change:** Status blockquote rolled to `planned` then `implemented`, same lockstep. Content is DECIDED and is not otherwise edited.
- **code_references:** - - **ref:** ROADMAP.md:197
    - **claim:** The H1 row: `| H1 | `escalation-queue` | `ESCALATIONS.md`, the parked-decision format, and a structural test that every entry names a real roadmap item | — | 1.3 | IN-PROGRESS |`. VERIFIED — the `Blocks: 1.3` cell and the three literal deliverables are exactly as the scope states.
  - - **ref:** ROADMAP.md:217-222
    - **claim:** The 'Parked, not scheduled' paragraph naming the three seeds and the 'serviceable is nowhere defined or tested' observation AC 13 must preserve. VERIFIED at 217-222. NOTE the scope's Affected Area also cites this as ':216-221' — off by one; 217-222 is correct.
  - - **ref:** ROADMAP.md:200-204
    - **claim:** 'Why H1 exists' — eight decisions arriving synchronously in one sitting. CORRECTION: the scope cites ':199-203'; line 199 is blank and the paragraph runs 200-204.
  - - **ref:** ROADMAP.md:151-158, :176-188, :195-198, :232-241, :253-259, :268-275
    - **claim:** The five item tables plus the harness table (which carries the extra `Blocks` column). ALL SIX VERIFIED at the cited ranges. Measured: they yield exactly 38 unique ids.
  - - **ref:** ROADMAP.md:23-35, :41-51, :75-84, :324-334
    - **claim:** The four non-item tables AC 3 requires to contribute zero ids. VERIFIED, and MEASURED: their leading cells are `**Creation**`, `Surface`, `**Career management**`, `Empirically-derived archetypes`, `**[OPEN-2]**`… — bold or plain text, NOT empty, exactly as finding F-27 corrected. An id-pattern discriminator rejects all 57 of them.
  - - **ref:** ROADMAP.md:144, :163, :224, :246, :261, :277
    - **claim:** `### Phase 0 — Harness`, `### Phase 1 — Skeleton`, `### Phase 2 — Economy`, `### Phase 3 — Season`, `### Phase 4 — Career → v1`, `### Post-v1`. NEW GROUNDING the scope did not cite: these are the headings the widened moment vocabulary (fold 1) must harvest, so `Phase 3` and `post-v1` are roadmap-derived rather than hardcoded. Note `Post-v1` is capitalized — compare casefolded.
  - - **ref:** ROADMAP.md:334
    - **claim:** `| **[OPEN-10]** | Does the offseason training payout scale with age? | post-v1, with aging |`. VERIFIED, and the orphan is real: GAME_DESIGN.md §8 begins at line 290 and its last entry is `[OPEN-9]` at line 335. Seed 7's moment is therefore `post-v1`.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:859-861
    - **claim:** CORRECTED. The scope calls these lines 'the client-side router seam' at moment `1.10`. They actually read: 'Out of scope but worth recording before item 2.1: `.gitignore:63`'s blanket `build/` will silently shadow the `build/build-*.py` builder pattern CLAUDE.md prescribes for Phase 2. Should become an intake item rather than being rediscovered at 2.1.' Content = the gitignore shadowing risk; moment = 2.1, not 1.10. Duplicated at the same item's PROJECT_SCOPE.md:442-444.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369
    - **claim:** 'OpenAPI snapshot test | drop | Out — asserts nothing the health test doesn't; right moment is 1.8. Record the pointer in 1.8's request'. VERIFIED — seed 4, moment 1.8, and the row itself says the pointer was meant to be recorded.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:370
    - **claim:** 'OpenAPI→TypeScript codegen | drop | Out — machinery for one two-field object; the typed fetch wrapper is the seam it slots into at 1.8'. VERIFIED — seed 5, moment 1.8.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:318-319
    - **claim:** 'SPA history fallback for unmatched non-/api paths… It is what stops item 1.10's router from being a breaking change.' VERIFIED as a SHIPPED fold under 'Folded in (all of them — Decision 7)' at line 316 — confirming finding F-02 that this is not a stranded decision. Three strays, not four.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:3-6
    - **claim:** An HTML comment settling the token: 'Status token is `planned`, per the grammar at requests/feature-requests/README.md:100 … The create-implementation-plan SKILL.md template says `plan`; the repo's own contract wins.' VERIFIED — use `planned`.
  - - **ref:** requests/feature-requests/_done/1.1-app-shell/IMPLEMENTATION_PLAN.md:157-161
    - **claim:** 'Confirm the branch — do NOT create one. `git branch --show-current` must print … `git switch -c` here would fork a duplicate branch off the same tip.' VERIFIED — the Phase 0 idiom. Measured today: the current branch IS `phase1/escalation-queue`, tip 5ccec37 'Open the intake request for H1 escalation-queue'.
  - - **ref:** tests/test_layering.py:1-17
    - **claim:** Module docstring stating why the guard is a pure function proven against tmp_path: lines 12-16 say the obvious alternative (add the import, revert it) 'is not available: subagents have read-only git'. VERIFIED — this is the idiom AC 1 and AC 5 require.
  - - **ref:** tests/test_layering.py:85-100
    - **claim:** `test_a_web_import_is_reported` and `test_a_clean_tree_reports_nothing` — the tmp_path red/green pair AC 5 mirrors, each writing fake files and asserting an exact return value. VERIFIED.
  - - **ref:** tests/test_layering.py:70-79 and :112-123
    - **claim:** `violation_message()` and `test_the_failure_message_cites_the_architecture_record` — the 'failure message is the record' pattern AC 7 mirrors, including asserting the message on a fabricated violation. VERIFIED.
  - - **ref:** tests/test_repo_structure.py:243-246
    - **claim:** `test_core_documents_exist` with docstring 'The three documents that carry the design, each doing one job.' over a FIVE-name tuple (GAME_DESIGN.md, ROADMAP.md, DESIGN.md, README.md, CLAUDE.md). VERIFIED — the docstring already lies, which is Decision 7's answer to the minimalist dissent. AC 17 makes it six.
  - - **ref:** tests/test_repo_structure.py:260-273 and :276-283
    - **claim:** `test_no_document_still_claims_there_is_no_application` and `test_claude_md_project_map_lists_both_new_directories` — the substring-assertion idiom AC 12 and AC 19 copy. VERIFIED.
  - - **ref:** tests/test_repo_structure.py:286-319
    - **claim:** `test_index_stage_cells_match_their_artifact_status_headers` compares the Index Stage cell against EVERY `*.md` in the item dir. VERIFIED — this is why all four H1 artifact headers must flip in lockstep with the Index cell, and it is the same 'state stored once' invariant AC 9 applies to Status-vs-section.
  - - **ref:** tests/test_request_links.py:24 and :31-37
    - **claim:** `SCANNED_TREES = (REPO_ROOT / "requests", REPO_ROOT / ".claude" / "skills")` and `_scanned_files()` calling `tree.rglob("*.md")` per tree. VERIFIED — adding REPO_ROOT to that tuple would rglob node_modules/.venv/research, so AC 18 needs a separate non-recursive root glob.
  - - **ref:** tests/test_request_links.py:26 and :40-64
    - **claim:** `FENCED_BLOCK` regex (reused by the new parser to strip the worked example) and `_dead_links`, which strips a `file.py:123` suffix at line 57 and skips var/ at 58. MEASURED 2026-08-15 by importing `_dead_links` and running it over all five root docs: CLAUDE.md [], DESIGN.md [], GAME_DESIGN.md [], README.md [], ROADMAP.md []. AC 18 costs zero cleanup — independently confirmed.
  - - **ref:** tests/test_no_leaks.py:61-74
    - **claim:** `_tracked_text_files()` builds its list from `git ls-files -z`. VERIFIED — an untracked new .md is invisible to the leak guards, so AC 22's coverage of ESCALATIONS.md only becomes real after /commit stages it.
  - - **ref:** pyproject.toml:80-84
    - **claim:** `[tool.mypy] strict = true`, `warn_unreachable = true`, `files = ["src", "tests"]`. VERIFIED — the new test module is strictly type-checked, which is why AC 21 forbids new `# type: ignore`.
  - - **ref:** pyproject.toml:53-77
    - **claim:** ruff `line-length = 100`, `target-version = "py312"`, select includes `I` (isort), `N`, `B`, `A`, `C4`, `DTZ`, `PTH`, `RUF`. VERIFIED — `PTH` is why the parser must use `Path.read_text` rather than `open()`.
  - - **ref:** .github/workflows/ci.yml:3-7, :19, :37, :40
    - **claim:** Triggers are `push: branches: [main]`, `pull_request`, `workflow_dispatch` — NO feature-branch push trigger; job display name is `Lint, types, tests`; `ruff check` at 37 and `ruff format --check` at 40 are separate steps. VERIFIED — confirming non-goal 11 (tests join the existing job, branch-protection untouched) and that local green is the only pre-PR signal.
  - - **ref:** .claude/skills/commit/SKILL.md:109-144
    - **claim:** Step 4 'Roadmap status' — 'This skill owns keeping those true', the diff→status table at 118-122, the never-mark-ahead / never-mark-down rails at 133-138, and 'Stage ROADMAP.md by path if you edited it' at 143-144. VERIFIED — the drain-hole bullet (AC 15) belongs here because Step 4 is the one thing running at the moment a row flips to DONE.
  - - **ref:** .claude/skills/make-feature-request/SKILL.md:74-95
    - **claim:** Step 2 'Ground it in the repo', bullets at 79-91 including the five-dataset-contracts bullet and the 'obvious constraints from the project rules' bullet at 90-91. VERIFIED — the read seam (AC 14) is one more bullet in this list. NOTE the scope cites ':74-92'; the section actually runs 74-95 (Step 3 starts at 97).
  - - **ref:** .claude/skills/diagnose-bug/SKILL.md:132-142 and :161-162
    - **claim:** The murky-cause Escalation template with its (a) Why murky / (b) Verdict / (c) Follow-up structure, and the 'Murky → the Escalation section is the deliverable; the bug waits' exit. VERIFIED — AC 16 adds a pointer line and does NOT replace this structure (Decision 3).
  - - **ref:** docs/decisions/README.md:20 and :26-34
    - **claim:** 'Status — proposed / accepted / superseded by NNNN' and the Rules section: 'Don't edit an accepted ADR to reflect a change of mind… the value here is the record of what was believed at the time.' VERIFIED — the immutability ethos the resolved-entries-stay rule borrows by ANALOGY. ADR 0003's append-only rule governs careers/**/events.jsonl only and is not engaged here.
  - - **ref:** docs/decisions/README.md:43-52
    - **claim:** Ten accepted ADRs indexed, 0001 through 0010. VERIFIED — confirming CLAUDE.md:216's 'Nine decisions are settled' is the measured drift AC 20 fixes, contradicted by CLAUDE.md lines 57, 90, and 111.
  - - **ref:** CLAUDE.md:51-67
    - **claim:** The project-map fenced block; line 53 is `ROADMAP.md          v1 boundary, five phases, 36 work items`. VERIFIED. Total file length MEASURED at 221 lines via `(Get-Content CLAUDE.md).Count`, confirming AC 23's baseline exactly.
  - - **ref:** README.md:53-73
    - **claim:** The `## Project map` fenced block using tree glyphs, with `├── ROADMAP.md         v1 boundary, five phases, 36 work items` at line 58 and `└── var/` at 72. VERIFIED — inside a fence, so the added row adds no link-check surface once AC 18 lands.
  - - **ref:** requests/README.md:6-10
    - **claim:** The three-track table (feature-requests / bugfix-requests / calibration-findings). VERIFIED — fold 7's one-line pointer goes beside it, and requests/ is already scanned by the link checker, making it the one pointer that cannot silently rot.
  - - **ref:** requests/feature-requests/README.md:104-110 and :112-117
    - **claim:** The status blockquote grammar (`intake → scoped → planned → implemented`, line 106) and the Index table whose H1 row is line 116, currently `scoped`. VERIFIED — the token is `planned`, not `plan`.
  - - **ref:** ops/README.md:30-37
    - **claim:** 'This file is inert until it is re-applied. Editing branch-protection.json changes nothing on GitHub…' VERIFIED — the named failure mode the inertness risk borrows. NOTE the scope cites ':20-37', which spans two blockquotes; the inertness one is precisely 30-37.
  - - **ref:** DESIGN.md:50 and :115
    - **claim:** `## 2. Open mechanism work` and `## 4. Open engineering questions`. VERIFIED — the two registers the boundary header must disambiguate against, §4 being the sharpest overlap (bullets with no ids and no named moment).
  - - **ref:** .gitignore:62-63
    - **claim:** `dist/` and `build/` as blanket rules matching at any depth. VERIFIED — this is the substance of corrected seed 6, whose named moment is item 2.1 `datasets-layer` (the row that introduces `lib/paths.py` and the builder pattern, ROADMAP.md:234).
  - - **ref:** filesystem probe, 2026-08-15
    - **claim:** `datasets/` False, `careers/` False, `rulesets/` False, `lib/` False, `build/` False, `ESCALATIONS.md` False, `docs/data-sources.md` False (the repo's equivalent is `docs/data-access.md`, which exists); `src/rpg_core/` contains only `__init__.py`. VERIFIED — the five dataset contracts, resolve-by-name, ADR 0003's append-only rule and ADR 0004's immutability rule are all genuinely N/A here, recorded rather than invented.
- **open_questions:** - THE THIRD RECOVERED STRAY IS MIS-DESCRIBED IN THE SCOPE — confirm the correction before Phase 1 writes it. `PROJECT_SCOPE.md:232` says 'the client-side router seam (`IMPLEMENTATION_PLAN.md:859-861`, moment 1.10)'. Those lines are actually the `.gitignore:63` blanket-`build/` rule that will shadow the Phase 2 `build/build-*.py` builder pattern, moment `2.1` (duplicated at `_done/1.1-app-shell/PROJECT_SCOPE.md:442-444`). Plan's default: seed the gitignore/2.1 item and note the correction in the entry's `Source:`. Alternative if you disagree: seed a router-seam entry at 1.10 anyway — but note finding F-02 already ruled the router mention (`PROJECT_SCOPE.md:318-319`) a shipped fold, so there would be nothing stranded to record.
  - TWO SEEDS HAVE NO MOMENT ANYWHERE IN THE REPO. `ROADMAP.md:217-222` claims each hypothesis has 'a named moment to re-decide' but names none. The serviceability gate maps cleanly to `Phase 3` (whose Exit at ROADMAP.md:248 is 'the app is genuinely serviceable'). The design/UX specialist and the autonomous stage dispatcher do not. Plan's defaults: design/UX specialist → `Phase 3`; autonomous stage dispatcher → `H2` (the `domain-engineer` row at ROADMAP.md:198 is the project's only agent-delegation item). Both would be labeled `inferred` in their own `Assumed:` field. Redirect either if you had a different moment in mind — this is the queue's first content and it sets the tone for what a 'moment' means.
  - THE WORKED EXAMPLE'S RESERVED ID. Plan's default is `E-000` inside the header's fence, which keeps AC 11 (the example parses under the real guards) compatible with AC 8 (ids unique) and AC 9 (every entry sits in an Open/Resolved section agreeing with its Status). Alternative: a non-numeric placeholder like `E-NNN`, which reads more obviously as a template but then CANNOT satisfy AC 11's 'parses green under the same regex' without loosening the id pattern — which would weaken the guard for every real entry.
  - THE CLOSED `Status:` VOCABULARY. Plan's default is exactly two values, `open` and `resolved`, matching the two sections the format defines. A third value (`superseded`) was considered and the scope explicitly DROPPED a `Supersedes:` field as 'real in principle, no instance yet' — so two is the consistent reading. Confirm, because AC 8 pins the vocabulary and every later entry inherits it.
  - AC 3's PROOF SHAPE. The criterion names line ranges (`ROADMAP.md:23-35`, `:41-51`, `:75-84`) as the tables that must contribute zero ids. The plan proves exactly that against the real file but slices it by HEADING ANCHOR (`### In scope — the engine`, `### In scope — the application`, `### Out of scope`, `## Open questions…`) rather than by hardcoded line number, because hardcoded ranges are the same brittleness finding F-28 rejected for the id set. Flagging it as a deliberate faithful-intent reading, not a silent substitution.
