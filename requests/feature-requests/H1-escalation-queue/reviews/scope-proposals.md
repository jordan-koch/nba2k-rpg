# Scoping Panel — Raw Proposals

> Verbatim output of the three divergent scopers. Kept unfiltered as the
> provenance trail: what was considered, separate from what was kept.


---

## Scoper 1

- **scoper:** fit
- **ok:** True
- **fit:** - **verdict:** clean
  - **rationale:** Clean fit, with one honest friction named at the end. Four grounded reasons.

(1) It has a reserved slot. ROADMAP.md:197 is the H1 row — `escalation-queue`, deliverable "ESCALATIONS.md, the parked-decision format, and a structural test that every entry names a real roadmap item", Blocks: 1.3, Status IN-PROGRESS. ROADMAP.md:200-204 argues why it exists. The roadmap already sequenced it and made it binding on 1.3.

(2) It touches nothing architecturally load-bearing — verified, not assumed. No dataset: datasets/manifest.json does not exist (item 2.1, ROADMAP.md:234), so the five data contracts (grain/keys/coverage/update-semantics/pull-cost) are N/A. No ledger and no economy: careers/ and rulesets/ do not exist on disk (verified by a repo-root listing), so ADR 0003's append-only history and ADR 0004's pinned-ruleset immutability are not engaged — no ruleset version changes, no career replays, no recorded history shifts. None of the ten ADRs is contradicted. Note: docs/data-sources.md named in the task brief does not exist; the real file is docs/data-access.md, and this item does not touch it or its epistemic labels.

(3) The substrate exists and is idiomatic. This is process-and-docs work plus one structural guard — the layer Phase 0 built. Two live precedents: tests/test_repo_structure.py:220-240 (test_every_adr_is_listed_in_the_index, test_adr_numbers_are_unique_and_contiguous) parse a document and assert it against the filesystem; tests/test_layering.py:38-123 is the closer match for a parser-based guard — a pure function (web_imports_under), proven red AND green against tmp_path fakes, plus test_the_failure_message_cites_the_architecture_record. Its docstring at lines 12-16 states why that idiom exists: subagents have read-only git, so a guard cannot be proven by editing tracked source and reverting.

(4) The failure mode is already measurable in this repo. [OPEN-10] is cited in ROADMAP.md:334 and defined NOWHERE — GAME_DESIGN.md section 8 stops at [OPEN-9] (line 335), and a repo-wide grep over *.md finds no other definition. CLAUDE.md and the FEATURE_REQUEST both claim section 8 "parks ten design questions"; it parks nine. That is measured, and it is exactly the rot H1's structural test prevents: an unenforced pointer into a hand-edited table goes stale silently.

The layer question, answered: NOT the domain core, NOT the API, NOT the app, NOT a builder. src/rpg_core/ is the I/O-free domain (DESIGN.md:106-109, enforced by tests/test_layering.py); the queue has no domain semantics, is not folded from events, and has no consumer that is a program. The request's own "Not now/later" bullet already forecloses programmatic reads. The correct homes are a tracked markdown document plus a test under tests/.

The friction (why this is not a free 'clean'): the repo already has FIVE places a question can be parked, and this adds a sixth. Measured: (a) GAME_DESIGN.md section 8 [OPEN-N], indexed by phase in ROADMAP.md:324-334; (b) DESIGN.md section 2 "Open mechanism work" (lines 50-98, each tied to a phase item); (c) DESIGN.md section 4 "Open engineering questions" (lines 115-127, four bullets with NO ids and NO named moment — the closest existing thing to what H1 proposes); (d) ADR 0010's required "Stage plan" section, enforced by tests/test_repo_structure.py:159-217; (e) .claude/skills/diagnose-bug/SKILL.md:132-142's Escalation section. Fragmentation is the real risk, and the boundary rule is the load-bearing deliverable — not the file. This is a scoping decision the request explicitly hands over (Open Question 1), not a reshape of the feature, so the verdict stays clean.
- **goals:** - Give a parked decision a durable, tracked home with a pinned entry format, so that recording an assumption is available as a third move beside 'interrupt the user' and 'guess silently'. The format is the load-bearing artifact — every entry written after it inherits it.
  - Draw an explicit, written boundary against the five parking mechanisms that already exist (GAME_DESIGN.md section 8 [OPEN-N]; DESIGN.md section 2 and section 4; ADR 0010's Stage plan section; diagnose-bug's Escalation section), so the project does not end up with two overlapping places to look for the same question. The boundary lives in the queue's own header where a worker reads it, not only in this scope.
  - Make the pointer mechanical rather than aspirational: a structural test that parses every entry's roadmap item id and asserts it exists in ROADMAP.md's phase tables, in the tests/test_layering.py idiom (pure parser, proven both ways against tmp_path, failure message that cites the rule). ROADMAP.md:334's orphaned [OPEN-10] is the measured proof that an unenforced pointer rots.
  - Discharge the three hypotheses currently held only in ROADMAP.md:217-222 prose ('a serviceability gate for the web UI, a design/UX specialist, and an autonomous stage dispatcher') into real entries, each naming the roadmap item that is its re-decide moment — and leave the paragraph pointing at the queue rather than carrying the content.
  - Give the queue at least one seam that actually reads it, so it is not the inert-config failure ops/README.md:30-37 documents ('This file is inert until it is re-applied'). The drain cadence the user settled — at roadmap item boundaries — makes /make-feature-request Step 2 ('Ground it in the repo', SKILL.md:74-95) the natural read point, since every roadmap row becomes one request.
  - Unblock ROADMAP.md:197's Blocks cell so item 1.3 correction-by-append can start, and start by reading what was parked against it.
- **non_goals:** - Absorbing or restructuring [OPEN-N]. GAME_DESIGN.md section 8 plus its phase index at ROADMAP.md:324-334 stays exactly as it is. Settled by the request's Scope Signals; the widest boundary in Open Question 1 is rejected here.
  - Absorbing DESIGN.md section 2 'Open mechanism work' (lines 50-98) or section 4 'Open engineering questions' (lines 115-127). CLAUDE.md's map assigns DESIGN.md one job — 'Open engineering work — unsettled mechanism math only' — and Phase 0's exit condition was 'every document doing one job' (ROADMAP.md:147). Moving them would re-open a completed phase's exit.
  - Changing ADR 0010's Stage plan mechanism. It is a per-request decision recorded in the request and already enforced twice (tests/test_repo_structure.py:159 and :185). A queue entry duplicating it would create two records of one decision — the exact failure this item exists to prevent.
  - Any code under src/rpg_core/, src/rpg_api/, or app/. No endpoint, no DTO, no UI, no read-model. src/rpg_core/ is still __init__.py alone (verified); this item must not be the thing that puts non-domain concepts in it.
  - Any blocking semantics. User-settled at intake: parking never stops work. Nothing in the test, the format, or the wiring may make an unanswered entry fail CI or halt a stage.
  - Any priority, urgency, severity, or owner field. User-settled: the drain is at item boundaries, so the roadmap item id is the sort key and urgency has no job.
  - Programmatic consumption of the queue — an API route, a rpg_core parser importable at runtime, a UI surface, or a datasets/ registration. The only program that reads it is the pytest guard.
  - Auto-writing: teaching /scope-feature, /create-implementation-plan, or /implement-plan to emit entries for their gated calls. That is the largest option in Open Question 2 and is tiered as an enhancement, not core.
  - A resolved-entry archive or a _done/ split. Parked by the request; revisit when the file is long enough to be annoying, which is a measurable trigger rather than a guess.
  - Entries that carry a decision's rationale. An entry points at the ADR, request, or commit that decided it; the rationale lives there. Parked by the request.
  - Fixing the substance of any parked decision. Migrating the three ROADMAP.md hypotheses means recording them, not deciding them.
  - Committing or merging. This lands on a branch through /commit; the PR and merge stay the user's.
- **acceptance_criteria:** - `uv run pytest tests/test_escalations.py -q` is green, containing a pure function that parses roadmap item ids out of ROADMAP.md's phase tables anchored on the leading cell, and returns exactly the 38 ids on disk today: 0.1-0.6 (ROADMAP.md:153-158), 1.1-1.11 (:178-188), H1-H2 (:197-198), 2.1-2.8 (:234-241), 3.1-3.5 (:255-259), 4.1-4.6 (:270-275). Asserted as an exact set, not a count.
  - The same test proves the parser rejects the two non-item table shapes in the same file: rows whose leading cell is empty (the v1 scope tables, ROADMAP.md:25-35 and :77-84) and rows whose leading cell is `**[OPEN-N]**` (the open-questions table, :326-334) contribute zero ids. A negative assertion, so a loosened regex fails rather than silently widening.
  - The entry-parser is proven red AND green against `tmp_path` without mutating any tracked file, in the tests/test_layering.py:85-100 idiom: a fabricated queue naming item `9.9` returns exactly that one violation, and a fabricated queue naming `1.3` and `H2` returns none. Required because subagents have read-only git (tests/test_layering.py:14-16).
  - A real assertion runs the parser over the actual queue file and finds zero violations — every entry names a roadmap item id that exists in ROADMAP.md.
  - A test asserts the failure message names the queue file, the offending entry id, and the offending item id, and states the rule (an entry must name a real roadmap item, because the queue is drained at item boundaries). Mirrors tests/test_layering.py:112-123, where the message IS the record.
  - A test asserts every entry id in the queue is unique and every status value is drawn from a closed vocabulary declared in the test, so a typo'd status is a failure rather than a silently new state.
  - `uv run pytest tests/test_repo_structure.py -q` is green with `test_core_documents_exist` (currently tests/test_repo_structure.py:243-247) extended to include the queue file, so deleting it fails the build rather than silently emptying the mechanism.
  - `uv run pytest tests/test_request_links.py -q` is green with the queue file inside the checker's scanned set (today `SCANNED_TREES` at tests/test_request_links.py:24 covers only `requests/` and `.claude/skills/`), so every relative pointer an entry carries — to an ADR, a request directory, a roadmap section — is verified to resolve.
  - A structural test asserts ROADMAP.md's 'Parked, not scheduled' paragraph (currently :217-222) names the queue file, so the prose and the queue cannot drift apart; and that the three hypothesis names it lists each appear as an entry in the queue.
  - A structural test asserts CLAUDE.md's project map and README.md's project map (README.md:55-73) both list the queue file — the tests/test_repo_structure.py:276-284 idiom, which exists because 'the docs describe the repo' otherwise has no pass/fail check.
  - A structural test asserts `.claude/skills/make-feature-request/SKILL.md` names the queue file, in the exact idiom of tests/test_repo_structure.py:159-182 (`test_both_intake_templates_carry_a_stage_plan_section`). This is the only mechanical evidence that the wiring is not inert, and it is what distinguishes this build from the ops/README.md:30-37 failure the request cites.
  - `uv run ruff check`, `uv run ruff format --check`, and `uv run mypy` all exit 0 with the new test file present. mypy is strict with `files = ["src", "tests"]` (pyproject.toml:80-84), so the new parser is fully annotated with zero new `# type: ignore` and zero new per-module overrides.
  - `uv run pytest tests/test_no_leaks.py -q` is green with the queue file tracked — it records real decisions and real assumptions in a PUBLIC repo, and `.md` is scanned. No drive letters, no home directories, no email addresses.
  - `uv run pytest` is green overall — in particular tests/test_repo_structure.py:185-217 (`test_every_live_intake_artifact_declares_a_stage_plan`) and :286-319 (`test_index_stage_cells_match_their_artifact_status_headers`) still pass with this request's artifacts advanced to `scoped`.
  - USER-RUN — the drain works at a real boundary. Opening item 1.3's intake surfaces the entries parked against 1.3 without the user being asked anything, and the user can answer an entry by editing the queue alone, with no build running. Marked user-run because only a human can judge whether it arrived at the right moment.
- **core_scope:** - The queue file itself — `ESCALATIONS.md` at the repo root, the name ROADMAP.md:197 already commits to. Root over docs/ because discoverability is the entire mechanism: the three hypotheses failed precisely by being somewhere nobody looks. The cost is honest and named — it becomes the sixth root document beside GAME_DESIGN.md / ROADMAP.md / DESIGN.md / README.md / CLAUDE.md, all of which tests/test_repo_structure.py:243 pins. Flagged for the user as Open Question 1, with docs/escalations.md and requests/ESCALATIONS.md as the alternatives.
  - A boundary header at the top of the file, written for the worker who is about to add an entry: this queue holds BUILD-TIME and PROCESS decisions that have a named roadmap moment. A game-design question is [OPEN-N] in GAME_DESIGN.md section 8. Unsettled mechanism math is DESIGN.md section 2. An open engineering question with no named moment is DESIGN.md section 4. A stage-skip argument is the request's own Stage plan section (ADR 0010). A murky root cause is the RCA's Escalation section. This paragraph is the deliverable that keeps the sixth parking place from being a sixth problem.
  - The entry format, pinned deliberately because every later entry inherits it. Five fields: a stable entry id; a REQUIRED roadmap item id (the re-decide moment); the question in one line; the assumption actually taken while proceeding, or an explicit 'none — not hit yet'; and a status from a closed vocabulary with a resolution pointer when resolved. The assumption field is the half the request identified as the quieter failure — a decision made by default currently leaves no trace.
  - Hard-require the roadmap item id — no `unscheduled` escape hatch. Two grounded reasons: the drain cadence makes an unmoored entry structurally invisible (the request's own Constraints section makes this argument), and the repo ALREADY has two homes for a genuinely unmoored question (DESIGN.md section 4, GAME_DESIGN.md section 8) so no new hatch is needed. Measured supporting evidence: ROADMAP.md:334's [OPEN-10] is cited with no definition anywhere, which is what an unenforced pointer becomes.
  - Resolved entries STAY in the file with a resolution pointer, rather than being deleted. Mirrors the repo's immutability ethos — docs/decisions/README.md:26-31 ('Don't edit an accepted ADR to reflect a change of mind... The value here is the record of what was believed at the time'). Labelled honestly: this is an ETHOS analogy, not an application of ADR 0003, whose append-only rule governs careers/**/events.jsonl and nothing else.
  - Migrating the three ROADMAP.md:217-222 hypotheses into entries: the web-UI serviceability gate, the design/UX specialist, and the autonomous stage dispatcher. Each gets a roadmap item id for its re-decide moment. The serviceability gate has an unusually concrete one — ROADMAP.md:220-222 notes 'serviceable' is a v1 constraint in both directions (ROADMAP.md:53-56) and is 'nowhere defined or tested', which points at Phase 3. The paragraph is rewritten to point at the queue instead of carrying the content.
  - `tests/test_escalations.py` — a new test module rather than an append to tests/test_repo_structure.py. Reasoning, and a considered divergence from the request's pointer: the H1 guard is parser-based, and tests/test_layering.py is the repo's precedent for that shape (pure function, tmp_path proofs both ways, a test asserting the failure message carries the rule). test_repo_structure.py holds substring and filesystem assertions with no pure functions and no tmp_path fixtures; the ADR tests the request names are its closest analogues but are five-line assertions, not parsers. Precedent also exists for splitting a parser guard out: test_request_links.py and test_ci_contexts.py are both standalone.
  - Extending `test_core_documents_exist` (tests/test_repo_structure.py:243-247) to include the queue, and adding the queue to `test_request_links.py`'s scanned set (line 24). Together these mean the file cannot vanish and its pointers cannot rot.
  - Discoverability wiring: CLAUDE.md's project map plus one rule line, and README.md's project map row (README.md:55-73). Constraint measured: CLAUDE.md is 181 lines against update-docs/SKILL.md:76-79's hard 200-line budget, so there are 19 lines of headroom — this is an edit within budget, not an append that blows it.
  - One read seam, so the artifact is not inert: a bullet in `/make-feature-request` Step 2 'Ground it in the repo' (SKILL.md:74-95) telling intake to check the queue for entries naming this item's roadmap id. This is the exact moment the request's observable signal describes ('item 1.3 can start by reading the queue'). NAMED DIVERGENCE: the request's Scope Signals list 'Not a change to how the pipeline skills behave', qualified as 'no skill's decision logic changes here'. This adds a READ to a grounding step and changes no decision logic — but it is a change to a skill file, so it is called out rather than folded in quietly, and it is the user's to cut.
  - ROADMAP.md row H1 advanced by /commit against the diff, never hand-edited (ROADMAP.md:139-142, .claude/skills/commit/SKILL.md:109-144).
- **enhancements:** - - **title:** Fix the measured [OPEN-10] orphan and add the mirror guard
    - **rationale:** MEASURED: ROADMAP.md:334 cites [OPEN-10] ('Does the offseason training payout scale with age?'); GAME_DESIGN.md section 8 stops at [OPEN-9] (line 335); a grep for OPEN-10 across all tracked *.md returns only ROADMAP.md:334. CLAUDE.md and this FEATURE_REQUEST both state section 8 parks ten questions — it parks nine. This is the exact class of rot H1's test prevents, sitting live in the mechanism H1 is told not to touch. The guard is a near-copy of the entry parser (every [OPEN-N] cited in ROADMAP.md's table is defined in GAME_DESIGN.md section 8), so the marginal cost is one function. Fixing the orphan itself is a doc decision the user should make, not the agent.
    - **cost:** cheap
  - - **title:** Give diagnose-bug's murky-cause Escalation somewhere to go
    - **rationale:** The request names this as a name collision and possible overlap. Verified: .claude/skills/diagnose-bug/SKILL.md:136-142 writes an Escalation section whose follow-up is 'hand-investigating the candidates above, or standing up that deferred panel', and SKILL.md:161-162 exits with 'the bug waits for the follow-up'. Nothing surfaces that wait again. The middle boundary from Open Question 1 is available WITHOUT absorbing content: the RCA keeps its Escalation section (that is the evidence trail), and gains one line telling it to park a POINTER entry naming the roadmap item the bug blocks. Two mechanisms, one index, no duplicated content. Cheap because it is one line in one skill plus one entry-format note.
    - **cost:** cheap
  - - **title:** Drain at the other item boundary — /commit Step 4
    - **rationale:** .claude/skills/commit/SKILL.md:109-144 is the only step in the pipeline guaranteed to run when work actually lands, which is why it already owns ROADMAP.md statuses ('Nowhere else in the pipeline is guaranteed to run at the moment work actually lands, so if the gate doesn't advance them, they rot' — SKILL.md:113-114). The identical argument applies to draining the queue: when a row moves to DONE, open entries against it are exactly the thing to surface. Held out of core because SKILL.md:28-30 says 'Keep this skill lightweight. It is a gate, not a pipeline', so this is a real judgment call rather than an obvious win.
    - **cost:** cheap
  - - **title:** Teach the panel skills to WRITE parked entries for their gated calls
    - **rationale:** The largest option in Open Question 2, and the request calls it 'the real payoff'. requests/README.md:37-47's standing principle is 'Greedy, but gated' — scope-growing calls get 'tiered and deferred for your decision'. Today that deferral lives inside one artifact's Gated table and is read once; parked as a queue entry it would resurface at the item boundary. Grows-build because it touches four skill files, needs a write format that will not fight concurrent panel lanes, and risks flooding a queue whose whole value is that it is short enough to read. Right after the format has survived a few hand-written entries, not before.
    - **cost:** grows-build
  - - **title:** An ADR recording the boundary between the five-going-on-six parking mechanisms
    - **rationale:** The boundary is the most expensive thing here to get wrong (the request says so, and it is why trigger 1 fired). An ADR is the repo's instrument for a call that is expensive to re-litigate. Counter-precedent is real and recent: the 1.1 scope's Decision 3 chose a guard test OVER a tenth ADR on the grounds that 'a test failing the build is a stronger record than prose', and tests/test_layering.py:8-10 records that. The queue's boundary is prose-shaped and cannot be tested, which is the argument FOR the ADR. Genuinely two-sided; the user's call. Cheap in tokens, but it permanently adds ADR 0011 to a contiguity-checked set.
    - **cost:** cheap
  - - **title:** A resolved-entry archive once the file gets long
    - **rationale:** Parked by the request as 'not now'. The idiom exists — requests/feature-requests/README.md:93-100's _done/ convention, exempted from the link checker at tests/test_request_links.py:36. Worth a written trigger (e.g. when open entries no longer fit one screen) rather than a guess now, so the split happens on evidence.
    - **cost:** grows-build
- **risks:** - FRAGMENTATION — the headline risk. This makes a sixth place to park a question (GAME_DESIGN.md section 8; DESIGN.md section 2; DESIGN.md section 4; ADR 0010 Stage plan; diagnose-bug Escalation; now the queue). DESIGN.md section 4's four bullets (lines 115-127) are the sharpest overlap: they are open questions with no ids and no named moment — literally what the queue is for, minus the discipline. The boundary header mitigates but cannot enforce; nothing mechanical can tell a build-time decision from an engineering question. Cost accepted, not solved.
  - INERTNESS — the request names it: ops/README.md:30-37's 'This file is inert until it is re-applied'. At the roadmap's literal three deliverables nothing reads the queue, and a queue nobody opens is strictly worse than the prose it replaced, because it looks like a solved problem. The single read seam in core is the minimum antidote; cutting it should be a deliberate decision, not an omission.
  - ROADMAP RENUMBERING TAX. Item ids are hand-edited cells in a hand-edited table, and rows do get added — H1 and H2 are recent, and ADR 0010 deleted two whole columns. Once entries pin ids, renumbering or deleting a row turns CI red until every entry is updated. That is the guard working as designed, but it is a real new coupling between ROADMAP.md and the queue, and it lands on whoever next restructures the roadmap.
  - PARSER BRITTLENESS. ROADMAP.md mixes four table shapes in one file: item rows with an id in the leading cell, v1 scope rows with an EMPTY leading cell (:25-35, :77-84), the harness table with an extra Blocks column (:195-198), and the open-questions table leading with `**[OPEN-N]**` (:326-334). A leading-cell-anchored pattern separates them today (verified by reading all four), but a hand edit that adds a fifth shape will either drop ids silently (false green) or swallow non-items. The negative assertion in the acceptance criteria is the guard against the second; the exact-set assertion is the guard against the first.
  - SCOPE CREEP INTO THE INTAKE TRACKS. A parked DECISION and a parked piece of WORK look similar and are not. requests/README.md:6-12 already draws three tie-breaks (missing = feature, broken = bug, ran-clean-but-wrong = calibration); the queue needs a fourth that keeps it from becoming a shadow backlog. Symptom to watch for: an entry whose 'assumption taken' field is empty because nothing was actually being built.
  - CLAUDE.md LINE BUDGET. Measured at 181 lines against the hard 200 in update-docs/SKILL.md:76-79 ('Over budget means cutting, not reformatting'). The map row plus a rule line fits, but H2 and the rest of Phase 1 will also want lines. If the queue's rules need a paragraph rather than a line, something else in CLAUDE.md gets cut in the same commit.
  - PUBLIC REPO. Entries record real decisions and, by design, the assumptions taken when nobody was sure. That is now world-readable forever (CLAUDE.md's repo-is-public section). tests/test_no_leaks.py blocks drive letters, home directories, and emails, but it cannot block an entry that is simply more candid than the user wants published. Worth one line in the queue header.
  - OVER-BUILDING THE WIRING DELAYS 1.3. ROADMAP.md:197's Blocks cell is binding: item 1.3 correction-by-append cannot start until this lands, and 1.3 is the item ROADMAP.md:310-312 flags as the one where 'the replay guarantee is decorative' if it slips. Every enhancement adopted into core pushes the ledger's correction model out. The core scope above is deliberately the smallest thing that is not inert.
  - THE FORMAT IS PINNED BY EVERY LATER ENTRY. The request's own trigger-3 argument. A field that turns out to be missing (owner, date, superseded-by) is cheap to add at three entries and annoying at thirty. Countervailing pressure: the settled non-goals already rule out the fields most likely to be missed (priority, urgency, rationale), so the surface for regret is small.
  - UNCONFIRMED — whether the drain cadence actually fires. 'Drained at roadmap item boundaries' assumes every item boundary passes through /make-feature-request. That holds while every roadmap row becomes one request (requests/feature-requests/README.md:34-40), but ADR 0010's entry condition (docs/decisions/0010-panels-by-default.md:63-66) exempts typo fixes, dependency bumps, and doc edits from the pipeline entirely. Work that never enters intake never drains the queue. Only observation over the next few items settles this.
- **open_questions:** - WHERE DOES THE FILE LIVE? Root `ESCALATIONS.md` (roadmap-literal, maximum discoverability, but a sixth root document and outside tests/test_request_links.py's scanned trees today) vs `docs/escalations.md` (docs/ already holds the reference layer, less visible) vs `requests/ESCALATIONS.md` (free link-checking, and requests/ is literally 'the project's work intake' — but risks reading as a fourth track beside the three that requests/README.md:6-10 and tests/test_repo_structure.py:20 enumerate). Scope recommends root; the user should confirm, because the root-document count is a taste call the repo has been deliberate about.
  - WHERE DOES THE READ SEAM GO? /make-feature-request Step 2 (the moment an item starts — recommended in core), /commit Step 4 (the moment an item lands — tiered as an enhancement), or both? Both is more robust and puts a queue check in the two skills the project runs most; one is cheaper and keeps /commit lightweight per its own SKILL.md:28-30.
  - CONFIRM THE HARD REQUIREMENT. Scope recommends no `unscheduled` escape hatch, on the argument that DESIGN.md section 4 and GAME_DESIGN.md section 8 already serve unmoored questions. If the user disagrees, the alternative that keeps the discipline is a hatch that is itself gated — an entry may name `unscheduled` only if it also names the document it will move to. Flagging because the request explicitly called this an argument rather than a decision.
  - CONFIRM RESOLVED-ENTRIES-STAY. Scope recommends keeping them with a resolution pointer, on the ADR-immutability ethos (docs/decisions/README.md:26-31). The counter-argument is real: a queue whose value is being short enough to read gets worse with every resolved row, and git history already preserves deleted entries. If the user prefers removal, the archive enhancement becomes moot and the file stays permanently small.
  - ADR OR GUARD-TEST-AS-RECORD for the boundary between the parking mechanisms? The 1.1 scope's Decision 3 chose a test over an ADR and tests/test_layering.py:8-10 records why. But the boundary here is prose and cannot be tested, which is the strongest argument for an ADR 0011. Genuinely open.
  - IS [OPEN-10]'s ORPHAN FIXED IN THIS BRANCH? It is a one-line GAME_DESIGN.md addition (or a one-line ROADMAP.md deletion) and it is the cleanest possible demonstration of why H1's test matters. But it edits GAME_DESIGN.md, which this item's non-goals put off limits, and 'does the offseason training payout scale with age' interacts with ADR 0005's carve-out and the deferred aging work (ROADMAP.md:77, :86-99). Recommend: record it as the queue's fourth seed entry pointing at the aging work, and do not decide it here.
  - SHOULD THE QUEUE FILE CARRY THE THREE HYPOTHESES' RE-DECIDE ITEMS AS ASSERTED OR AS PROPOSED? ROADMAP.md:217-222 says each 'is a hypothesis with a named moment to re-decide' but names no moment for two of the three. The serviceability gate has a defensible pointer (Phase 3, whose exit is 'the app is genuinely serviceable', ROADMAP.md:248-249). The design/UX specialist and the autonomous stage dispatcher do not obviously have one, and the hard-require rule means the migration cannot proceed until someone names them. That is the rule biting on day one, which is either the feature working or the first sign the hatch is needed.

---

## Scoper 2

- **scoper:** ambitious
- **ok:** True
- **fit:** - **verdict:** clean
  - **rationale:** Verified. This is process/docs work landing squarely in an idiom the repo already runs, contradicting no accepted ADR. (1) The deliverable is a parse-a-document-and-assert-against-the-filesystem test, which tests/test_repo_structure.py already does five times: test_every_adr_is_listed_in_the_index (line 220) and test_adr_numbers_are_unique_and_contiguous (line 231) are near-identical in shape to 'every entry names a real roadmap moment' and 'entry ids are unique and contiguous'. (2) It touches NO dataset, ledger, economy, or ruleset. Nothing under src/ or app/. So the five dataset contracts (grain / keys / coverage / update semantics / pull cost) and the economy-or-ledger questions (which ruleset version changes, whether careers replay, whether replay shifts recorded history) are all vacuously N/A and should be recorded as such rather than invented. datasets/manifest.json does not exist yet (roadmap item 2.1, NOT STARTED) and this feature does not need it. Side note: the brief cited docs/data-sources.md; that path does not resolve. The real file is docs/data-access.md, and it is about the 2K install and save files, irrelevant here. (3) ADR-boundary check. Four decision registers would then exist: an ADR records a decision MADE (docs/decisions/README.md: immutable once accepted); ADR 0010's Stage plan section pre-registers CEREMONY FOR ONE REQUEST (0010-panels-by-default.md:38-62, enforced by test_every_live_intake_artifact_declares_a_stage_plan, test_repo_structure.py:185); GAME_DESIGN.md section 8 parks GAME-DESIGN questions the user answers by taste ([OPEN-1..9]); the queue parks a BUILD-TIME judgment call with a named moment. That boundary is real but not free, and getting it wrong is the most expensive failure available here, so it must be written into the queue's own header rather than only into this scope. (4) An accepted ADR already argues for it: 0010 rejects 'require the user to approve a stage plan on every item' precisely because 'it makes the human a gate on the routine path, which is the synchronous-interruption tax roadmap item H1 exists to remove' (0010:126-128). H1 is the mechanism 0010 already assumes. (5) Public-repo constraint is satisfied for free: ESCALATIONS.md would be a tracked .md, so tests/test_no_leaks.py already scans it (TEXT_SUFFIXES includes .md, line 42-58). (6) No new CI job is needed. The tests join the existing 'Lint, types, tests' job, so ops/branch-protection.json and tests/test_ci_contexts.py are untouched. Adding a job would have been a real cost (ops/README.md:20-28). The one honest friction, recorded not hidden: the request itself concedes at the roadmap-literal deliverable NOTHING WRITES TO THE QUEUE, which is the same inert-artifact failure ops/README.md:32-37 documents for branch-protection.json. A structural test can prove entries are well-formed; it can never prove anyone parked anything. That is the argument for the wiring tier, and it is why a clean fit verdict on the narrow version would still be a bad build.
- **goals:** - Give a parked decision a durable home with a NAMED MOMENT, so a decision the user has not made stops being a reason for anything to stop (the request's Desired Outcome, FEATURE_REQUEST.md:27-39).
  - Make the third option exist: a worker facing a judgment call can park-and-proceed with the assumption stated, instead of the current two moves of interrupt or guess silently (FEATURE_REQUEST.md:15-18). The Assumed field is the load-bearing one here; nothing in the repo records 'I picked A over B and I wasn't sure'.
  - Make the drain mechanical at roadmap-item boundaries: picking up item 1.3 starts by reading what was parked against 1.3, and that reading is a single file with one section, not a search.
  - Migrate the three hypotheses now stranded in ROADMAP.md:217-222 prose ('Parked, not scheduled' - a serviceability gate for the web UI, a design/UX specialist, an autonomous stage dispatcher) into entries with real moments, and leave the prose pointing at the queue rather than repeating it.
  - Recover the parked decisions ALREADY LOST into _done/ artifacts, which is the proof the queue was needed. Verified strays: requests/feature-requests/_done/1.1-app-shell/PROJECT_SCOPE.md:369 (OpenAPI snapshot test, 'right moment is 1.8. Record the pointer in 1.8's request'), :370 (OpenAPI-to-TypeScript codegen, 'the seam it slots into at 1.8'), IMPLEMENTATION_PLAN.md:859-861 ('.gitignore:63's blanket build/ will silently shadow the build/build-*.py builder pattern CLAUDE.md prescribes for Phase 2. Should become an intake item rather than being rediscovered at 2.1'), and the router-at-1.10 pointer (PROJECT_SCOPE.md:127). All four sit in _done/, which test_request_links.py deliberately skips and which nobody re-reads.
  - Draw the register boundary explicitly and defensibly, so the project does not acquire two places to look for a parked question: queue vs [OPEN-N] vs ADR vs Stage plan, stated in the queue's own header and cross-referenced from GAME_DESIGN.md section 8.
  - Give /diagnose-bug's murky-cause Escalation hand-off a real destination. Today SKILL.md:136-142 writes a section whose Follow-up is 'the RCA panel is NOT built in v1... Until then this bug is NOT ready to plan' and then points nowhere.
  - Close the inert-artifact risk by wiring at least one writer and one reader, so the queue cannot be the branch-protection.json failure mode (ops/README.md:32-37) in document form.
  - Leave a cold agent picking up ANY roadmap item able to answer 'what was parked against this?' in one command, with no institutional memory.
- **non_goals:** - NOT a replacement for [OPEN-N]. GAME_DESIGN.md section 8's nine open design questions and ROADMAP.md:322-338's phase-indexed table stay exactly where they are. This scope adds cross-references, not a migration.
  - NOT a blocking mechanism. User-settled at intake. No test fails because an entry is unresolved, no CI gate, no /commit refusal. An open entry is never an error state.
  - NOT a priority, urgency, or severity system. User-settled at intake. The roadmap moment is the only sort key.
  - NOT an ADR replacement. An entry is a question PARKED; an ADR is a decision MADE. A queue entry that resolves into a structural decision graduates to an ADR and the entry records the pointer.
  - NOT a general TODO list. An entry must be a DECISION with alternatives, not a task. A task with an obvious right answer belongs in a roadmap row or a request.
  - No new CI job, deliberately. Tests join the existing 'Lint, types, tests' job so ops/branch-protection.json and tests/test_ci_contexts.py stay untouched (ops/README.md:20-37).
  - Nothing under src/ or app/. No API surface, no domain model, no read-model table, no ledger event type.
  - No agent ever RESOLVES an entry. Agents park and read; disposition is the user's, same gate as ADR 0010's human handoff.
  - No dataset, no manifest name, no pull. The five dataset contracts are N/A and this scope says so rather than inventing them.
  - No free-text 'unscheduled' escape hatch on the moment field (see enhancement 1 for what replaces it).
  - No automatic archival/rotation of resolved entries in v1, though the file format is designed so a later split is a mechanical move rather than a reformat.
- **acceptance_criteria:** - `uv run pytest tests/test_escalations.py -q` is green, and the module lives beside the existing structural guards with a module docstring in the same idiom as tests/test_repo_structure.py:1-9.
  - test_every_entry_names_a_real_roadmap_moment: the test parses every entry's `When:` value out of ESCALATIONS.md, parses the valid moment vocabulary out of ROADMAP.md itself (item ids from the phase tables including the H-rows' extra Blocks column, phase names, and the literal `post-v1`), and asserts set membership. Its failure message names the offending value AND lists the valid ones. Proven by a negative fixture: a temp ESCALATIONS-shaped file with `When: 9.9` makes the assertion fail, mirroring the negative-fixture idiom in tests/test_ci_contexts.py:102-134.
  - test_entry_ids_are_unique_and_contiguous: entry ids match `^E-(\d{3})$`, are unique, and run 1..N with no gaps - the same assertion trio as test_repo_structure.py:231-240.
  - test_every_entry_carries_the_required_fields: each entry block has all of `When:`, `Parked:` (date), `Assumed:`, `Source:`, and (Resolved entries only) `Resolved:`. A missing field names the entry id and the field.
  - test_open_and_resolved_are_separated: every entry under `## Open` has no `Resolved:` line and every entry under `## Resolved` has one, so the drain query is a section read rather than a per-entry filter.
  - test_roadmap_prose_defers_to_the_queue: `ROADMAP.md` contains the substring `ESCALATIONS.md` and its 'Parked, not scheduled' paragraph no longer enumerates the three hypotheses inline - same substring-assertion idiom as test_repo_structure.py:260-274.
  - The three ROADMAP.md:217-222 hypotheses exist as entries with real moments, asserted by title substring. Named moments proposed: serviceability gate -> `Phase 3` (ROADMAP.md:53-55 makes 'serviceable' a v1 constraint in both directions and Phase 3's Exit is where it first bites); design/UX specialist -> `Phase 3`; autonomous stage dispatcher -> `post-v1`.
  - The four recovered _done/ strays exist as entries: OpenAPI snapshot test -> `1.8`, OpenAPI-to-TS codegen -> `1.8`, client-side router -> `1.10`, `.gitignore` blanket `build/` shadowing the Phase-2 builder pattern -> `2.1`. Each carries a `Source:` pointing at the exact archived artifact and line.
  - tests/test_repo_structure.py::test_core_documents_exist includes `ESCALATIONS.md` in its tuple (currently line 245) and passes.
  - tests/test_request_links.py's SCANNED_TREES (line 24) is extended so ESCALATIONS.md's relative links are checked; `uv run pytest tests/test_request_links.py -q` is green. This is a strict improvement - root documents are currently link-unchecked entirely.
  - test_claude_md_documents_the_escalation_queue: `CLAUDE.md` mentions `ESCALATIONS.md` in its project map, mirroring test_repo_structure.py:276-284.
  - test_diagnose_bug_escalation_points_at_the_queue: `.claude/skills/diagnose-bug/SKILL.md` contains `ESCALATIONS.md`, so the murky-cause hand-off (SKILL.md:136-142) has a destination - same shape as test_both_intake_templates_carry_a_stage_plan_section (test_repo_structure.py:159-182).
  - test_no_entry_impersonates_an_open_design_question: no entry title contains `[OPEN-`, guarding the register boundary against drift.
  - The whole suite stays green: `uv run pytest` passes with no new CI job, and `uv run ruff check`, `uv run ruff format --check`, `uv run mypy` are clean (ops/README.md:106-113).
  - USER-RUN: picking up item 1.3, the user reads the queue's 1.3 entries and reports that the parked context was useful rather than noise. No test can prove a queue is actually being drained; this is the honest half and it must be labelled so the acceptance panel does not claim it.
- **core_scope:** - `ESCALATIONS.md` at the repo root, with a fixed header carrying (a) the one-paragraph statement of what the queue is for, (b) a REGISTER MAP table disambiguating queue vs [OPEN-N] vs ADR vs ADR-0010 Stage plan, and (c) a fenced entry template. Root placement, not docs/, because it joins the core-documents set the repo already asserts exists (test_repo_structure.py:243-246) and because a queue one directory deep is a queue nobody opens.
  - The entry format: a per-entry heading block `### E-NNN - <title>` followed by bold-key lines `When:` / `Parked:` / `Assumed:` / `Source:` and a short body stating the decision and its alternatives. Chosen over a Markdown table because the Assumed field is a sentence or two and a table degrades immediately; the heading grammar parses with one regex, exactly like ADR_FILENAME (test_repo_structure.py:21).
  - Two sections in one file: `## Open` and `## Resolved`. A resolved entry MOVES sections and gains a `Resolved:` pointer (an ADR number, a PR, or a commit). It is never deleted - mirroring docs/decisions/README.md:28-31's rule that the record of what was believed at the time is the value.
  - The hard requirement that every entry names a real moment, with the vocabulary WIDENED rather than escaped: a roadmap item id (`1.3`, `H2`, `2.1`), a phase (`Phase 2`), or `post-v1`. This answers open question 3 without a bare `unscheduled` hatch, which the request correctly calls the path of least resistance.
  - `tests/test_escalations.py` - the structural guard, in the existing parse-and-assert idiom, covering moment validity, id contiguity, required fields, section/resolution consistency, and the register-boundary guard.
  - Seed migration: the three ROADMAP.md:217-222 hypotheses out of prose and into entries, with the prose paragraph rewritten to point at the queue.
  - Seed recovery: the four verified strays from `_done/1.1-app-shell` (PROJECT_SCOPE.md:369, :370, :127; IMPLEMENTATION_PLAN.md:859-861) parked as entries with `Source:` citations. This is what turns the queue from an empty promise into an artifact with immediate content.
  - Discoverability pointers: `CLAUDE.md`'s project map, `README.md`'s repo tree (README.md:57-61), and `requests/README.md`, plus a cross-reference line in `GAME_DESIGN.md` section 8 saying design questions live there and build decisions live in the queue.
  - The AGENT RULE in CLAUDE.md: park, do not guess silently. Three lines stating that a worker hitting a judgment call writes an entry with its Assumed field and keeps going, and never blocks on it. Without this the queue is a file no behavior references.
  - Extending `tests/test_repo_structure.py::test_core_documents_exist` and `tests/test_request_links.py`'s SCANNED_TREES to cover the new document.
- **enhancements:** - - **title:** Give /diagnose-bug's Escalation hand-off a destination
    - **rationale:** SKILL.md:136-142 currently writes a murky-cause section whose Follow-up says the RCA panel is not built and the bug 'is NOT ready to plan' - then points nowhere. A queue entry `When: <the item the buggy code belongs to>` is exactly the missing landing pad, and the name collision the request flags becomes a merge rather than a conflict. Answers open question 1 at the middle boundary: absorb the hand-off that has no home, leave [OPEN-N] alone.
    - **cost:** cheap
  - - **title:** Widen the moment vocabulary to item-id | phase | post-v1 instead of an unscheduled hatch
    - **rationale:** Hard-requiring a roadmap item id creates real pressure to mis-file an entry under whatever item is nearest, which silently corrupts the drain - a worse failure than an honest hatch because it looks correct. Phase and post-v1 granularity keeps the discipline (every entry has a named moment, validated against ROADMAP.md) while giving genuinely unmoored questions somewhere honest to sit. Directly resolves open question 3.
    - **cost:** cheap
  - - **title:** Resolved-in-place with a resolution pointer, and ADR graduation
    - **rationale:** Answers the request's adjacent question in favour of the repo's own ethos: docs/decisions/README.md:28-31 says do not rewrite the record. A resolved entry keeps its Assumed field, which is the interesting part - it shows what was believed while the decision was open. The `Resolved:` pointer accepting an ADR number gives the queue a natural feed INTO docs/decisions/, so the two registers compose instead of competing.
    - **cost:** cheap
  - - **title:** Recover the four lost decisions from _done/1.1-app-shell
    - **rationale:** This is the enhancement that changes the feature from plausible to proven. Four decisions with explicitly named moments (1.8, 1.8, 1.10, 2.1) already exist, already stranded, in artifacts that test_request_links.py deliberately skips (line 36) and that nobody will reopen at 2.1. One of them is a real latent bug: .gitignore:63's blanket build/ will shadow the build/build-*.py pattern CLAUDE.md prescribes for Phase 2. Landing the queue WITH these is a live demonstration rather than an empty schema.
    - **cost:** cheap
  - - **title:** Extend tests/test_request_links.py to cover root documents
    - **rationale:** SCANNED_TREES (line 24) is only requests/ and .claude/skills/, so ROADMAP.md, CLAUDE.md, GAME_DESIGN.md, README.md and DESIGN.md have NO link checking at all today. The queue will be dense with relative links to roadmap rows and archived artifacts, so it needs the check - and every other root doc gets it for free. Reusable win beyond this feature.
    - **cost:** cheap
  - - **title:** The register-map table in the queue's own header
    - **rationale:** The request names this as the expensive-to-get-wrong risk: two overlapping places to look for a parked question. A four-row table (ADR = decided; Stage plan = ceremony for one request; [OPEN-N] = game-design question, taste-answered, phase-indexed; queue entry = build judgment call, moment-indexed) at the top of the file, plus a reciprocal line in GAME_DESIGN.md section 8, converts a scoping argument into a durable artifact a cold agent reads before writing an entry.
    - **cost:** cheap
  - - **title:** Teach /scope-feature and /implement-plan to PARK their deferred items
    - **rationale:** The real payoff, and open question 2's largest option. The pipeline already MANUFACTURES parked decisions at industrial rate - scope-feature/SKILL.md:118 tiers into core/cheap/gated and :175 tags survivors 'core / cheap / gated / deferred', and _done/1.1-app-shell/PROJECT_SCOPE.md:350-373 shows twenty-eight proposals collapsed into a table where four 'drop' rows carry explicit future moments. Those rows are then archived and lost. One added step in each skill's finalize section - 'any deferred item with a named moment becomes a queue entry' - makes the queue self-feeding, which is the only thing that beats the inert-config failure. Grows the build: two SKILL.md edits plus a substring test each, and it changes agent behavior, so it wants the user's explicit yes.
    - **cost:** grows-build
  - - **title:** Teach /make-feature-request and /create-implementation-plan to READ the queue for their item
    - **rationale:** The writer half is useless without the reader half - this IS the drain, and it is what makes the request's observable signal ('item 1.3 can start by reading the queue') true by construction rather than by hope. One step at the head of each skill: read ESCALATIONS.md's ## Open section, surface entries naming this item, note them in the artifact. Grows the build modestly; pairs with the previous enhancement and should be gated with it as one decision.
    - **cost:** grows-build
  - - **title:** One-line /commit surface when an item advances to DONE
    - **rationale:** commit/SKILL.md Step 4 is already the only place guaranteed to run when work lands, and it already reads ROADMAP rows. When it moves an item to DONE, one grep for open entries naming that item and one line in the Step 5 report ('ESCALATIONS - 2 open entries named 1.3') catches the case where the item shipped and its parked decisions were never read. Must stay one grep and one line: commit/SKILL.md:28-30 explicitly says keep this skill lightweight and if it grows a panel something has gone wrong. Gate it on whether the user wants /commit touched at all.
    - **cost:** cheap
  - - **title:** An /update-docs check for queue drift
    - **rationale:** update-docs/SKILL.md already checks ADR invalidation, epistemic honesty, and Index-row agreement. A sixth check - does any entry marked Open point at an item that is now DONE, and did anything in this diff resolve an entry that is still filed Open - is the same class of judgment check and lands in an existing slot rather than a new mechanism.
    - **cost:** cheap
  - - **title:** A read-only drain command: uv run python ops/escalations.py --for 1.3
    - **rationale:** Proposed rather than self-censored, and flagged honestly: the request lists 'anything that reads the queue programmatically' under Not now / later. But the entry format is being designed for machine parsing anyway (the structural test parses it), so the reader is a thin wrapper over a parser that must exist regardless, and it makes the drain a command instead of a habit. Grows the build - a new module, its own tests, and a documented command in ops/README.md. Genuinely gated; the skill-level read step above delivers most of the value without it.
    - **cost:** grows-build
  - - **title:** A worked example entry in the header's fenced template
    - **rationale:** Mirrors the intake templates, and the fenced block is automatically skipped by test_request_links.py's FENCED_BLOCK exemption (line 26) and its <placeholder> rule (line 50), so a template can show the shape of a link it tells you to write without tripping the link checker. Removes the main reason a worker writes a malformed entry: not knowing the shape.
    - **cost:** cheap
  - - **title:** A Supersedes: field for entries that replace an earlier one
    - **rationale:** The repo's ADR ethos already has supersession (docs/decisions/README.md:20, 28-31). A parked question that gets reframed rather than answered - very likely across a multi-phase project - otherwise either gets edited in place (destroying the record) or duplicated (breaking the drain). Optional field, one extra branch in the field test.
    - **cost:** cheap
  - - **title:** Record ROADMAP.md's Blocks-cell semantics in the queue header
    - **rationale:** H1 Blocks 1.3 (ROADMAP.md:197) is binding and the queue is the artifact that could plausibly be mistaken for a second blocking mechanism. One header line - 'a Blocks cell in ROADMAP.md stops work; a queue entry never does' - kills that confusion permanently and reinforces the user-settled non-blocking constraint at the point of use.
    - **cost:** cheap
- **risks:** - THE INERT-ARTIFACT RISK, and it is the dominant one. At the roadmap-literal three deliverables nothing writes to and nothing reads the queue - structurally identical to ops/README.md:32-37's 'This file is inert until it is re-applied'. A structural test proves entries are WELL-FORMED; it can never prove anyone parked anything. If the wiring enhancements are all gated out, the honest expectation is a file with seven seed entries that is never touched again, and the acceptance criteria will still be green. Assessed: high likelihood, and it is the thing the user should decide with eyes open.
  - REGISTER CONFUSION. Four decision registers (ADR / Stage plan / [OPEN-N] / queue) is one more than most projects survive. If the boundary is stated only in this scope and not in the queue's own header, a cold agent files a design question as a queue entry or a build decision as an [OPEN-N], and the project acquires exactly the two-places-to-look failure the request names as expensive. Mitigated by the register-map enhancement; not eliminated.
  - THE MOMENT-FIELD PARSER IS FIDDLIER THAN IT LOOKS. ROADMAP.md's phase tables have five columns (line 151-158, 176-189) but the harness table has six (line 195-198, adding Blocks). A naive positional parser silently drops H1/H2 as valid moments, and the failure is quiet - the test would just reject a legitimate entry. Inferred from reading the tables; must be handled explicitly.
  - ROADMAP CHURN GOES RED AT AN AWKWARD MOMENT. Renumbering or deleting a roadmap row makes every entry naming it fail the test, mid-way through an unrelated roadmap edit. That is the guard working as designed, but the failure message must say so plainly or the next agent will 'fix' it by loosening the assertion.
  - MIS-FILING UNDER THE NEAREST ITEM. Hard-requiring a moment creates pressure to attach a genuinely unmoored question to whatever item is closest, which corrupts the drain while looking correct. This is the strongest argument against the hard requirement and the reason for the widened vocabulary; it is reduced, not removed.
  - SCOPE CREEP INTO SKILL BEHAVIOR. The request's Explicitly out says 'not a change to how the pipeline skills behave', and immediately concedes that whether anything writes to the queue is open. The park/read wiring enhancements sit exactly on that line. They add a STEP, not a change to any skill's decision logic - but that distinction needs to be stated in the scope or an adversary will read it as the scope quietly overriding its own non-goal.
  - FILE GROWTH WITH NO ROTATION. Resolved-in-place plus an append-only ethos means ESCALATIONS.md only grows. At Phase 1 volumes this is a non-issue; by Phase 4 it may not be. The format's two-section split makes a later archive a mechanical move, which is the mitigation, but v1 deliberately ships without one.
  - TOUCHING /commit AT ALL. commit/SKILL.md:28-30 states the keep-it-lightweight rule in its own voice. Even a one-grep addition sets a precedent, and the next feature will cite it. Worth the user disposing explicitly rather than folding in.
  - SEED-ENTRY ACCURACY. The four recovered strays are quoted from archived artifacts; their named moments (1.8, 1.8, 1.10, 2.1) are the ORIGINAL authors' judgments, not re-verified against today's roadmap. Labelled inferred, not verified. Re-parking a stale judgment as if it were current is a small but real way to launder a guess.
- **open_questions:** - WHICH WIRING TIER? Roadmap-literal (three deliverables, inert), plus-discoverability (pointers in CLAUDE.md / README.md / requests/README.md and the park-do-not-guess agent rule), or full skill wiring (scope-feature and implement-plan park; make-feature-request and create-implementation-plan read). This scope recommends plus-discoverability as the floor and full wiring as the build, because the inert-artifact risk is the one that actually kills the feature.
  - REPO ROOT OR docs/? This scope argues root - it joins the core-documents set the repo already asserts (test_repo_structure.py:243-246), and README.md's tree (lines 57-61) has an obvious slot. docs/ keeps the root tidier at the cost of discoverability.
  - SHOULD /commit BE TOUCHED? Its own SKILL.md says keep it lightweight. The value is real (it is the only step guaranteed to run when work lands) and the cost is one grep and one report line, but it is a precedent.
  - IS THE READ-ONLY DRAIN COMMAND IN OR OUT? The request parked it under Not now / later, but the parser it needs is being written anyway for the structural test. Cheap to add now, awkward to retrofit if the entry format drifts first.
  - ARE THE FOUR RECOVERED SEED ENTRIES' MOMENTS STILL RIGHT? 1.8 / 1.8 / 1.10 / 2.1 come verbatim from the 1.1 panel's judgment. Re-park as-is with a Source citation, or re-verify each against the current roadmap before landing?
  - DOES ROADMAP.md's closing open-questions table (lines 322-338) get a cross-reference row pointing at the queue, or does the cross-reference live only in GAME_DESIGN.md section 8? Two pointers is more discoverable; one is less to keep true.

---

## Scoper 3

- **scoper:** minimalist
- **ok:** True
- **findings:** []
- **fit:** - **verdict:** clean
  - **rationale:** Grounded-clean, with one caveat about size discipline. (1) It has a roadmap row that already exists and is IN-PROGRESS (ROADMAP.md:197, H1 `escalation-queue`, Blocks 1.3), so it is not speculative work. (2) Its entire deliverable is a tracked markdown file plus one structural test — the repo's single most established idiom: `tests/test_repo_structure.py` already contains five parse-a-document-and-assert-against-the-filesystem tests (`test_every_adr_is_listed_in_the_index`:220, `test_adr_numbers_are_unique_and_contiguous`:231, `test_index_stage_cells_match_their_artifact_status_headers`:286, `test_every_live_intake_artifact_declares_a_stage_plan`:185). The new test is a sixth of the same shape, in the same module. (3) No ADR conflict. It touches nothing in ADRs 0001-0009 (no save access, no ingestion, no ledger, no ruleset, no training, no draft tier, no repo location, no cost model, no regression). ADR 0010 is adjacent and *supportive*, not conflicting: `0010-panels-by-default.md:127-129` rejects 'require the user to approve a stage plan on every item' on the grounds that it 'makes the human a gate on the routine path, which is precisely the synchronous-interruption tax roadmap item H1 exists to remove.' H1 is already load-bearing inside an accepted ADR. (4) It touches NO dataset — `datasets/manifest.json` does not exist (ROADMAP.md:234 puts it at item 2.1, NOT STARTED) and `docs/data-sources.md` does not exist either (verified: `docs/` contains only `data-access.md` and `decisions/`). The five dataset contracts (grain/keys/coverage/update-semantics/pull-cost) are N/A: nothing is pulled, nothing is cached, `var/` is untouched. (5) It touches NO economy and NO ledger — `src/rpg_core/` is still a version string (CLAUDE.md:23), `careers/` does not exist, no ruleset version changes, no career replays, no recorded history shifts. The three ledger/economy contracts are N/A by construction. CAVEAT on the verdict: fit is clean only for the NARROWEST reading. The request's open question 2 offers a path ('teaching the pipeline skills to park their gated calls') that would edit decision logic in four or more SKILL.md files — that path directly contradicts the request's own Explicitly-out line ('Not a change to how the pipeline skills behave', FEATURE_REQUEST.md:59-60) and would hold item 1.3 hostage to skill-authoring work with zero ledger value behind a binding Blocks cell. Clean fit is contingent on cutting it.
- **goals:** - Ship one tracked file `ESCALATIONS.md` that holds build-time parked decisions, each naming the roadmap row it bears on, so a worker's third option (record and keep going) exists where today there are only two (interrupt, or guess silently).
  - Pin an entry format that is mechanically parseable, so the enforcement test can be a regex over a document rather than a human reading prose — matching the idiom of `tests/test_repo_structure.py:220-240`.
  - Add exactly one test group to the EXISTING `tests/test_repo_structure.py` asserting every entry names a roadmap id that actually appears in a `ROADMAP.md` table row, with a failure message that says which entry and which unknown id, and which of the two documents is likely wrong.
  - Migrate the three hypotheses currently in `ROADMAP.md:217-222` ('Parked, not scheduled' — serviceability gate, design/UX specialist, autonomous stage dispatcher) into the queue as real entries, and DELETE the prose paragraph so there is exactly one source. Carry forward the substantive claim embedded in that paragraph (that 'serviceable' is a v1 constraint in both directions per ROADMAP.md:53-56 and is nowhere defined or tested) rather than losing it in the move.
  - State the queue's boundary IN the file itself: it is not `[OPEN-N]` (GAME_DESIGN.md:290-339, indexed by phase at ROADMAP.md:322-338), it is not `/diagnose-bug`'s Escalation hand-off (`.claude/skills/diagnose-bug/SKILL.md:132-142`), and it is not ADR 0010's Stage plan section.
  - Make the queue discoverable at the cost of at most one line in `CLAUDE.md`'s project-map tree block, so the inert-config failure mode documented at `ops/README.md:30-37` is mitigated without spending a budget that is already blown.
  - Unblock item 1.3 `correction-by-append` fast. H1 is a documentation-and-test change sitting on a binding Blocks cell (ROADMAP.md:197); every hour it grows is an hour the ledger work waits.
- **non_goals:** - Do NOT absorb `[OPEN-N]`. Those ten questions live in `GAME_DESIGN.md` §8 with substantive prose and are already indexed by answering phase in `ROADMAP.md:322-338`. Absorbing them rewrites two documents plus CLAUDE.md's standing rule ('Don't answer [OPEN-N] questions ad hoc', CLAUDE.md:218-219) to produce one index instead of two, at n=10. This is the request's own 'Explicitly out' and the widest option in open question 1 — reject it outright.
  - Do NOT absorb `/diagnose-bug`'s Escalation hand-off. It is a per-bug BLOCKING verdict — `.claude/skills/diagnose-bug/SKILL.md:141` says 'this bug is NOT ready to plan' — while H1's queue is contractually NON-blocking (FEATURE_REQUEST.md:55-57, user-settled). Merging a blocking hand-off into a non-blocking queue destroys the one property the user actually settled. It is a NAME collision, not a mechanism collision.
  - Do NOT teach any pipeline skill to write to the queue. No edit to any `.claude/skills/*/SKILL.md` decision logic. This is the request's own Explicitly-out (FEATURE_REQUEST.md:59-60) and it is the largest option in open question 2. Automating a write protocol before a single entry has been written by hand is designing the format from imagination.
  - Do NOT add a priority, urgency, severity, owner, or due-date field. Settled by the user: the queue drains at roadmap item boundaries, so the item id IS the sort key (FEATURE_REQUEST.md:57-58).
  - Do NOT build a resolved-entry archive, a `_done/` directory, or a `_done/`-style test exemption. The request already tiers this as 'not now / later' (FEATURE_REQUEST.md:61-62) and the file will hold single-digit entries for the whole of Phase 1.
  - Do NOT create a new test module. The request's Constraints section binds the new test to the existing idiom in `tests/test_repo_structure.py`; a `tests/test_escalations.py` fragments the structural-guard layer for one function group.
  - Do NOT define a JSON/YAML schema, a Pydantic model, or any parser under `src/`. Nothing under `src/rpg_core/`, `src/rpg_api/`, or `app/` is touched. The test's parser is a module-level regex in the test file, exactly like `ADR_FILENAME` (test_repo_structure.py:21) and `STAGE_PLAN_HEADING` (:30).
  - Do NOT add an `unscheduled` escape hatch. The drain cadence is item boundaries; an entry naming no item is a write-only sink that never surfaces, which is strictly worse than the `ROADMAP.md` paragraph it replaces. The request itself concedes it 'will absorb entries' (FEATURE_REQUEST.md:118-119). Note the asymmetry: adding the hatch later is a one-alternation regex change; removing it later means migrating every entry written under it.
  - Do NOT let the queue become a home for the ADR 0010 Stage plan call. That decision is made at intake, recorded in the request, and surfaced at an already-human-gated handoff (`0010-panels-by-default.md:44-47`). Parking it here re-creates the alternative ADR 0010 explicitly rejected.
  - Do NOT carry a decision's rationale in an entry. An entry names the question, the assumption made, and where the answer will be recorded. Rationale belongs in an ADR (`docs/decisions/README.md:26-34`).
  - Do NOT extend `tests/test_request_links.py`'s `SCANNED_TREES` (:24) in this item, even though root-doc links are genuinely unchecked today. That change surfaces pre-existing dead links across ROADMAP/CLAUDE/GAME_DESIGN and converts a small harness row into an unbounded link-fixing session behind a Blocks cell.
  - Do NOT add `ESCALATIONS.md` to `test_core_documents_exist` (test_repo_structure.py:243-247). That test guards 'the three documents that carry the design'; a queue is not a design document and conflating them muddies both.
- **acceptance_criteria:** - `uv run pytest tests/test_repo_structure.py -k escalation` is GREEN: for every entry parsed from `ESCALATIONS.md`, the roadmap id it names appears in the set of ids extracted from `ROADMAP.md`'s tables. Failure message names the offending entry id, the unknown roadmap id, and states that either the entry is wrong or the roadmap row moved.
  - NON-VACUITY OF THE ENTRY PARSER (this is the criterion that stops the test being a no-op): the same test asserts the entry regex matched >= 1 entry in `ESCALATIONS.md`, and fails with 'no entries parsed - the entry format drifted from the regex' if it matched zero. Without this, a format change silently turns the guard into `assert not []`.
  - NON-VACUITY OF THE ROADMAP PARSER: the test asserts the id set extracted from `ROADMAP.md` is non-empty AND contains the literals `1.3` and `H1` (both present today at ROADMAP.md:180 and :197). A ROADMAP.md table reformat then fails as 'could not parse roadmap ids' rather than as a false accusation against a valid entry.
  - THE PREDICATE IS PROVEN TO REJECT: the roadmap-id check is a pure module-level helper, and a separate test asserts it returns False for a fabricated id (`9.9`) and True for `1.3` — provable without editing the real `ESCALATIONS.md`. `uv run pytest tests/test_repo_structure.py -k escalation` covers both.
  - MIGRATION HAPPENED: a substring assertion in the same idiom as `test_no_document_still_claims_there_is_no_application` (test_repo_structure.py:260-273) proves all three seeds landed — `ESCALATIONS.md` contains distinctive strings for the serviceability gate, the design/UX specialist, and the stage dispatcher.
  - NO DUPLICATE SOURCE: a substring assertion proves `ROADMAP.md` no longer carries the 'Parked, not scheduled' enumeration (currently ROADMAP.md:217-222) — i.e. the phrase 'Parked, not scheduled' is either absent or reduced to a pointer line containing `ESCALATIONS.md`. This is the criterion that prevents the migration leaving two sources of truth.
  - `uv run pytest` is GREEN across the whole suite — in particular `tests/test_no_leaks.py` (a new tracked `.md` is scanned per its `TEXT_SUFFIXES`, test_no_leaks.py:41-58) and `tests/test_request_links.py` (any pointer added under `requests/` must resolve).
  - `uv run ruff check`, `uv run ruff format --check`, and `uv run mypy` are all GREEN — the four commands `ops/README.md:108-113` says CI runs.
  - CLAUDE.md BUDGET NOT WORSENED BY MORE THAN ONE LINE: `(Get-Content CLAUDE.md).Count` increases by at most 1 versus HEAD. Measured baseline today: 221 real lines, 181 non-blank. This is checkable with one command and exists because the file is already over the 200-line budget stated at `update-docs/SKILL.md:76-77`.
  - USER-RUN, NOT PANEL-CLAIMABLE: whether the queue is actually used — whether any worker parks a decision during item 1.2 instead of interrupting or guessing — cannot be asserted by any test and MUST be marked user-run per `requests/feature-requests/README.md:76-79`. The acceptance panel may not claim it.
- **core_scope:** - `ESCALATIONS.md` at the repo root (the name and location `ROADMAP.md:197` already specifies). Opens with a short header stating: what it is (build-time decisions parked instead of asked), what it is NOT (`[OPEN-N]`, `/diagnose-bug`'s Escalation, ADR 0010's Stage plan), that parking never blocks, and that it drains at roadmap item boundaries.
  - A fixed, regex-parseable entry format. Minimal viable field set: an entry id, the question, `Bears on:` (a roadmap id), `Parked:` (a date), `Assumed:` (what the worker did in the meantime — this field is the whole point of the quieter half of the problem statement, FEATURE_REQUEST.md:15-18), and `Status:` (open | resolved -> pointer). Nothing else.
  - One test group appended to the EXISTING `tests/test_repo_structure.py`, under a new `### Escalation queue` banner comment matching the module's existing sectioning (:46, :66, :141, :249). Two module-level regexes and one pure predicate helper, mirroring `ADR_FILENAME`/`STAGE_PLAN_HEADING`.
  - Migration of the three `ROADMAP.md:217-222` hypotheses into the queue as entries, WITH honest `Bears on:` targets. Verified against the roadmap, plausible honest targets exist for all three: the design/UX specialist and the serviceability gate both bear on Phase 3 (whose Exit at ROADMAP.md:248-249 is literally 'the app is genuinely serviceable'), and the autonomous stage dispatcher bears on H2 `domain-engineer` (ROADMAP.md:198), the only other agent-infrastructure row.
  - Deletion of the `ROADMAP.md` 'Parked, not scheduled' paragraph, replaced by a one-line pointer at `ESCALATIONS.md` — with the paragraph's embedded observation (that 'serviceable' is an untested v1 constraint) preserved inside the corresponding entry rather than dropped.
  - One line added to `CLAUDE.md`'s project-map tree block (CLAUDE.md:51-67) naming `ESCALATIONS.md` and its one-line job. Inside a fenced block, so it is not a link and adds no link-check surface.
  - Resolution rule stated in one sentence: a resolved entry STAYS in the file with its `Status:` flipped and a pointer to the ADR or commit that settled it. No archive, no move, no exemption — consistent with the ADR-immutability ethos at `docs/decisions/README.md:28-31`.
- **enhancements:** - - **title:** Allow a phase header id (`Phase 3`) as a valid `Bears on:` target alongside item ids
    - **rationale:** The honesty valve that makes hard-requiring an id defensible without an `unscheduled` escape hatch. The serviceability gate genuinely bears on Phase 3's Exit condition (ROADMAP.md:248-249), not on any single row — forcing it to name `3.5 fast-entry` would be arbitrary precision, which is exactly the dishonesty the escape hatch was reaching for. A phase is still a named moment, still surfaces at a boundary, and is still mechanically checkable against `ROADMAP.md`'s `### Phase N` headers. Costs one regex alternation and one extra id-extraction pass.
    - **cost:** cheap
  - - **title:** Substring test that `ROADMAP.md`'s 'Parked, not scheduled' prose is gone
    - **rationale:** Already listed as acceptance criterion 6, but worth calling out as an enhancement because nothing forces it: the migration could copy the three hypotheses into the queue and leave the paragraph in place, producing two sources that drift. The test costs three lines and reuses the exact idiom at test_repo_structure.py:260-273.
    - **cost:** cheap
  - - **title:** ADR 0011 recording the queue's boundary against `[OPEN-N]` and `/diagnose-bug`'s Escalation
    - **rationale:** 'Why isn't this one index for every open question in the project?' is precisely the class of thing this repo re-litigates — the request itself names it as 'expensive to get wrong because it would leave the project with two overlapping places to look' (FEATURE_REQUEST.md:130-131), and `update-docs/SKILL.md:100-101` says an unrecorded decision someone would ask 'why did you do it that way' about IS a missing ADR. ~40 lines, and `test_adr_numbers_are_unique_and_contiguous` (:231) plus `test_every_adr_is_listed_in_the_index` (:220) make the cost of adding one mechanical. Gated, not folded: the file's own header covers roughly 80% of this, so it is a judgment call whether the ADR earns its number.
    - **cost:** cheap
  - - **title:** One-line pointer in `requests/README.md` beside the three-track table
    - **rationale:** The queue is the fourth place a decision can be recorded in this repo, and `requests/README.md` is the document a cold agent reads to learn where work is recorded. Costs one line and IS link-checked (test_request_links.py:24 covers `requests/`), so the pointer cannot rot silently — unlike the `CLAUDE.md` one.
    - **cost:** cheap
  - - **title:** A `## Resolved` section at the foot of the file, entries moved down on resolution
    - **rationale:** Keeps the open list short as the file grows without building the archive the request defers. Purely cosmetic ordering — the test must then not care which section an entry sits in, which is one line of parser tolerance. Genuinely optional at single-digit entries; fold only if the format work is happening anyway.
    - **cost:** cheap
  - - **title:** Teach `/commit` or the intake skills to offer to park a gated call
    - **rationale:** DEFER — this is the real payoff named in open question 2 and it is also the request's own Explicitly-out ('Not a change to how the pipeline skills behave'). It edits decision logic across multiple SKILL.md files, pre-commits a write protocol before any entry has been written by hand, and sits behind a binding `Blocks: 1.3` cell. File it as its own request after ~5 entries exist and the format has survived contact.
    - **cost:** grows-build
  - - **title:** Extend `tests/test_request_links.py`'s `SCANNED_TREES` to cover root documents
    - **rationale:** DEFER, but file it — it is a real measured gap. `test_request_links.py:24` scans only `requests/` and `.claude/skills/`, so every relative link in `ROADMAP.md`, `CLAUDE.md`, `GAME_DESIGN.md`, `DESIGN.md`, and `README.md` is unchecked today, including the `ESCALATIONS.md` pointer this item adds. Turning it on will surface pre-existing dead links across five documents and convert a docs-and-one-test item into an open-ended cleanup while item 1.3 waits.
    - **cost:** grows-build
  - - **title:** A per-entry `Surfaced by:` field naming which item's start should read the entry
    - **rationale:** CUT — it is `Bears on:` under a second name. Two fields that must agree is a drift source with no new information, and the drain cadence already answers 'when does this surface'.
    - **cost:** cheap
- **risks:** - INERTNESS — the headline risk, and it is the same failure `ops/README.md:30-37` already documents for `branch-protection.json` ('This file is inert until it is re-applied'). At the roadmap-literal three deliverables, NOTHING writes to the queue. Mitigations available: seed it with three real entries at birth so it is not an empty file, and put pointers where readers already look. But be honest — there is no test that can prove the queue gets used, and criterion 10 is marked user-run for exactly that reason.
  - THE OBSERVABLE SIGNAL IN THE REQUEST MAY NOT FIRE. FEATURE_REQUEST.md:38-39 proposes 'item 1.3 can start by reading the queue and learning what was parked against it.' That only holds if something is parked during 1.2. If nothing is, 1.3 reads an empty result — which is INDISTINGUISHABLE from a queue that is broken, undiscovered, or ignored. The stated success signal is therefore not a signal. Do not write it as an acceptance criterion.
  - THE TEST IS TRIVIALLY VACUOUS IF WRITTEN NAIVELY. A `for entry in parsed_entries: assert ...` over an empty list passes forever. The module's own docstring at test_repo_structure.py:196 concedes a sibling test is 'Vacuous while nothing is in flight'. Here vacuity is worse, because the file will always exist and always look enforced. Criteria 2, 3, and 4 exist specifically to close this and should not be trimmed.
  - ROADMAP-ID PARSING IS BRITTLE AND COUPLES THE TEST TO TABLE FORMATTING. Ids live in the first cell of six separate markdown tables (ROADMAP.md:153-158, 178-188, 197-198, 234-241, 255-259, 270-275) in three shapes: `0.6`, `1.10`, `H1`. A future roadmap refactor breaks the extraction — and this repo has already done exactly such a refactor once: ADR 0010 deleted the `Size` and `Panel` columns from every row (`0010-panels-by-default.md:38-41`). Precedent for the failure class is `ops/README.md:20-28` (CI contexts matching job display names). Mitigation is criterion 3 plus a failure message that names both candidate causes.
  - AN ENTRY CAN BE ORPHANED BY A ROADMAP EDIT, NOT BY ITS OWN FAULT. If a row is renumbered or removed, CI goes red on an entry that was correct when written, and the obvious 'fix' is to weaken the test. The failure message must point at the roadmap first.
  - NAME COLLISION WITH `/diagnose-bug`'s Escalation section (`.claude/skills/diagnose-bug/SKILL.md:132-142`, and its frontmatter at :9). After this lands, 'escalation' means two different things in one repo — one non-blocking and item-scoped, one blocking and bug-scoped. An agent searching for 'escalation' finds both. Cheapest mitigation is a one-line disambiguation in each file; renaming the queue contradicts `ROADMAP.md:197`, which is the spec of record.
  - CLAUDE.md BUDGET IS ALREADY BLOWN, AND THE MEASURING INSTRUMENT LIES. MEASURED: 221 real lines, 181 non-blank, 40 blank. `update-docs/SKILL.md:76-77` prescribes `(Get-Content CLAUDE.md | Measure-Object -Line).Lines` against a 200-line budget — that command returns 181 because `Measure-Object -Line` counts zero lines for an empty string, so it silently discounts every blank line. The budget reads as met with 19 to spare while the file is actually 21 over. Any CLAUDE.md addition here is spending a budget that does not exist. (Separately: this is a latent defect in the update-docs skill worth its own bugfix request — I did not file one, being read-only.)
  - SCOPE CREEP IS THE DOMINANT RISK AND IT HAS A BINDING COST. `ROADMAP.md:197` sets `Blocks: 1.3`, so item 1.3 `correction-by-append` — the append-only correction model that ADR 0003 and ROADMAP.md:57-62 both call load-bearing — cannot start until H1 lands. Every option in open question 2 beyond a pointer trades ledger progress for harness polish. Partial relief: 1.2 `career-ledger` is NOT blocked by H1 and 1.3 needs 1.2 anyway (ROADMAP.md:179-180), so there is genuine slack — but slack is not a licence.
  - THE MIGRATION CAN LOSE CONTENT. `ROADMAP.md:219-222` does not merely list three hypotheses; it also records that 'serviceable' is a v1 constraint in both directions and 'is nowhere defined or tested — that, rather than design polish, is what a gate would be for.' A mechanical copy of the three titles drops the only place that observation is written down.
  - A THIRD PARKING MECHANISM IS BEING ADDED TO A REPO THAT ALREADY HAS TWO THAT WORK. `[OPEN-N]` (GAME_DESIGN.md:290-339) plus its phase index (ROADMAP.md:322-338) is a functioning two-part mechanism for DESIGN questions. The genuinely new capability here is BUILD-TIME parking with the assumption recorded. If the scope drifts toward 'an index of open questions', it has built a duplicate of something that already works and the request's own worst case ('two overlapping places to look') has happened.
  - ASSUMPTION IN THE REQUEST THAT MAY NOT HOLD, LABELLED ASSUMED: that every parked build-time decision naturally has a roadmap item to name. UNCONFIRMED until the three seeds are actually written — 'autonomous stage dispatcher' has no numbered row and maps only to H2 by analogy. If the seeds cannot honestly name targets, hard-requiring an id is being validated against zero real evidence.
  - MAINTENANCE BURDEN IS SMALL BUT NON-ZERO AND PERMANENT. One more root document, one more test to keep green, one more file `/commit` and `/update-docs` must consider on every doc-drift sweep, and one more place `test_no_leaks.py` scans. Justified for a queue that gets used; pure overhead for one that does not. The lightest honest version — three entries, one file, one test group, one pointer — is what keeps that trade positive.
  - NOTHING PROVES THE FORMAT IS WRITEABLE. The entry format will be pinned by every entry written after it (the request's own trigger-3 argument, FEATURE_REQUEST.md:133-135), yet it is being designed from three retrospective hypotheses rather than from a real mid-build parking event. The mitigation is cheapness, not foresight: keep the field set to five, so a format revision at entry six is an edit rather than a migration.
- **open_questions:** - Does `ESCALATIONS.md` live at the repo root or under `docs/`? `ROADMAP.md:197` names the bare filename, which implies root, and root is where a queue meant to be noticed belongs — but root currently holds exactly five documents and `test_core_documents_exist` (test_repo_structure.py:243-247) treats that set as meaningful. Recommendation: root, and explicitly NOT added to that test's list.
  - Is a phase header (`Phase 3`) an allowed `Bears on:` target alongside item ids? This is the narrow question that open question 3 actually reduces to once the `unscheduled` escape hatch is rejected. Recommendation: yes — it is one regex alternation and it is what lets the serviceability-gate entry be honest instead of arbitrarily precise.
  - Can all three seeds name an honest roadmap target? Verify by writing them before finalizing the id rule, not after. If the stage-dispatcher entry cannot honestly name H2, the hard-require decision was validated against nothing.
  - Keep the name `ESCALATIONS.md` despite the collision with `/diagnose-bug`'s Escalation section, and disambiguate in prose in both files? Or rename the queue and accept a `ROADMAP.md:197` edit? Recommendation: keep the name, disambiguate — the roadmap is the spec of record and prose is cheaper than a rename.
  - Does this earn ADR 0011? The boundary against `[OPEN-N]` and the Escalation hand-off is a real decision that will be re-proposed, and `update-docs/SKILL.md:100-101` says that is the definition of a missing ADR. Against it: the file's own header carries most of the same content, and an eleventh ADR for a markdown queue is ceremony. User's call.
  - Should the `CLAUDE.md` pointer be paid for by cutting an existing line? MEASURED: the file is 21 lines over the stated 200-line budget, and the skill's own command cannot see it. Note also a pre-existing drift worth fixing in the same breath: CLAUDE.md:216 still reads 'Nine decisions are settled' while :111 correctly says ten.
  - Is one line in `requests/README.md` wanted? It is the only pointer location covered by `test_request_links.py` (:24), so it is the only one that cannot silently rot — which is a real argument for putting the load-bearing pointer there rather than in `CLAUDE.md`.
